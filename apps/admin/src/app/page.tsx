import { Button } from "@ai-km/ui";

/**
 * Pre-story scaffolding only. This is the precondition for E11 (Admin
 * Console) stories, not an implementation of any of them.
 */
export default function AdminHomePage() {
  return (
    <main style={{ padding: 32 }}>
      <h1>AI KM — apps/admin</h1>
      <p>Scaffold placeholder. Replace with the first E11 admin console story.</p>
      <Button>Placeholder action</Button>
    </main>
  );
}
