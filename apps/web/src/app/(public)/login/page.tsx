/**
 * Route skeleton placeholder for the (public) zone, established by
 * E01-S001. Visual/interaction implementation against the E02 auth
 * contract (still undefined — Team A develops against a mock AuthClient
 * per CLAUDE.md Team A scope) lands in E01-S002.
 */
export default function LoginPage() {
  return (
    <main style={{ padding: 32 }}>
      <h1>Login (placeholder)</h1>
      <p>Awaiting E01-S002 implementation against the E02 auth contract.</p>
    </main>
  );
}
