/**
 * Pre-story scaffolding only — route stub for E01-S001 to build the real
 * login experience against the E02 auth contract (see contracts/openapi).
 */
export default function LoginPage() {
  return (
    <main style={{ padding: 32 }}>
      <h1>Login (placeholder)</h1>
      <p>Awaiting E01-S001 implementation against the E02 auth contract.</p>
    </main>
  );
}
