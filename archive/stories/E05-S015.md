# E05-S015 — Text knowledge input

- 狀態:APPROVED
- Branch:story/E05-S015-text-knowledge-input
- 日期:2026-08-15

## Scope 說明

E05 的第十五個 story。與 S001-S014 相同情況:SOURCE_BASELINE.md 對 E05
只有一份扁平標題清單,本 story 在 epic 檔中只有標題,無進一步內文。已讀
過 epic 檔本 story 自己的章節(line 2952-3161,下一節「Folder sync
setup UI」從 line 3162 開始),並用 `diff` 直接逐字比對本 story 章節
與 S014 章節的內文(排除 H1 標題與「User / System Value」段落唯一的
story 名稱代換),確認兩者逐字相同,是通用 MVP 樣板文字,無本 story
專屬內容。

**與 S011-S014 的關鍵差異:第一次有真實內容可以誠實儲存**——S011-S013
(檔案)刻意不讀取/儲存真實檔案位元組(Frontend/BFF 不得直連 Object
Storage);S014(URL)刻意不擷取任何真實內容(沒有真正的擷取管線)。
本 story 完全相反:使用者**直接輸入的文字本身就是內容**,沒有檔案
要讀、沒有網址要擷取,文字在瀏覽器裡就是真實、完整存在的資料。這讓
本 story 面臨與 S011-S014 相反的問題:不是「該不該假裝有資料」,而是
「明明有誠實可得的真實資料,沒有理由不儲存」。

## 設計決策

1. **`KnowledgeBaseDocument` 新增選填的 `content` 欄位,真的儲存輸入
   的文字**——與 S011-S013(不存真實檔案內容)、S014(不存在任何
   內容)不同,本 story 的文字內容是誠實可得的,不儲存反而會讓
   「文字知識輸入」這個功能名不符實。純新增選填欄位,既有的檔案/
   URL 來源文件完全不受影響(它們的 `content` 就是不存在,不是
   刻意留空)。
2. **`sizeBytes` 是真實計算出來的位元組數(`new Blob([content]).size`),
   不像 S014 的 URL 匯入省略這個欄位**——刻意記錄下這個與 S014
   相反的選擇:URL 匯入省略 `sizeBytes` 是因為根本沒有位元組可回報
   (沒有任何真實擷取發生);本 story 的文字內容確實存在、確實可以
   準確計算真實 UTF-8 位元組長度,計算並記錄它是誠實的,不是捏造
   ——同一套「不假裝擁有不存在的資料」原則,套用到不同情境會得出
   不同(但邏輯一致)的結論。用 `Blob` 而非單純字元數,是因為
   Blob 的 size 計算會正確反映 UTF-8 編碼後的真實位元組數(中文字元
   通常佔 3 位元組,不是 1),與檔案上傳路徑記錄的 `file.size`
   語意一致(都是真正的位元組數,不是字元數)。
3. **標題與內容是兩個獨立欄位,不是單一輸入**——與
   `createKnowledgeBase` 的 `name`/`description` 分離同一種考量,
   也與 `addKnowledgeBaseDocumentFromUrl` 的單一 `url` 欄位不同
   ——網址本身沒有獨立於網址之外的「標題」概念,但使用者輸入的知識
   天生就有「給這段內容一個名稱」與「內容本身」兩個不同東西。
4. **內容為空時明確拒絕(`VALIDATION_ERROR`),不像 S008 的
   `boundPrompt` 允許空白**——`boundPrompt` 的空字串是有意義的合法
   狀態(「不使用自訂提示詞,回退預設」);本 story 的內容若允許
   空白,產生的文件毫無意義——這個能力存在的唯一目的就是新增真實
   內容,因此空白提交比照空白檔名/格式錯誤網址,一律拒絕。
5. **`KnowledgeDocumentTextInput` 是第三個獨立元件,不是塞進既有
   兩者之一**——延續 `KnowledgeDocumentUrlImport` 自己 doc comment
   已經建立的「每種根本不同的互動形狀各自獨立元件」原則:這是
   標題欄位+多行文字框+明確送出,既不是檔案選取器,也不是單一
   網址欄位。渲染在同一個 `/knowledge/[id]/documents` 頁面,共用
   同一個 `refetchDocuments` callback(以 `onAdded` 傳入)。
6. **儲存的 `content` 本身目前不在任何地方顯示**——本 story 的標題
   是「輸入」,不是「檢視」;文件列表沿用既有的 name/size/time
   摘要格式,不新增顯示內容全文的能力(與 S008 的 `boundPrompt`
   摘要只顯示「已設定」、不顯示提示詞全文,是同一種「輸入能力≠
   檢視能力,檢視是自己的、尚未到來的 story」範圍紀律)。
7. **Telemetry 排除標題與內容,但包含 `sizeBytes`**——標題與內容
   都是使用者自由輸入的文字,延續 S008(提示詞)/S011(檔名)/
   S014(網址)一致的「不記錄自由格式內容」克制;`sizeBytes` 是
   安全的數字型資料,延續既有先例記錄。

## 開發過程中的技術發現

無 FIX 循環——全部 gate(typecheck/lint/unit/E2E/build)第一輪即
全數通過。這是本批 E05 story 中少見的「零 FIX 循環」案例,可能與
本 story 大量直接重用 S008(標題/內容雙欄位、submit-based 表單)、
S011-S014(disabled-until-valid 按鈕、pending 期間鎖定欄位、失敗
保留草稿、具體錯誤訊息、telemetry 排除自由格式內容)已經反覆驗證過
的既有模式有關,沒有引入新的互動機制或瀏覽器 API(不像 S013 的
`webkitdirectory`、S014 的 `type="url"` 各自帶來意料之外的框架
行為)。

## 變更檔案

- `apps/web/src/lib/knowledge-documents.ts`——`KnowledgeBaseDocument`
  新增選填的 `content?: string`(見設計決策 #1);新增
  `addKnowledgeBaseDocumentFromText(knowledgeBaseId, title, content)`
  ——trim 後拒絕空白標題與空白內容(皆為 `VALIDATION_ERROR`)、
  重用 `getKnowledgeBase` 確認 KB 存在(`NOT_FOUND`)、成功時建立
  含真實計算 `sizeBytes`(`new Blob([...]).size`,見設計決策 #2)與
  `content` 的文件記錄。未修改 `addKnowledgeBaseDocument`/
  `addKnowledgeBaseDocumentFromUrl` 本身的簽名或行為。
- `apps/web/src/lib/knowledge-documents.test.ts`——新增獨立 describe
  block「addKnowledgeBaseDocumentFromText (E05-S015)」共 8 個測試:
  成功新增且 `sizeBytes` 為真實計算值;**多位元組字元的位元組長度
  正確性測試**(「知識」2 字元但應為 6 UTF-8 位元組,直接驗證
  Blob 計算的是位元組而非字元數);trim 標題與內容前後空白;拒絕
  空白標題且不產生 side effect;拒絕空白內容且不產生 side effect;
  不存在的 KB 回傳 NOT_FOUND;不影響 KB 既有(無 `content` 的)
  檔案來源文件;**關鍵安全性質測試**——新增文字知識不會讓該 KB 從
  `listKnowledgeBases()` 消失。
- `apps/web/src/app/(app)/knowledge/[id]/documents/_components/knowledge-document-text-input.tsx`
  (新增)——標題文字輸入 + 多行內容文字框(鏡射
  `KnowledgePromptEditor`,S008)+「新增」按鈕(兩欄皆為空白時
  disabled);pending 期間兩欄與按鈕皆 disabled;成功清空兩欄並呼叫
  `onAdded`;失敗顯示具體訊息(鏡射 S014 的「顯示具體而非通用訊息」
  設計)且保留兩欄已輸入內容;任一欄位重新輸入時清除前次錯誤訊息。
- `apps/web/src/app/(app)/knowledge/[id]/documents/_components/knowledge-document-text-input.test.tsx`
  (新增)——12 個測試涵蓋:兩欄皆空時按鈕 disabled;僅填一欄時
  按鈕仍 disabled(各一測試);兩欄皆填後按鈕 enabled;成功新增
  呼叫正確參數、清空兩欄、呼叫 onAdded;內容被後端拒絕時顯示 distinct
  錯誤且保留兩欄;NOT_FOUND 顯示不同的具體訊息(證明訊息非寫死);
  重新輸入清除前次錯誤;pending 期間兩欄與按鈕皆 disabled;成功/
  失敗 telemetry 各一測試(**明確斷言所有 trackEvent 呼叫皆不含
  標題或內容文字,但成功事件包含 sizeBytes**)。
- `apps/web/src/app/(app)/knowledge/[id]/documents/_components/knowledge-document-list.tsx`
  ——新增 import 並渲染 `<KnowledgeDocumentTextInput>`(與既有兩者
  同頁);未修改既有的條件式大小渲染邏輯(文字輸入的文件永遠有真實
  `sizeBytes`,既有的「僅在 `sizeBytes !== undefined` 時渲染」邏輯
  已經正確處理,不需要任何進一步修改);更新 doc comment。
- `apps/web/src/app/(app)/knowledge/[id]/documents/_components/knowledge-document-list.test.tsx`
  ——新增 `addKnowledgeBaseDocumentFromText` 的 mock;新增 2 個測試:
  載入後三個新增文件的元件(上傳/URL 匯入/文字輸入)同時顯示;文字
  輸入成功後 `listKnowledgeBaseDocuments` 被呼叫第二次且新文件無需
  重新整理頁面即出現(鏡射既有的上傳/URL 匯入整合測試)。
- `tests/e2e/specs/knowledge-documents.spec.ts`——新增 1 個關鍵流程
  測試:對原本 0 份文件的 KB(人力資源與請假規範)輸入標題與內容
  (新增按鈕原本 disabled 先確認),送出後確認清單顯示新文件、兩欄
  清空、**畫面上確實出現格式化過的真實大小文字**(與 S014 的 URL
  匯入測試刻意驗證「無大小顯示」恰成對照)、返回詳情頁「1 份文件」
  正確。全部 gate 一次通過,無 FIX。
- `docs/stories/PROGRESS.md`——E05-S015 todo→in-progress→done;
  in-progress 轉換時同批次更新總覽表計數,並用逐列手動計數比對
  複核。

未修改 `apps/web/src/app/(app)/knowledge/[id]/documents/_components/knowledge-document-upload.tsx`/
`knowledge-document-url-import.tsx`(S011-S014 既有元件本體)、
`format-file-size.ts`、`file-attachment-picker.tsx`(E03 domain)、
`packages/permissions`、`packages/auth-client`。

## Contract / Migration 差異

None——純前端 mock。`contracts/openapi/core.yaml` 未變更。
`KnowledgeBaseDocument.content` 為向後相容的新增選填欄位;
`addKnowledgeBaseDocumentFromText` 為全新 export。

## Gate 紀錄

| Gate | 指令 | Exit code | 關鍵測試 |
|---|---|---|---|
| typecheck | `pnpm typecheck`(自 repo root) | 0 | 20/20 tasks,乾淨(一次通過,含 `new Blob(...)` 的型別) |
| lint | `pnpm lint`(自 repo root) | 0 | 20/20 tasks(一次通過) |
| unit | `pnpm test`(自 repo root,turbo 全 workspace) | 0 | `@ai-km/web`:697/697(676+21 新增,一次通過) |
| E2E | 同上(`pnpm test` 觸發 `@ai-km/e2e:test`) | 0 | 137/137(含新增的 E05-S015 測試,一次通過) |
| build | `pnpm build`(自 repo root) | 0 | 12/12 tasks(`/knowledge/[id]/documents` 大小反映新增的文字輸入元件) |

**FIX 循環:0 次**(全部 gate 第一輪即通過,見上方「開發過程中的
技術發現」的說明)。

L2 contract test:不適用。L3 integration:不適用(純前端 mock)。
L4 security-negative:與 S010-S014 相同分析,存取控制產生相同的
可觀察結果。**本 story 特有的安全性驗證**:
`knowledge-documents.test.ts` 的「is a setting only」測試證明新增
文字知識不會讓 KB 從清單消失;多位元組字元測試證明 `sizeBytes`
計算的正確性(避免未來如果改用字元數而非位元組數導致的度量錯誤)。
未修改 `role-guard.tsx`/`nav-items.ts`/`packages/permissions`/
`packages/auth-client`。

## AC 對照

| AC | 實作位置 | 測試 |
|---|---|---|
| Functional 1(合法輸入→預期成功結果) | 輸入標題+內容→新增→清單即時顯示新文件(含真實大小)、兩欄清空 | `knowledge-document-text-input.test.tsx`「adds the entered title and content...」;`knowledge-document-list.test.tsx`「refreshes the document list after successfully adding text knowledge...」;E2E |
| Functional 2(缺少必要輸入→fail-closed) | 空白標題/空白內容皆回傳 `VALIDATION_ERROR`,不產生任何 side effect | `knowledge-documents.test.ts` 對應 2 個測試 |
| Functional 3(無權限使用者不得讀取受保護內容) | 不適用——與 S010-S014 相同分析;**額外驗證**:新增的文字知識不會外洩或讓自己從清單消失 | 同既有判斷 + 「is a setting only」測試 |
| Functional 4(dependency 失敗→可分類錯誤) | 新增失敗顯示具體錯誤訊息(依失敗原因而不同) | 「shows a distinct error...」與「shows a different specific message...」兩則對照測試 |
| Functional 5(重複請求/重試不得造成未定義重複 side effect) | 兩欄與按鈕在 pending 期間皆 disabled,直接驗證競態防護 | 「disables both fields and the button while an add is in flight...」 |
| Functional 6(correlation id / structured telemetry) | `logger`/`trackEvent`(`knowledge_base_document_text_input_attempt/success/failure`)皆帶 `correlationId` | 兩個 telemetry 測試 |
| Functional 7(敏感操作 audit event) | 不適用(與 S011-S014 同理)——新增知識內容是內容新增,非存取控制變更 | — |
| Functional 8(MVP 可簡化但功能本身不可缺席) | 完整可用的文字知識輸入能力:真實驗證、真實(mock)持久化(含真實內容與真實計算大小)、真實清單即時更新 | 全部新測試 |
| Security(Authorization/Deny-Wins/stale cache) | 不適用——與 S006-S014 相同分析 | 同上 |
| Data/Contract(向後相容、UTC 時間) | `content?`/新函式皆為向後相容新增,既有呼叫端零修改仍通過 typecheck | typecheck 全綠 |
| UX(loading/empty/success/validation/error 狀態皆有明確呈現;keyboard 可操作) | 空白/已輸入未送出/pending/成功/失敗五態皆有明確處理;原生 form/input/textarea/button 天生可鍵盤操作 | `knowledge-document-text-input.test.tsx` 全部測試;E2E |

## SELF-REVIEW checklist 結果

- ✅ 每條 AC 都有對應實作 + 對應測試(見上表;不適用項目皆有明確
  理由)。
- ✅ 無 scope 外變更——變更檔案清單中每一個檔案都在 `apps/web`/
  `tests/e2e`(Team A 範圍)內;`knowledge-document-upload.tsx`/
  `knowledge-document-url-import.tsx`(既有元件本體)完全未修改;
  `pnpm test` 全量 697 unit + 137 E2E 皆綠已驗證。
- ✅ 無發明 contract 未定義的 endpoint/欄位/權限——`addKnowledgeBaseDocumentFromText`
  是全新 Team A mock export,不涉及任何真實索引/embedding 處理,
  `contracts/openapi/core.yaml` 未變更。
- ✅ 未偷跑其他 story 的範圍——不做資料夾同步設定 UI(S016)、
  上傳/解析/索引進度或失敗狀態(S017-S020);不顯示已儲存的完整
  內容(檢視能力是自己的、尚未到來的 story,見設計決策 #6);不做
  真實的 chunking/embedding(E06,Team B)。
- ✅ Security AC:延續 S006-S014 已完整記錄的分析,額外用專屬測試
  驗證「is a setting only」與 UTF-8 位元組計算正確性。
- ✅ UX AC:空白/已輸入未送出/pending/成功/失敗五態皆有明確處理。
- ✅ 無 debug 殘留——對本次變更的所有新增/修改檔案逐一搜尋
  `console.`/`TODO`/`FIXME`/`debugger`/`.skip(`/`.only(`/
  `passWithNoTests`/`|| true`皆為 0 筆。
- ✅ 無「mock 通過就當作整合完成」的宣稱——本 EVIDENCE 誠實記錄
  `content`/`sizeBytes` 是本 story 唯一一次「有真實資料就誠實儲存」
  的情境(與 S011-S014 一致的「不假裝擁有不存在的資料」原則,套用
  在相反方向的判斷上),且明確說明為何與 S014 的省略決定不衝突。

## Assumptions / 計畫外事項

1. **`content` 真的儲存輸入的文字,與 S011-S014 的「不存真實資料」
   形成對照但邏輯一致**——見設計決策 #1、Scope 說明。
2. **`sizeBytes` 是真實計算值(UTF-8 位元組),不像 S014 省略**——
   見設計決策 #2 的完整理由。
3. **空白內容明確拒絕,不像 S008 的 `boundPrompt` 允許空白**——見
   設計決策 #4,因為兩者的「空白」代表的意義不同。
4. **儲存的內容目前不在任何地方顯示,是刻意的範圍界線**——見設計
   決策 #6。
5. **本 story 零 FIX 循環**——大量重用已驗證過的既有互動模式,誠實
   記錄於「開發過程中的技術發現」。

## 獨立審核結果

**APPROVE**(第 1 輪),0 個 BLOCKER / 0 個 MAJOR / 0 個 MINOR。

審核者(未參與實作,以獨立視角重跑全部驗證)對本 story 與 S014
方向相反的核心設計判斷(「有真實資料就該誠實記錄」)投入最高查證
力道:

1. **UTF-8 位元組長度宣稱的獨立驗證**:不信任測試自己的斷言,直接
   另外執行 `node -e 'console.log(new Blob(["知識"]).size)'`,獨立
   確認輸出為 `6`——證實「知識」2 個字元、6 個 UTF-8 位元組
   (每個 CJK 字元 3 位元組)這個具體技術宣稱屬實,不是編造的
   數字。
2. **`content`/`sizeBytes` 真的被儲存且為真實計算值的宣稱**:讀
   `addKnowledgeBaseDocumentFromText` 原始碼確認
   `content: trimmedContent` 與
   `sizeBytes: new Blob([trimmedContent]).size` 兩者都在建構文件
   物件時真的被設定,不是文件描述與程式碼脫節。
3. **「既有函式完全未修改」的宣稱**:讀 diff 確認
   `addKnowledgeBaseDocument`/`addKnowledgeBaseDocumentFromUrl` 兩者
   在 diff 中皆無任何 `+`/`-` 行,新函式是純粹接在檔案尾端新增。
4. **「telemetry 排除標題與內容,但包含 sizeBytes」的宣稱**:確認
   三個 `trackEvent` 呼叫的 `properties` 分別只有 `knowledgeBaseId`
   (attempt)、`knowledgeBaseId`+`code`(failure)、
   `knowledgeBaseId`+`sizeBytes`(success)——沒有任何一處包含
   `title`/`content`。
5. **跨文件來源不互相污染的測試品質**:讀「appends to the existing
   list without disturbing...」測試,確認明確斷言既有 3 筆檔案來源
   文件在新增 1 筆文字知識後,`content` 欄位仍是 `undefined`——
   直接證明不同來源的文件彼此不會互相污染欄位。
6. **零 FIX 循環的宣稱**:獨立重跑全部 gate,一次性全部通過,與
   DEV 階段記錄的「零 FIX」一致,無需要求解釋任何修正歷程。
7. **邊界檢查**:確認全部 9 個變更檔案落在 `apps/web`/`tests/e2e`/
   `docs/stories`;`knowledge-document-upload.tsx`/
   `knowledge-document-url-import.tsx`(既有元件本體)/
   `packages/permissions`/`packages/auth-client`/
   `AI_KM_BMAD_High_Granularity/`/`contracts/`/Team B 資料夾皆未
   觸碰。
8. **造假掃描**:對整份 diff(排除 EVIDENCE 檔本身)搜尋
   `console.`/`TODO`/`FIXME`/`debugger`/`.skip(`/`.only(`/
   `passWithNoTests`/`|| true`,0 筆命中。
9. **範圍宣稱複驗**:`grep` 確認 `# E05-S015` 位於 line 2952、
   `# E05-S016` 位於 line 3162;獨立重跑 `diff`(line 2743-2951 vs.
   2953-3161)確認兩個 story 章節內文逐字相同,僅 story 名稱代換
   那一行不同。
10. **獨立重跑全部 gate,且全部 force(0 cache)**:
    - `pnpm turbo run typecheck lint --force` → 32/32 tasks,0 cached。
    - `pnpm turbo run build --force` → 12/12 tasks,0 cached。
    - `pnpm turbo run test --force` → 13/13 tasks,0 cached,697 unit +
      137 E2E(含新增的 E05-S015 測試)全過。
    - 另以 `pnpm --filter @ai-km/web test -- run` 單獨確認
      697/697,與 DEV 階段數字完全一致。
11. **PROGRESS.md 逐列手動計數複核**:approved 14 列、done 1 列、
    in-progress 0 列、todo 16 列,14+1+0+16=31 與總覽表列完全一致;
    總計列 67+1+0+0+107=175 正確。

未發現任何 BLOCKER、MAJOR 或 MINOR。本 story 與 S014 形成的「省略
vs. 誠實記錄」對照(URL 匯入無資料可報 vs. 文字輸入有真實資料誠實
記錄)是同一套原則在兩種情境下得出不同但邏輯一致結論的良好示範,
審核者認為這種前後呼應的設計論證品質值得在收尾報告中特別記錄。

## Rollback

Revert 本 story 的 merge commit 即可。變更集包含純新增檔案
(`knowledge-document-text-input.tsx`/`.test.tsx`)、以及對既有檔案
的追加式修改(`knowledge-documents.ts` 新增選填欄位與函式、
`knowledge-document-list.tsx` 新增元件渲染、`.spec.ts` 新增測試)
——revert 不影響任何既有 export 的簽名。`addKnowledgeBaseDocument`/
`addKnowledgeBaseDocumentFromUrl`(S011-S014 的既有路徑)完全未被
觸碰,revert 不會對既有功能造成任何影響。
