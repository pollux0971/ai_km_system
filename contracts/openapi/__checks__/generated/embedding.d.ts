/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/embeddings": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Embed a batch of texts.
         * @description Batching is in the contract because it is the difference between one round trip and N against a real model. The order of `data` MUST match the order of `input`; consumers index into it positionally.
         */
        post: operations["createEmbeddings"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        EmbeddingRequest: {
            input: string[];
            /** @description Optional pin. Omitting it means "provider default", which is acceptable at PF1/PF2 but MUST be explicit for any PF3 evidence — an evaluation result that does not name its model is not reproducible. */
            model?: string;
        };
        EmbeddingResponse: {
            model: string;
            dimensions: number;
            data: {
                index: number;
                embedding: number[];
            }[];
        };
        /**
         * @example {
         *       "code": "VALIDATION_ERROR",
         *       "message": "input 必須是至少一個字串的陣列。"
         *     }
         */
        ValidationErrorBody: components["schemas"]["Error"] & {
            /** @enum {string} */
            code: "VALIDATION_ERROR";
        };
        /**
         * @example {
         *       "code": "PAYLOAD_TOO_LARGE",
         *       "message": "一次最多只能嵌入 256 段文字。"
         *     }
         */
        PayloadTooLargeBody: components["schemas"]["Error"] & {
            /** @enum {string} */
            code: "PAYLOAD_TOO_LARGE";
        };
        /**
         * @example {
         *       "code": "EMBEDDING_UNAVAILABLE",
         *       "message": "嵌入模型目前無法使用。"
         *     }
         */
        EmbeddingUnavailableBody: components["schemas"]["Error"] & {
            /** @enum {string} */
            code: "EMBEDDING_UNAVAILABLE";
        };
        Error: {
            /**
             * @description Stable, machine-readable failure code. Deliberately free-form (not an enum) — each domain spec defines and documents its own codes; turning this into a shared enum would make every new domain code a breaking change here. Platform-wide codes used across every domain (not specific to one spec's own endpoints) are documented here for discoverability:
             *     `CSRF_HEADER_MISSING` (403) — E04-S048, ADR 0005 addendum. A state-changing (POST/PUT/PATCH/DELETE) request was missing the `x-requested-with` header, or — for `multipart/form-data` requests specifically — had no `Origin`/`Referer` matching the configured allowlist. Returned instead of a domain-specific code so the check can sit in front of every domain uniformly. Never distinguishable from a session-related 401/403 by response shape alone; only the `code` differs.
             */
            code: string;
            message: string;
            details?: {
                [key: string]: unknown;
            };
        };
    };
    responses: {
        /** @description Malformed request. `VALIDATION_ERROR` is the platform-wide code from ADR 0003 §4, not an endpoint-specific one — the shape of a bad request is the same everywhere and consumers already branch on it. */
        ValidationError: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ValidationErrorBody"];
            };
        };
        /** @description Batch larger than the provider accepts (`input` maxItems). */
        PayloadTooLarge: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["PayloadTooLargeBody"];
            };
        };
        /** @description Model unavailable. Callers MUST surface this rather than falling back to an empty result — an empty embedding silently retrieves nothing and is indistinguishable from "no matching documents". Distinct from the platform-generic `SERVICE_UNAVAILABLE` so a caller can tell "the embedding model is down" from "the API is down". */
        EmbeddingUnavailable: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["EmbeddingUnavailableBody"];
            };
        };
    };
    parameters: never;
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    createEmbeddings: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["EmbeddingRequest"];
            };
        };
        responses: {
            /** @description Vectors, in input order. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["EmbeddingResponse"];
                };
            };
            400: components["responses"]["ValidationError"];
            413: components["responses"]["PayloadTooLarge"];
            503: components["responses"]["EmbeddingUnavailable"];
        };
    };
}
