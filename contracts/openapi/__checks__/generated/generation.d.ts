/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/generate": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Generate a grounded answer from retrieved context. */
        post: operations["generateAnswer"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        GenerationRequest: {
            question: string;
            context: components["schemas"]["ContextChunk"][];
            model?: string;
        };
        ContextChunk: {
            chunkId: string;
            documentId: string;
            text: string;
            startOffset: number;
            endOffset: number;
            score?: number;
        };
        GenerationResponse: {
            answer: string;
            citations: components["schemas"]["Citation"][];
            model: string;
        };
        /** @description chunkId MUST appear in the request's `context`. Offsets are character offsets into the ORIGINAL document, carried unchanged from chunking, so the UI can highlight the exact span. */
        Citation: {
            chunkId: string;
            documentId: string;
            startOffset: number;
            endOffset: number;
        };
        /**
         * @example {
         *       "code": "VALIDATION_ERROR",
         *       "message": "question 不得為空。"
         *     }
         */
        ValidationErrorBody: components["schemas"]["Error"] & {
            /** @enum {string} */
            code: "VALIDATION_ERROR";
        };
        /**
         * @example {
         *       "code": "GENERATION_NO_CONTEXT",
         *       "message": "沒有可引用的來源,無法作答。"
         *     }
         */
        GenerationNoContextBody: components["schemas"]["Error"] & {
            /** @enum {string} */
            code: "GENERATION_NO_CONTEXT";
        };
        /**
         * @example {
         *       "code": "GENERATION_UNAVAILABLE",
         *       "message": "生成模型目前無法使用。"
         *     }
         */
        GenerationUnavailableBody: components["schemas"]["Error"] & {
            /** @enum {string} */
            code: "GENERATION_UNAVAILABLE";
        };
        Error: {
            /**
             * @description Stable, machine-readable failure code. Deliberately free-form (not an enum) — each domain spec defines and documents its own codes; turning this into a shared enum would make every new domain code a breaking change here. Platform-wide codes used across every domain (not specific to one spec's own endpoints) are documented here for discoverability:
             *     `CSRF_HEADER_MISSING` (403) — E04-S048, ADR 0005 addendum. A state-changing (POST/PUT/PATCH/DELETE) request was missing the `x-requested-with` header, or — for `multipart/form-data` requests specifically — had no `Origin`/`Referer` matching the configured allowlist. Returned instead of a domain-specific code so the check can sit in front of every domain uniformly. Never distinguishable from a session-related 401/403 by response shape alone; only the `code` differs.
             */
            code: string;
            message: string;
            details?: {
                [key: string]: unknown;
            };
        };
    };
    responses: {
        /** @description Malformed request. `VALIDATION_ERROR` is the platform-wide code from ADR 0003 §4, not an endpoint-specific one. */
        ValidationError: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ValidationErrorBody"];
            };
        };
        /** @description Context was empty or unusable. The provider MUST NOT answer from parametric knowledge when retrieval returned nothing — an uncited answer in a knowledge-management product is indistinguishable from a hallucination to the person reading it. This is a distinct code rather than an empty 200 precisely so a caller cannot mistake abstention for a successful answer with no sources. */
        GenerationNoContext: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["GenerationNoContextBody"];
            };
        };
        /** @description Model unavailable. Distinct from the platform-generic `SERVICE_UNAVAILABLE` so a caller can tell "the generation model is down" from "the API is down". Fails closed: never silently rerouted to an unapproved provider (baseline §5 rule 29). */
        GenerationUnavailable: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["GenerationUnavailableBody"];
            };
        };
    };
    parameters: never;
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    generateAnswer: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["GenerationRequest"];
            };
        };
        responses: {
            /** @description An answer with structured citations. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["GenerationResponse"];
                };
            };
            400: components["responses"]["ValidationError"];
            422: components["responses"]["GenerationNoContext"];
            503: components["responses"]["GenerationUnavailable"];
        };
    };
}
