# contracts/permissions

Shared permission/role model contract, owned by Team B (E02 Identity, RBAC &
Authorization). Team A's `packages/permissions` re-exports the agreed
TypeScript types once this is populated — do not fork the model. Not yet
populated.
