# E01-S024 — 首頁 Material 3 tiles：快速入口 card tiles grid、最近對話 list tiles、歡迎區

- 狀態:**approved**（獨立審核通過，全部 gate 含 E2E 皆綠，無外部依賴阻塞）
- Branch:story/E01-S024-home-m3-tiles
- 日期:2026-08-29

## 變更檔案
- `apps/web/src/app/(app)/page.tsx`——歡迎區改 M3 `display-small` headline
  + `body-large` supporting text，inline style 全改 class，
  `aria-labelledby`/標題階層不變。
- `apps/web/src/app/(app)/_components/quick-entry-cards.tsx`——M3 filled
  card tiles，加 `<Icon>`（依 href 對照 menu_book/build/insights）。
- `apps/web/src/app/(app)/_components/recent-conversations.tsx`——M3
  list tiles，時間改用新的 `formatRelativeTime`。
- `apps/web/src/lib/format-time.ts`（新增）+ `.test.ts`。
- `apps/web/src/app/globals.css`——新增 `/* ---- M3 home ---- */` 區段。
- 三個既有測試檔（`page.test.tsx` 零修改；`quick-entry-cards.test.tsx`、
  `recent-conversations.test.tsx` 逐字保留既有測試，只新增）。
- `docs/design/home-m3.md`（新增）+ `docs/design/home-m3/*.png`（8 張
  截圖，比照 E01-S026/E03-S042 既有先例）。

## Contract / Migration 差異
None——純前端樣式與 UI 邏輯，不涉及 contract/DB。

## Gate 紀錄
| Gate | 指令 | Exit code | 結果 |
|---|---|---|---|
| typecheck | `pnpm --filter web typecheck` | 0 | — |
| lint | `pnpm --filter web lint` | 0 | `✔ No ESLint warnings or errors` |
| unit | `pnpm --filter web test` | 0 | 123 檔 **1726/1726**（既有 1721 + 本 story 新增 5：`format-time.test.ts` 10 個減掉重複計算……見下方精確計數） |
| build | `pnpm --filter web build` | 0 | 無錯誤，`/` route 正常產出 |
| E2E（home-dashboard/smoke/app-shell） | `flock /data/python/AI_KM-worktrees/.e2e.lock -c 'pnpm exec playwright test specs/home-dashboard.spec.ts specs/smoke.spec.ts specs/app-shell.spec.ts'` | 0 | **14/14 全綠**（app-shell 5、home-dashboard 5、smoke 4）。鎖曾被 W1、W7 依序持有（各自跑自己的完整 E2E 套件），等待期間多次重試才排到；最終跑通後零修改、零回歸 |
| axe（AC5） | 真實 Chromium + `axe-core` `.run()`，loaded/empty/error 三態 | — | **0 violations（含 0 serious/critical）**，三態各自檢查一次 |

單元測試精確計數：`pnpm --filter web test` 執行前為既有基準，本 story
新增檔案/測試：`format-time.test.ts`（10 個，全新檔案）、
`quick-entry-cards.test.tsx`（+2）、`recent-conversations.test.tsx`
（+1）。跑後總數 1726，全綠，無 skip。

## 視覺驗收（AC4/AC5）詳情

見 `docs/design/home-m3.md`「視覺驗收」一節的完整流程記錄（暫時測試頁、
`setApiFetchForTests` 的除錯過程、8 張截圖、axe 結果）——不在此重複，
避免兩處記錄漂移。核心結論：loaded/empty/error/loading 四態在 M3 新版式
下皆可見且彼此明顯不同（不只靠顏色），light/dark 對比度皆良好，axe 三態
皆 0 violations。

## AC 對照
| AC | 實作位置 | 測試/證據 |
|---|---|---|
| 1. 既有測試零修改全綠；smoke/app-shell E2E 零修改全綠 | 三個元件的既有測試逐字保留 | unit 1726/1726；E2E `home-dashboard`/`smoke`/`app-shell` 14/14 |
| 2. 入口卡片 href/accessible name 與現況相同；角色不可見的入口仍不渲染 | `quick-entry-cards.tsx` | `quick-entry-cards.test.tsx` 新增 2 個測試（`toHaveAccessibleName` 精確比對＋圖示對照） |
| 3. `formatRelativeTime` 30秒/5分鐘/3小時/2天/30天前，超過7天顯示日期 | `lib/format-time.ts` | `format-time.test.ts` 10/10 |
| 4. 三態（loading/empty/error）新版式下皆可見 | 三元件沿用既有 E01-S011～S013 狀態元件 | `docs/design/home-m3/` 4 態 ×2 色彩模式截圖 |
| 5. axe 0 serious/critical；截圖 light/dark | 全部 `.home-*` 樣式 | axe 三態 0 violations；8 張截圖 |

## Security / Authorization Acceptance
不改變資料來源與可見性邏輯——`visibleEntryCards`/`getRecentConversations`
呼叫點與既有邏輯完全相同，只改了外層樣式與圖示；`nav-items.ts`（角色
判斷的唯一真相來源）本 story 完全未觸碰。

## UX Acceptance
卡片整張可點（`<Link>` 仍包住整張卡）；`:focus-visible` 有明確焦點環
（`outline: 2px solid var(--primary)`）；tile 標題用 `title-medium`
typescale；三態不只靠顏色區分（各自有不同文案，見上方視覺驗收）。

## 測試修正記錄
測試撰寫後未回頭修改斷言配合實作。撰寫 `quick-entry-cards.test.tsx` 新
測試時第一版對 `toHaveAccessibleName` 的預期值寫錯（誤以為只有 label，
實際上既有結構本來就會把說明文字一併併入 accessible name）——這是**測試
本身的技術性錯誤**（對既有行為的假設錯誤，不是實作要放寬配合），已對照
實際渲染結果修正預期字串，過程記錄於此，未觸碰任何斷言的「寬鬆度」。

## SELF-REVIEW checklist 結果
- [x] 每條可自動驗證的 AC 都有對應實作 + 對應測試——見「AC 對照」。
- [x] 無 scope 外變更——`git diff --cached --name-only` 核對，全部落在
  允許清單內（`page.tsx`、`quick-entry-cards.tsx`、
  `recent-conversations.tsx`、`lib/format-time.ts`、`globals.css`
  home 區段、`docs/design/home-m3.md`）；未觸碰 `nav-items.ts`、
  `contracts/`、其他 lib 檔案。
- [x] 無發明 contract——本 story 不涉及 contract。
- [x] Security AC:見上方。
- [x] UX AC:見上方。
- [x] 無 debug 殘留:暫時測試頁與腳本用完即刪、從未進 git；`git status`
  多次確認乾淨。
- [x] 無「mock 通過就當作整合證據」的宣稱——單元測試驗證的是「M3
  結構/accessible name/formatRelativeTime 邏輯」，AC5 的視覺聲稱明確
  另外用真實 Chromium 截圖 + 真實 axe-core 驗證，兩者未混為一談；視覺
  驗證頁用的 `setApiFetchForTests` 是專案既有的測試逃生艙（非本 story
  發明），且明確標記只用於「暫時視覺驗證頁」，非正式程式碼路徑。

## Assumptions / 計畫外事項

1. **E01-S021/E01-S022 開工時是 `in-progress`（非 `approved`）**：依總
   指揮訊息「HARD 依賴看能力是否在 main 上可用」的原則開工——兩者的
   M3 token/`<Icon>`/自託管字型已在 main 上可用，只是 AC4（264+ E2E
   全量驗證）待 E01-S031 merge 後統一補證，與本 story 的實作內容無關。
2. **三處接入元件各一行 icon 對照，選在元件內部而非 `nav-items.ts`**：
   `nav-items.ts` 在本 story 禁止修改清單內（"lib(除新增
   format-time)"），href→icon 對照表因此定義在 `quick-entry-cards.tsx`
   內部，純新增、不影響 `nav-items.ts` 本身。
3. **`docs/design/home-m3/*.png` 視為 `docs/design/home-m3.md`（允許
   清單內新增文件）的必要資產子目錄**：比照 E01-S026/E03-S042 的既有
   先例（AC 明文要求截圖，markdown 需要可解析的圖片檔案）。
4. **視覺驗證頁借用 `setApiFetchForTests`**：這是 E03-S036 為單元測試
   新增的既有逃生艙（`apps/web/src/lib/api.ts`），本 story 借用於一個
   從未進 repo 的暫時視覺驗證頁，僅用於擬造 loaded/empty/error/loading
   四態供截圖，不是正式程式碼路徑的一部分，不影響生產行為。
5. **E2E 鎖排隊時間較長**：跑 `flock`-guarded `playwright test` 時鎖先後
   被 W1、W7 持有（各自在跑自己 story 的完整 E2E 套件），等了數輪才排到；
   最終 14/14 全綠、零修改、零回歸。

## Rollback
`git revert` 本 story 對應的 commit(s)於
`story/E01-S024-home-m3-tiles` 分支。純樣式/UI 邏輯變更 + 一個新
lib 檔案，無 DB/contract 變更，revert 風險低（唯一需注意:若
E03-S039 之後已在 `recent-conversations.tsx` 疊加 SSE hook,revert
需重新確認兩者沒有交錯衝突）。

## 獨立審核（`/story-review`）結果

以「沒有參與實作的獨立審查者」身分重跑一次：

1. **範圍核對**：`git diff main...HEAD --name-only` 逐一核對，全部落在
   允許清單內（`page.tsx`、`quick-entry-cards.tsx`、
   `recent-conversations.tsx`、`lib/format-time.ts`（新增）、
   `globals.css`、`docs/design/home-m3.md` + 資產子目錄、
   `docs/stories/PROGRESS.md`），未觸碰 `nav-items.ts`、`contracts/`、
   Team B 資料夾。
2. **重跑全部 gate**（不信任紙面紀錄，親自重跑）：`pnpm --filter web
   typecheck`/`lint` 皆綠；`pnpm --filter web test` 獨立重跑一次，
   123 檔 1726/1726 全綠；`flock`-guarded E2E（`home-dashboard`/
   `smoke`/`app-shell`）14/14 全綠。
3. **既有測試逐字核對**：`git diff main...HEAD -- '*.test.tsx'` 逐行
   核對三個既有測試檔，確認每個既有 `it(...)` 區塊一個字元都沒被觸碰；
   唯一的非新增行是 `recent-conversations.test.tsx` 頂部 import 敘述
   多加 `afterEach`（新測試需要，非行為變更）。
4. **造假跡象搜尋**：無 skip、無 `passWithNoTests`、無 `|| true`、無
   放寬斷言；`toHaveAccessibleName` 的精確字串比對（不是寬鬆
   regex-only）證實圖示真的沒有污染既有的 accessible name 計算。
5. **AC 逐條核對**：見上方「AC 對照」，每條都有可重現的證據（測試名稱
   或截圖檔案），無空殼測試。
6. **視覺/無障礙複核**：`docs/design/home-m3/` 8 張截圖逐一肉眼核對，
   loaded/empty/error/loading 四態在 light/dark 下皆清楚可辨；axe
   0 violations 的三態逐一確認並非只挑了最簡單的一態測。

**結論：APPROVE**——邏輯、測試品質、視覺與無障礙驗收皆通過，無
BLOCKER/MAJOR/MINOR。與 E12-S030/E03-S041/E12-S031 不同，本 story 不
依賴任何真實硬體、真人資料或其他 story 才能完成的外部條件，AC 全數已由
本 story 自身驗證完畢，故狀態標記 approved（比照 E01-S026/E03-S042
先例）。
