# E03-S030 — No-evidence/abstention UX

- 狀態:DONE
- Branch:story/E03-S030-no-evidence-abstention-ux
- 日期:2026-08-14

## 獨立審核 BLOCKER 與修正(先讀這段)

第一輪獨立審核發現一個 BLOCKER:本檔最初版本的 Scope 說明(見下方)
宣稱「SOURCE_BASELINE.md 對本 story(line 1247)只有標題,無任何
內文」——**這個宣稱是錯的**。SOURCE_BASELINE.md line 1249-1251
實際上有具體內容:

```
E03-S30 No Evidence UX

顯示：

«找不到足夠企業資料支持此答案。»
```

`«»` 是這份文件保留給「需要逐字引用」內容的標記,整份文件只在
少數幾處使用(平台自己的正式名稱/定義、幾條 pinned MVP 哲學引言、
以及這一行)——這是刻意的強訊號,代表這段文字應該被接近逐字採用,
不是拿來改寫的素材。

**根本原因**:我先前針對 S30 的關鍵字搜尋(`abstain`/`NO_EVIDENCE`/
`查無依據`)沒有涵蓋「找不到足夠企業資料支持此答案」這個實際出現
的字串,導致這行內容被完全漏掉——即使我確實有對 SOURCE_BASELINE
做關鍵字搜尋,搜尋策略本身有缺口,不是沒有查證。獨立審核者直接
`Read` 了 SOURCE_BASELINE 該行範圍抓到這個落差,這正是獨立審核
存在的意義。

**修正**:比對後確認既有的
`ANSWER_STATE_FALLBACK_CONTENT.NO_EVIDENCE`(E03-S021 建立,S21
當初也沒有找到這一行)文字與 SOURCE_BASELINE 的引言意思相近但
文字不同。已修正為直接採用 SOURCE_BASELINE 的引言文字,前面加上
這個檔案既有的「(模擬回覆)」誠實標籤慣例(與其他 3 個 fallback
狀態一致):

- 修正前:「（模擬回覆）在您有權限的知識範圍內，找不到足夠的依據
  可以回答這個問題。」
- 修正後:「（模擬回覆）找不到足夠企業資料支持此答案。」

已同步更新所有斷言舊文字的地方(`answer-state.spec.ts`——S21
自己的 E2E,因為它斷言同一個常數;`no-evidence-abstention.spec.ts`
——本 story 自己新增的 E2E)。`message-thread.test.tsx` 的測試透過
常數引用(非字面字串),已自動套用新值,不需要另外修改。完整重跑
過的 gate 紀錄見下方(含第 3 輪 force 全量複驗)。

這個修正本身是可追溯的實作變更(直接對應 SOURCE_BASELINE 明確引用
的文字),不是「發明產品行為」——與下方 Scope 說明原本判定「不新增
可見 UI」的核心論點(不發明沒人要求的內容)完全一致:這次修正做的
恰好是把已經被要求、但先前漏掉的內容補上。

## Scope 說明(含完整 /advisor 分析過程,第一輪審核前撰寫)

SOURCE_BASELINE.md 對本 story(line 1247)只有標題「E03-S30 No
Evidence UX」,無任何內文。Epic 檔的展開標題「No-evidence/abstention
UX」同樣沒有進一步內文。

> **上方「獨立審核 BLOCKER 與修正」已經記錄:這段話是錯的。**
> SOURCE_BASELINE line 1249-1251 其實有具體的 `顯示：«...»` 內容。
> 保留這段原始分析文字(而非直接刪除改寫)是為了誠實呈現決策
> 過程,包含犯錯的部分——下方「選項分析」與「Step 4 決策權限」的
> 推理本身(不發明未經授權的 UI 內容)在修正後依然成立,只是
> Step 2 查權威的執行本身有缺口。

**問題**:這個能力字面上看起來已經被 E03-S021「Answer State
Rendering」完整實作過了——S21 的 6 個狀態
(ANSWERED/PARTIAL/NO_EVIDENCE/ERROR/PERMISSION_DENIED/
SOURCE_UNAVAILABLE)已包含 `NO_EVIDENCE` 的完整分類機制
(`classifyAnswerState`)、固定 fallback 文字、badge 呈現(非
alert,與 ERROR/PERMISSION_DENIED 區分)、mock trigger
(`[模擬:NO_EVIDENCE]`)、以及專屬 E2E 測試(surviving reload)。
`lib/answer-state.ts` 自己的文件註解甚至已經引用了與 S30 相同的
SOURCE_BASELINE §5 pinned decision #11「無足夠資料時 AI 必須
Abstain」。

本 story 因此啟動了 `/advisor` 流程,完整走過演算法:

**Step 1 問題定義**:(a) 要決定 S30 的實質增量範圍是什麼;(b) 卡在
S30 字面描述的能力看起來已被 S21 完整涵蓋,SOURCE_BASELINE/epic 檔
都沒有給出區分兩者的具體內容;(c) 做錯代價低——純前端 mock、單一
story、UI/測試增量,不涉及安全/契約/跨組介面,可回退。

**Step 2 查權威**:依序查詢:
1. `AI_KM_BMAD_High_Granularity/policies/`——**找到決定性依據**:
   `ATOMIC_STORY_BOUNDARIES.md` 的「Scope Freeze」段落明確、無條件
   禁止「發現缺漏 → Developer 自己腦補需求 → 擴大 scope → 一起
   commit」——這是本判斷真正的決定性依據,直接排除「發明一段新的
   UI 建議文字(例如『請嘗試換個方式提問』)或連結到一個還不存在
   的頁面」這類做法。「AI Agent Rule」的「不知道產品行為 →
   BLOCKED/ASSUMPTION」也相關,但這條本身其實留了「ASSUMPTION」
   這個自主採納的路徑,不是無條件禁止(獨立審核 MINOR 指出先前
   版本把這條引用得太絕對,已在此修正措辭)——真正讓「發明下一步
   引導文字」出局的,是 Scope Freeze 那條禁止腦補需求的規則,不是
   AI Agent Rule 本身。SOURCE_BASELINE 從未要求任何具體的「下一步
   引導」內容,發明這種內容正是 Scope Freeze 要禁止的行為。
2. SOURCE_BASELINE.md §5——pinned decision #11 是唯一直接相關項,
   已經是 S21 自己的依據來源,沒有給 S30 新的資訊。額外搜尋整個
   SOURCE_BASELINE 與 epic 檔的「abstain」關鍵字:唯一另一個相關
   結果是 E04-S28(Team B)「Evaluation Dataset」格式裡的
   `must_abstain` 欄位——那是 RAG 評測資料集格式,與 Team A 前端
   UX 無關。Epic 檔每個 story 章節都重複出現「Evidence 不足時依
   policy abstain,不得用模型常識假裝企業事實」——這是每個 story
   共用的樣板 policy footer,不是 S30 專屬指引。
3. Epic 檔 S30 開發邊界與 AC——標準樣板文字,無 S30 專屬內容。
4. `docs/adr/`——目前只有 2 篇既有 ADR(BFF 位置、前端測試
   runtime),與本問題無關。
5. `readme_zh.md`——確認 SOURCE_BASELINE 把工作拆成「高細粒度
   Atomic Stories」,S21 與 S30 被列為兩個獨立 story 本身就是這種
   細粒度拆分的產物,不必然代表兩者在功能上完全不重疊。

**Step 3 選項分析**:
1. **新增「下一步引導」UI 文字/連結**——與 ATOMIC_STORY_BOUNDARIES.md
   的 Scope Freeze 段落直接衝突(自行發明產品行為),直接淘汰。
2. **強化既有機制的正確性保證(不新增可見 UI,新增測試)——採用**:
   符合「Change Budget」(每個修改可追溯回 AC/test obligation)與
   Scope Freeze(不腦補需求);風險低(純新增測試,零回歸風險);成本小,
   在 0.5–2 developer-day 範圍內;對後續 story 無負面影響,反而
   替未來真的接上 E04 真實 RAG 後端時留下明確的 regression guard;
   無 A/B 耦合。
3. **判定 BLOCKED,寫入 PENDING_DECISIONS 交給使用者**——依 Step 4
   判準,這是單一 story 範圍內、低風險、可回退的純實作細節,不符合
   「必須問使用者」的四個條件(contract 變更、安全/授權行為、跨
   Team A/B 介面、多 epic 架構選擇、難以回退),BLOCKED 會是不必要
   的中斷,淘汰。

**推薦解**:選項 2。

**Step 4 決策權限判定**:單一 story(E03)範圍內、低風險(只新增
測試,不改變任何既有行為)、可回退——屬於「可自行採納推薦解」
類別,不需要 AskUserQuestion。

**具體增量內容**:搜尋既有測試套件後確認,S21 的既有測試從未把
「非 ANSWERED 狀態」與**兩個特定、真實存在的既有互動**放在一起
測試過:
- **S13「Citation Badge」(比 S21 更早合併)**:fallback 內容
  (含 NO_EVIDENCE)從未被明確斷言「不會意外殘留引用 badge」——
  雖然這在結構上必然成立(fallback 文字本身沒有 `[N]` 字面字元),
  但從未被明確測試過。
- **S27「Copy Answer」(比 S21 更晚合併)**:複製按鈕對「非
  ANSWERED 狀態的回覆」從未被測試過會複製到什麼內容——理論上
  `content` 局部變數已經正確解析到 fallback 文字,但這是 S21
  合併時 S27 還不存在、S27 合併時也沒有回頭補上的一個真實
  未涵蓋交集。

這兩點是真實、具體、可追溯到既有 AC 的驗證缺口,填補它們不是發明
新產品行為,而是確認既有已核准機制的正確性——完全符合 Change
Budget 的可追溯性要求。

## 變更檔案

- `apps/web/src/app/(app)/conversations/[id]/_components/message-thread.test.tsx`
  ——新增獨立 describe block「MessageThread no-evidence/abstention
  UX (E03-S030)」共 5 個測試:`it.each` 涵蓋 4 個有 fallback 內容的
  狀態(NO_EVIDENCE/ERROR/PERMISSION_DENIED/SOURCE_UNAVAILABLE),
  逐一確認皆不殘留引用 badge(`role="superscript"` 不存在);另 1
  個測試確認複製 NO_EVIDENCE 回覆會複製到誠實的 fallback 文字,
  非真實但實際上不存在的答案內容。這個 describe block 自己重新
  建立 `navigator.clipboard` stub(同 S027 已建立的
  `Object.defineProperty` + `afterEach` 還原慣例,不與 S027 自己
  的 describe block 共用,維持每個區塊獨立)。
- `tests/e2e/specs/no-evidence-abstention.spec.ts`(新增)——2 個
  關鍵流程測試:NO_EVIDENCE 回覆不會渲染任何引用 badge;複製
  NO_EVIDENCE 回覆會複製到誠實的 fallback 文字(讀回真實 clipboard
  內容驗證,不只斷言按鈕文字)。
- `docs/stories/PROGRESS.md` ——E03-S030 todo→in-progress→done。

**獨立審核 BLOCKER 修正後新增的變更**(見本檔最上方「獨立審核
BLOCKER 與修正」):
- `apps/web/src/lib/answer-state.ts` ——`ANSWER_STATE_FALLBACK_
  CONTENT.NO_EVIDENCE` 的文字改為直接採用 SOURCE_BASELINE line
  1251 的引言;文件註解新增說明這個修正的段落。這是本 story
  **唯一**修改的既有實作程式碼,且直接可追溯到 SOURCE_BASELINE
  明確引用的文字,不是自行發明。
- `tests/e2e/specs/answer-state.spec.ts`(E03-S021 既有檔案)——
  更新一處斷言舊 fallback 文字的地方,改為斷言新文字。這是本
  story 對 S21 既有測試檔案的唯一修改,純粹是同步文字內容,不改變
  測試意圖或涵蓋範圍。
- `tests/e2e/specs/no-evidence-abstention.spec.ts` ——同步更新
  `NO_EVIDENCE_FALLBACK` 常數與檔案頂端文件註解。
- `message-thread.test.tsx` 的 S030 describe block 透過
  `ANSWER_STATE_FALLBACK_CONTENT.NO_EVIDENCE` 常數引用(非字面
  字串),文字修正後自動套用新值,不需要另外修改測試斷言本身,
  只更新了 describe block 開頭的文件註解說明。

## Contract / Migration 差異

None——`ANSWER_STATE_FALLBACK_CONTENT.NO_EVIDENCE` 是純顯示文字內容
的修正(對齊 SOURCE_BASELINE 明確引用的文字),不是函式簽名、元件
props 或資料結構變更,不涉及 `contracts/openapi/` 變更。

## Gate 紀錄

| Gate | 指令 | Exit code | 關鍵測試 |
|---|---|---|---|
| typecheck | `pnpm typecheck` | 0 | 20/20 tasks |
| lint | `pnpm lint` | 0 | 20/20 tasks |
| build | `pnpm build` | 0 | 12/12 tasks |
| unit | `pnpm --filter @ai-km/web test -- run` | 0 | 442/442 |
| E2E(隔離,新 spec) | `playwright test no-evidence-abstention.spec.ts` | 0 | 2/2 |
| E2E(隔離,相關迴歸) | `playwright test answer-state citation-badge citation-preview copy-answer regenerate-answer answer-revision streaming-response generation-status stop-generation multi-turn-conversation send-message file-processing-status` | 0 | 28/28(含新 spec 共 30/30) |
| 全量(force,第 1 輪,BLOCKER 修正前) | `pnpm turbo run build` 後 `pnpm turbo run test --force`(自 repo root) | 0 | 13/13 tasks,442 unit + 103 E2E |
| 全量(force,第 2 輪,BLOCKER 修正前) | `pnpm turbo run test --force`(自 repo root) | 0 | 13/13 tasks,442 unit + 103 E2E |
| typecheck/lint/build(BLOCKER 修正後複驗) | `pnpm typecheck && pnpm lint && pnpm build` | 0 | 20/20、20/20、12/12 |
| unit(BLOCKER 修正後複驗) | `pnpm --filter @ai-km/web test -- run` | 0 | 442/442(常數引用的測試自動套用新文字仍全過) |
| E2E(隔離,相關迴歸,BLOCKER 修正後複驗) | 同上清單,新增 `answer-state.spec.ts` 的更新斷言 | 0 | 30/30 |
| 全量(force,第 3 輪,BLOCKER 修正後複驗) | `pnpm turbo run build` 後 `pnpm turbo run test --force`(自 repo root) | 0 | 13/13 tasks,442 unit + 103 E2E |
| 全量(force,第 4 輪) | `pnpm turbo run test --force`(自 repo root) | 0 | 13/13 tasks,442 unit + 103 E2E |

L2 contract test:不適用(無對應 contract,同既有判斷)。
L3 integration:不適用(純前端 mock,同既有判斷)。
L4 security-negative:不適用——本 story 未新增任何受保護資源或權限
邊界,PERMISSION_DENIED 狀態本身的安全相關測試已由 S21 涵蓋。

**FIX 循環:0 次(正式 gate 序列本身)**——依序執行的正式 gate 序列
在 DEV 階段每一輪皆一次全綠通過,gate 從未紅過。獨立審核發現 1 個
BLOCKER(SOURCE_BASELINE 內容誤判,見本檔最上方)後,修正了
`ANSWER_STATE_FALLBACK_CONTENT.NO_EVIDENCE` 的文字並同步更新受影響
的斷言,完整重跑過 typecheck→lint→build→unit→E2E 隔離→E2E 全量
兩輪,全部一次全綠通過——這屬於 REVIEW→修正→複驗流程,不是 Phase
4 FIX 循環(gate 本身從未失敗,是審核發現規格理解錯誤)。

## AC 對照

由於本 story 的實質內容是「驗證已核准機制的正確性」而非「新增新
能力」,以下對照表大量引用 S21 既有實作作為 AC 的實作位置,並誠實
標註哪些部分是本 story 新增的驗證。

| AC | 實作位置 | 測試 |
|---|---|---|
| Functional 1(合法輸入→預期成功結果) | E03-S021 既有的 `classifyAnswerState`/fallback 呈現機制;`ANSWER_STATE_FALLBACK_CONTENT.NO_EVIDENCE` 的文字內容經獨立審核 BLOCKER 修正後,現在直接對齊 SOURCE_BASELINE line 1251 明確引用的顯示文字 | S21 既有測試套件;本 story 新增的 5 個測試進一步確認其與 S13/S27 的正確互動;`answer-state.spec.ts`/`no-evidence-abstention.spec.ts` 已更新為斷言修正後的文字 |
| Functional 2(缺少必要輸入→fail-closed) | 不適用——沒有新的輸入概念,同 S21 既有判斷 | — |
| Functional 3(無權限使用者不得讀取受保護內容) | E03-S021 既有的 PERMISSION_DENIED 狀態(未改動) | S21 既有測試套件涵蓋 |
| Functional 4(dependency 失敗→可分類錯誤) | E03-S021 既有機制(未改動) | S21 既有測試套件涵蓋 |
| Functional 5(重複請求/重試不得造成未定義重複 side effect) | E03-S021 既有的「regenerate 重用原始 state,不重新分類」機制(未改動,已由 S21 自己的「regenerating reuses the original message's own state instead of reclassifying」測試涵蓋) | 同既有判斷,本 story 未重複 |
| Functional 6(correlation id / structured telemetry) | E03-S021 既有機制(未改動) | S21 既有測試套件涵蓋 |
| Functional 7(敏感操作 audit event) | 不適用——這層 mock 沒有真正的 audit 系統,同既有判斷 | — |
| Functional 8(MVP 可簡化視覺/演算法但功能本身不可缺席) | **本 story 的核心貢獻**:確認「abstention」這個能力與 S13 引用渲染、S27 複製功能的真實互動正確無誤,填補兩個先前從未交集測試過的真實缺口 | `message-thread.test.tsx` 新增 5 個測試 + E2E 新增 2 個測試 |
| Security(Authorization 先於 retrieval、Deny-Wins) | 不適用——無新受保護資源 | 同既有判斷 |
| Data/Contract(新增欄位向後相容) | 未修改任何既有函式簽名 | typecheck 全綠 |
| UX(不得只靠顏色傳達狀態) | 確認 NO_EVIDENCE 的 fallback 文字誠實(標註「模擬回覆」,非假裝真實答案),複製功能誠實反映這一點 | 本 story 新增的複製測試 |

## SELF-REVIEW checklist 結果

- ✅ 每條 AC 都有對應實作 + 對應測試(見上表;大量項目誠實標註
  「不適用/同既有判斷」並說明理由,而非為了填表格而虛構關聯)。
- ✅ 無 scope 外變更——新增測試檔案內容,加上獨立審核 BLOCKER
  修正後對 `answer-state.ts` 的一行文字修正(直接對應
  SOURCE_BASELINE line 1251 明確引用的內容,可追溯,非腦補);
  `message-thread.tsx`、`message-content.tsx` 等其餘既有檔案的
  實作程式碼完全未觸碰(已用 `git diff` 確認,以及完整迴歸 E2E
  30/30 全綠驗證零回歸)。
- ✅ 無發明 contract 未定義的 endpoint/欄位/權限——未涉及任何
  contract。
- ✅ **未違反 ATOMIC_STORY_BOUNDARIES.md 的 Scope Freeze**——這是
  本 story 自我審查的核心項目:確認沒有新增任何未經授權的 UI
  文字、建議、連結;唯一的實作變更(NO_EVIDENCE fallback 文字)
  直接對應 SOURCE_BASELINE 明確引用的內容,不是自行發明;其餘
  新增內容都是測試,可追溯回既有已核准的 S13/S21/S27 行為。
- ✅ Security AC:不適用,已在 AC 對照表說明理由。
- ✅ UX AC:確認 fallback 內容誠實、複製行為誠實反映 abstention
  狀態,非僅靠顏色傳達。
- ✅ 無 debug 殘留——`git diff` 對
  `console.`/`TODO`/`FIXME`/`debugger`/`.skip(`/`.only(`/
  `passWithNoTests` 逐一搜尋皆為 0 筆。
- ✅ 無「mock 通過就當作整合完成」的宣稱——EVIDENCE 誠實記錄這是
  純前端 mock 分類機制的正確性驗證,不是真正的 RAG/abstention
  後端整合。

## Assumptions / 計畫外事項

1. **本 story 判定除了修正 SOURCE_BASELINE 明確引用的 fallback
   文字之外,不新增任何其他新的可見 UI**——完整的 `/advisor` 分析
   過程記錄於本檔最上方的「Scope 說明」段落,依據
   `ATOMIC_STORY_BOUNDARIES.md` 的 Scope Freeze(禁止自行發明產品
   行為)做出此判斷。第一輪獨立審核發現原始分析誤判
   SOURCE_BASELINE 無內文這件事本身(見本檔最上方「獨立審核
   BLOCKER 與修正」),已經修正;「不發明未經授權內容」這個核心
   判斷邏輯本身在修正後依然成立,將提交第二輪獨立審核複驗。
2. **沒有新增「下一步建議」文字或連結**——SOURCE_BASELINE 與 epic
   檔皆未要求任何具體內容;E05 Knowledge Management Experience
   目前是 0 approved / 31 todo,任何連到知識庫頁面的內容都會是
   死連結,故意不發明。
3. **新增的測試聚焦在 S13(引用)與 S27(複製)這兩個與 S21 時間
   上錯開、從未交集測試過的既有功能**——這是本 story 判定的真正
   增量範圍,而非隨機挑選的測試補強。
4. **沒有涵蓋 PARTIAL 狀態的引用測試**——`ANSWER_STATE_FALLBACK_
   CONTENT` 沒有 PARTIAL 的項目(PARTIAL 保留正常串流的 MOCK_REPLY
   內容,含真正的引用標記),`it.each` 的過濾條件
   (`ANSWER_STATE_FALLBACK_CONTENT[state] !== undefined`)已正確
   排除它,同 S21 自己既有的「PARTIAL 不取代內容」設計判斷。
5. **regenerate 一則已經是固定 fallback 狀態的回覆,會在 revision
   history 留下一筆與目前內容相同的「先前版本」,沒有特別處理**
   ——獨立審核第一輪 MINOR 指出:因為 `handleRegenerate` 重用
   `originalMessage.state`(S21 既有設計),對同一個固定狀態
   regenerate 必然重新產生逐字相同的 fallback 文字,`reviseMessage`
   仍然會把「舊內容」存進 `revisions` 陣列,即使新舊內容一模一樣。
   審核者自己也判斷這「不明顯是 bug」(誠實反映「重試過、結果
   相同」這件事本身沒有錯),且明確定位成「未來若有相關 story 再
   處理的判斷,不是本 story 的義務」。刻意不在本 story 修改這個
   行為或補測試——這正是本 story 全篇論述的核心紀律(不在
   SOURCE_BASELINE 沒有要求的地方擴大 scope)在這裡的延伸應用:
   若要處理「內容相同的 regenerate 不該產生一筆多餘的 revision」
   這個行為變更,那本身會是一個需要自己走 AC/測試的獨立決定,不是
   本 story 職責範圍內的「驗證既有正確性」。

## Rollback

Revert 本 story 的 merge commit 即可,是自包含的變更集:新增
`message-thread.test.tsx` 的一個 describe block、新增 E2E spec 檔、
`answer-state.ts` 一行 fallback 文字修正(連同 `answer-state.spec.ts`
的對應斷言更新)。文字修正 revert 後會回到 S21 當初(誤解
SOURCE_BASELINE 內容前)的既有文字,不會破壞任何其他既有行為——
`ANSWER_STATE_FALLBACK_CONTENT` 的其他 3 個狀態、`classifyAnswerState`
的分類邏輯、`message-thread.tsx` 的渲染邏輯皆完全未觸碰。
