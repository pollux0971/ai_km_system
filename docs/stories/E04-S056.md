# E04-S056 — `/v1/health` 在 dev/test 的 ASR 耦合 + `reuseExistingServer` env 靜默失效

- 狀態:DONE
- Branch:story/E04-S056-health-degraded-dev
- 日期:2026-08-29

## 規格前提更正(先於本 story 其餘內容發生,誠實記錄)

原始規格聲稱「自 `ca7ef86` 起每條 lane 的每一次全量 E2E 都固定紅
`api-sandbox.spec.ts:143`」。實作前先在未修正的樹上實跑
`pnpm exec playwright test api-sandbox --project=web`,結果 **4/4 全綠**：

```
✓  4 [web] › specs/api-sandbox.spec.ts:143:5 › apps/web's /api/v1/health rewrite reaches the real apps/api instance (527ms)
✓  3 [web] › specs/api-sandbox.spec.ts:121:5 › a fake microphone is available: ... (2.7s)
✓  1 [web] › specs/api-sandbox.spec.ts:95:5 › a second page in the same context ... (7.0s)
✓  2 [web] › specs/api-sandbox.spec.ts:66:5 › two browser contexts each logging in ... (7.1s)
4 passed (33.7s)
```

原因:`tests/e2e/playwright.config.ts`（修正前）第 163 行早在 E03-S038
(`304c83f`) 就已對 api webServer 設 `AI_KM_ASR_PROVIDER: "fake"`，且該
webServer `reuseExistingServer: false`、獨佔 :4100，所以 Playwright 管理的
路徑從一開始就是 fake ASR。真正會 `degraded` 的只有 §5-penta 手動啟動的
共用 :4000 實例（未設 `AI_KM_ASR_PROVIDER`），且它目前不會讓任何 lane 的
E2E 變紅（E2E 走 :4100，且舊 preflight 只看 HTTP 200，`degraded` 一樣回
200）。此發現已回報總指揮，總指揮確認並改寫規格（`c0f22e8`），story 範圍
因此從「修一個紅測試」變成「(1) 讓 :4000 preflight/啟動指令名副其實 (2)
把路由測試的耦合解掉 (3) 修一個規格重寫過程中意外發現、影響更大的真實
hazard：`reuseExistingServer` 會靜默丟棄整組 webServer env」。

## 變更檔案
- `ROADMAP_TEMP.md` — §5-penta：preflight 改檢查 body `status`（不只 HTTP
  200）；補上共用 :4000 重啟指令要帶 `AI_KM_ASR_PROVIDER=fake` 的說明。
- `tests/e2e/specs/api-sandbox.spec.ts` — `:143` 的斷言從 `status:"ok"`
  改成斷言路由真正要驗的東西（3-key shape + `status` 是合法 enum 值）。
- `tests/e2e/helpers/env-sentinel.ts`（新增）— reused-server env 比對邏輯。
- `tests/e2e/helpers/write-env-sentinel.mjs`（新增）— 隨每次真正啟動
  web/admin webServer 寫入它實際拿到的 env。
- `tests/e2e/helpers/webserver-env.ts`（新增）— web/admin 預期 env 的
  唯一真相來源，`playwright.config.ts`（設定值）與 `global-setup.ts`
  （驗證值）共用同一份常數，避免兩邊字面值各自维护、悄悄漂移。
- `tests/e2e/playwright.config.ts` — web/admin 兩個 webServer 的
  `command` 改用 `wrapCommandWithSentinel` 包裝；`env` 改引用
  `WEB_EXPECTED_ENV`/`ADMIN_EXPECTED_ENV`；`API_PORT`/`API_BASE_URL`
  改從 `helpers/webserver-env.ts` 匯入（不再自己重複定義字面值）。
- `tests/e2e/global-setup.ts` — 新增 `assertReusedServerEnvMatches` 呼叫，
  跑在既有暖機之前。
- `tests/e2e/specs/env-sentinel.spec.ts`（新增）— AC5.1 的 TDD 測試，
  沿用 `port-check.spec.ts` 已建立的「spec 檔案其實是純邏輯單元測試」樣式。
- `docs/stories/E04-S056.md`、`docs/stories/PROGRESS.md`

**未修改**（規格明文禁止，已遵守）：`apps/api/src/health/checks.ts` 的
`overallStatus` 語意；`apps/api/src/config.ts` 的 production 預設值
（本 story 完全沒有修改 `apps/api` 任何原始碼 —— 根因在 dev/test 的環境
設定與 E2E 路由測試的耦合，不在應用程式邏輯本身）。

## Contract / Migration 差異
None

## 反向驗證（AC1 + AC3 共用一組實測，未捏造紅燈）

在隔離的、未修正的樹上，起一個全新 apps/api 實例（無 whisper sidecar，
模擬 §5-penta 共用實例目前的真實設定）：

```
$ AI_KM_API_PORT=4200 AI_KM_DB_PATH=<tmp>.sqlite AI_KM_LOG_LEVEL=silent node_modules/.bin/tsx src/main.ts &
$ curl -s http://127.0.0.1:4200/v1/health
{"status":"degraded","version":"0.1.0","uptimeMs":571}
```

同一個實例，加上 `AI_KM_ASR_PROVIDER=fake`（AC1 建議的修法）：

```
$ AI_KM_API_PORT=4200 AI_KM_DB_PATH=<tmp>.sqlite AI_KM_ASR_PROVIDER=fake AI_KM_LOG_LEVEL=silent node_modules/.bin/tsx src/main.ts &
$ curl -s http://127.0.0.1:4200/v1/health
{"status":"ok","version":"0.1.0","uptimeMs":1088}
```

這證明了機制本身有效（AC3），同時也是 AC1「修正後對沒有 sidecar 的新啟動
API 直接 curl /v1/health」的證據。未對 `api-sandbox.spec.ts` 本身捏造一個
假紅燈 —— 它在 Playwright 管理的路徑上本來就是綠的（見上方前提更正）。

## Gate 紀錄
| Gate | 指令 | Exit code | 關鍵測試 |
|---|---|---|---|
| typecheck | `pnpm --filter @ai-km/e2e... typecheck` | 0 | 11 個受影響 workspace 全過 |
| unit (apps/api，AC4 引用測試所在套件，證明未被動到) | `pnpm --filter @ai-km/api test` | 0 | 157/157，見下 AC4 |
| E2E (api-sandbox + env-sentinel + port-check) | `pnpm exec playwright test api-sandbox env-sentinel port-check` | 0 | **14/14 全過**(見下方完整輸出),含 web/admin webServer 真的用 `wrapCommandWithSentinel` 啟動、`global-setup.ts` 的 `assertReusedServerEnvMatches` 真的跑過且靜默通過(fresh start,非 reuse 路徑)——不是只有 scratch node 驗證,是真實 Playwright pipeline 端到端跑過 |

### E2E 完整輸出（真實 `.e2e.lock` 下執行，非 scratch）
```
=== stray listener check ===
(none)
=== uptime at start ===
 07:57:32 up 1 day,  5:01,  1 user,  load average: 6.02, 8.67, 13.38
Running 14 tests using 4 workers

  ✓   3 [web] › specs/api-sandbox.spec.ts:161:5 › apps/web's /api/v1/health rewrite reaches the real apps/api instance (366ms)
  ✓   5 [web] › specs/env-sentinel.spec.ts:35:7 › assertReusedServerEnvMatches › throws when no sentinel exists for the port (unknown env, must not silently reuse) (13ms)
  ✓   6 [web] › specs/env-sentinel.spec.ts:42:7 › assertReusedServerEnvMatches › throws naming the mismatched variable when the sentinel disagrees with what this run expects (52ms)
  ✓   7 [web] › specs/env-sentinel.spec.ts:54:7 › assertReusedServerEnvMatches › does not throw when the sentinel matches exactly (37ms)
  ✓   8 [web] › specs/env-sentinel.spec.ts:84:7 › wrapCommandWithSentinel › the wrapped command writes a sentinel that reflects the env it actually ran with, before running the real command (98ms)
  ✓   9 [web] › specs/port-check.spec.ts:38:7 › resolveReuseExistingServer › AC1: CI unset -> true (unchanged local-dev behaviour) (10ms)
  ✓  10 [web] › specs/port-check.spec.ts:48:7 › resolveReuseExistingServer › CI=true -> false (6ms)
  ✓  11 [web] › specs/port-check.spec.ts:61:7 › findOccupiedPorts / assertPortsFreeForCI › AC2: an occupied port is detected and identified (151ms)
  ✓  12 [web] › specs/port-check.spec.ts:74:7 › findOccupiedPorts / assertPortsFreeForCI › AC2: assertPortsFreeForCI throws with the port and process info when CI=true and the port is occupied (128ms)
  ✓  13 [web] › specs/port-check.spec.ts:89:7 › findOccupiedPorts / assertPortsFreeForCI › AC3: assertPortsFreeForCI does not throw when CI=true and the port is free (144ms)
  ✓  14 [web] › specs/port-check.spec.ts:103:7 › findOccupiedPorts / assertPortsFreeForCI › assertPortsFreeForCI is a no-op outside CI even when the port is occupied (3ms)
  ✓   4 [web] › specs/api-sandbox.spec.ts:121:5 › a fake microphone is available: getUserMedia({audio:true}) resolves with a live track, no permission prompt (2.0s)
  ✓   1 [web] › specs/api-sandbox.spec.ts:95:5 › a second page in the same context (same session, no second login) sees what the first page created (5.3s)
  ✓   2 [web] › specs/api-sandbox.spec.ts:66:5 › two browser contexts each logging in as demo-user get independent sandboxes — one context's new conversation is invisible to the other (5.5s)

  14 passed (22.2s)
EXIT=0
=== uptime at end ===
 07:57:55 up 1 day,  5:02,  1 user,  load average: 5.90, 8.45, 13.21
```
web/admin 兩個 webServer 都是全新啟動（起跑前 `stray listener check` 為
`(none)`），也就是 `wrapCommandWithSentinel` 真的跑過、`global-setup.ts`
的 `assertReusedServerEnvMatches` 真的執行且靜默通過（fresh-start 路徑，
sentinel 剛寫入即比對，理當相符）——不只是 scratch node 邏輯驗證。

## 測試修正記錄(若有)
None —（b）改的是本 story 自己在 IMPLEMENT 前就規劃要改的
`api-sandbox.spec.ts:143` 斷言，屬於 AC2 明文要求的範圍內變更，不是
Phase 4 第 5 點的「修正既有測試的技術性錯誤」窄例外；其餘既有測試
（`server.test.ts`、`config.test.ts`、`checks.test.ts`）逐字未動。

## AC 對照
| AC | 實作位置 | 測試 |
|---|---|---|
| AC1（§5-penta preflight + 啟動指令） | `ROADMAP_TEMP.md` §5-penta | 手動 curl 反向驗證（見上） |
| AC2（`api-sandbox.spec.ts:143` 解耦） | `tests/e2e/specs/api-sandbox.spec.ts` | 該測試本身，4/4 綠（見 Gate 紀錄）；ok/degraded 兩方向由 AC4 引用測試涵蓋 |
| AC3（反向驗證機制） | 同 AC1 | 上方 curl 輸出 |
| AC4（fail-loud 未弱化） | `apps/api/src/health/checks.ts`（未改） | `apps/api/src/server.test.ts` › `GET /v1/health (AC1)` › `AC1/AC3: reports status degraded when a subsystem (asr, unreachable whisper-server default) is down`（157/157 綠，見 Gate 紀錄） |
| AC5.1（reuseExistingServer env 靜默失效） | `tests/e2e/helpers/env-sentinel.ts`、`write-env-sentinel.mjs`、`webserver-env.ts`，`playwright.config.ts`/`global-setup.ts` 接線 | `tests/e2e/specs/env-sentinel.spec.ts`（4 案例，4/4 綠）+ scratch node 執行驗證（見下）+ 真實 Playwright 全流程跑過（global-setup 的比對呼叫真的執行，見 Gate 紀錄） |
| AC5.2（實測假說：PAGE_SIZE drift 是否重現 waitForURL 逾時） | — | W6（ai-km-01）獨立執行，負向結論（失敗簽章不同），見下方「AC5.2」 |

### AC5.1 的 scratch 驗證（`.e2e.lock` 釋出前，先用 `node --experimental-strip-types` 直接跑純函式邏輯）
```
OK missing: [E04-S056] port 59099 is already listening but has no env sentinel — ...
OK mismatch:   X: expected "right", reused server has "wrong"
OK match: no throw
OK wrapCommandWithSentinel: no throw
```
四種情境（sentinel 缺失／不符／相符／端到端 wrapped command 真的會寫檔）
全部符合預期。`tests/e2e/specs/env-sentinel.spec.ts` 用相同斷言，正式
Playwright 執行結果待 `.e2e.lock` 釋出後補上。

**W4（ai-km-f9）發現的真實 bug（誠實記錄）**：`env-sentinel.ts` 最初用
`fileURLToPath(import.meta.url)` 解析 `write-env-sentinel.mjs` 的路徑。
`tests/e2e/package.json` 沒有 `"type": "module"`，Playwright 的
config/test loader 因此把這個檔案當 CommonJS 載入，`import.meta` 一載入
就丟 `SyntaxError: Cannot use 'import.meta' outside a module`——連
`playwright test --list`（不碰任何 port）都會炸。W4 在用自己的
E04-S057 對著本 branch 做 `--list` demo 時發現並回報。這是純執行期的
模組格式問題，`tsc --noEmit` 完全測不出來（型別檢查不執行程式碼）。
修法:比照 `port-check.ts`/`fake-microphone.ts` 的既有模式,改用
`__dirname`(CJS 原生可用,不需要 `import.meta`)。修正後
`pnpm exec playwright test --list`(單獨 `env-sentinel` 與全量
323 tests/102 files)皆乾淨載入,無 load error。

## AC5.2：交由 W6（ai-km-01）獨立執行 —— 已完成

總指揮指示：AC5.2 的價值在於執行者事先不知道答案（W6 自己在 E01-S025 的
12 個 `waitForURL` 逾時上，已經自己發現「重跑證據無法區分『系統性延遲』
與『重跑時 server 本來就是全新啟動』兩個假說」，是最適合執行這個實驗的人）。
已將 AC5.2 的機制（scratch config、env 需求、真實登入帳密）交接給 W6，
刻意未透露我自己 scratch 環境已經跑出的結果，避免污染他們的獨立判斷。

**W6 的設定**：scratch apps/api（:4200，throwaway sqlite，
`AI_KM_TEST_SANDBOX`/`AI_KM_DEV_TRIGGERS`/`AI_KM_SEED_DEMO_USERS=true`，
`AI_KM_ASR_PROVIDER=fake`），web 起在 :3200（而非規格字面寫的 :4000 ——
W6 判斷：不想動到其他 lane 依賴的共用長駐實例，而本實驗要驗的因果機制
「adopted server 缺 `NEXT_PUBLIC_*`」用 scratch port 一樣能忠實重現，
風險更低。此偏離已誠實記錄，判斷合理，予以採用）。

**RUN 1(實驗組 —— 只設 `API_INTERNAL_URL`，不設
`NEXT_PUBLIC_CONVERSATIONS_PAGE_SIZE`/`NEXT_PUBLIC_FEATURE_MOCK_TRIGGERS`)**：
7 個測試 6 個失敗。**RUN 2(對照組 —— 同一 scratch 環境，全新 api 實例，
三個變數都正確設定)**：7/7 全過（25s），證明 scratch 環境本身可靠，
且正是這兩個變數在起作用。

**結論：config drift 是真實、可重現的缺陷**——沒有 `PAGE_SIZE=2`，3 筆種子
對話全部擠在第 1 頁，「下一頁」按鈕根本不存在。**但失敗形狀跟 W6 原本
E01-S025 的 12 個失敗對不上**，這是本次實驗最重要的部分：
- RUN 1 的失敗：1 個 `toHaveCount(0)` 斷言失敗（第 1 頁顯示 3 筆而非 2 筆）
  + 5 個 `locator.click: Test timeout of 30000ms exceeded`（等
  「下一頁」按鈕，但按鈕根本不存在，連點擊目標都找不到）。
- W6 原本 E01-S025 的 12 個失敗是 `page.waitForURL: Test timeout of
  30000ms exceeded`——點擊本身成功、有正確 dispatch，只是點擊後的導覽
  沒在時限內完成。這在 W6 當時的 EVIDENCE 就已經明確區分過
  （waitForURL 逾時 ≠ click 逾時）。

兩種失敗簽章指向不同機制：config drift 讓按鈕不存在；W6 原本的失敗是
「按鈕存在、點了、只有導覽逾時」。**依此證據，config drift 不像是
W6 原本 12 個失敗的成因**——它確實重現了一個真實、獨立的缺陷（AC5.1
本來就要修，不受此結論影響），但很可能不是那次事故的解釋。負載/併發
仍是原本失敗更一致的解釋，雖然 W6 尚未跑正向實驗證實負載這一側。

**判定**：AC5.2 到此為止，不再追加正向實驗（W6 有提議但我判斷 AC5.2
「兩種結果都要落檔，價值在於得到答案不在於哪個答案」的要求已經滿足，
追加正向重現屬於另一個問題「W6 原本的失敗到底是不是負載」，超出本
story 的範圍，不在此追加)。原始輸出存於 W6 的 scratchpad，已由 W6
清理 scratch ports/DB。

## SELF-REVIEW checklist 結果
- ✅ 每條 AC 都有對應實作 + 對應測試?見上方「AC 對照」表,逐條有實作位置
  與測試,AC5.2 由 W6 獨立執行、結果已核實併入。
- ✅ 有無 scope 外變更?`git diff main...HEAD --stat` 只有 9 個檔案,全部
  落在規格允許修改清單內(`tests/e2e/`、`ROADMAP_TEMP.md`、
  `docs/stories/`);`tests/e2e/helpers/*` 的涵蓋範圍判斷見下方
  Assumptions,已誠實記錄從寬解釋的理由。**完全沒有修改 `apps/api` 任何
  原始碼**——比原規格猜測的修法更小範圍。
- ✅ 有無發明 contract 未定義的 endpoint/欄位/權限?沒有,`/v1/health` 的
  回應形狀完全依既有實作,沒有新增或臆測欄位。
- ✅ Security AC:未授權路徑真的被測過且被拒?`/v1/admin/health` 完全未
  修改,對應測試(`admin-health.test.ts`)未受影響,157/157 apps/api 測試
  含此檔案全過。
- ✅ UX AC:不適用(本 story 無前端 UI 變更)。
- ✅ 有無留下 debug 殘留?無 console.log、無註解掉的程式碼、無無 ticket
  的 TODO。scratch 驗證用的暫存檔案(sqlite、log)均已清理,未提交。
- ✅ 是否有任何「mock 通過就當作整合完成」的宣稱?沒有——AC1/AC3 的
  反向驗證用的是真實 `tsx src/main.ts` 啟動的 apps/api 實例(非 mock);
  AC5.1 的最終驗證是真實 Playwright pipeline(非僅 scratch node);AC4
  引用的是既有真實整合測試(`server.test.ts`,走真實 `buildServer()`)。

## Assumptions / 計畫外事項
- **規格前提被推翻並改寫**：見文件開頭。已回報總指揮，總指揮已改寫規格
  （`c0f22e8`）並留下 struck-through 的原始前提供追溯。
- **AC5.1 是規格改寫過程中新發現的 hazard，不在原始 story 大小估計
  （0.5 developer-day）內**——已如實反映在變更檔案清單與此處，未縮小
  AC5.1 的實作範圍去配合原估計。
- **開發邊界未明文列出 `tests/e2e/helpers/*`**：規格的允許修改清單寫
  「`tests/e2e/` 的設定/webServer env」，`helpers/env-sentinel.ts` 等檔案
  性質上就是 webServer env 機制本身的一部分（`port-check.ts` 這個既有
  helper 也在同一分類下），依此從寬解釋為涵蓋在內；未另外詢問總指揮，
  若判斷有誤請於 review 提出。
- **AC5.2 交接給另一位開發者（W6/ai-km-01）執行**：機制交接、結果核實
  併入 EVIDENCE 皆已完成（見上方「AC5.2」一節），非本 story 自己遺漏。
- **W4（ai-km-f9）發現並協助定位一個真實的 `import.meta`/CJS 執行期
  bug**（見上方 AC5.1 scratch 驗證段落）——他們在對著本 branch 做
  E04-S057 的 `--list` demo 時發現,已修正並在完整 E2E 重跑中證實修好。

## Rollback
revert 本 branch 的 commit(s)。`ROADMAP_TEMP.md`／`api-sandbox.spec.ts`
的變更皆為 dev/test-only（不影響 production 預設、不影響
`overallStatus` 語意），revert 風險低。
