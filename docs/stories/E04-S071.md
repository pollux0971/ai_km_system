# E04-S071 — 兩份契約寫得比實作寬,收緊為實作永遠成立的保證

- 狀態:**BLOCKED**(本 story 授權範圍內的契約收緊已完成、通過全部自身 gate;
  BLOCKED 是因為誠實跑出 `pnpm turbo run test` 後,發現兩個**既有、跨此
  story 邊界**的測試 fixture 因本次收緊而變紅——兩者都不在本 story 的允許
  修改清單內,修正需要使用者對邊界例外的裁示。詳見「BLOCKED 原因」一節。)
- Branch:`story/E04-S071-contract-tightening`
- Worktree:`/data/python/AI_KM-worktrees/E04-S071`
- 日期:2026-09-02

## 變更檔案

| 檔案 | 說明 |
|---|---|
| `contracts/openapi/generation.yaml` | `GenerationResponse.model` 加入 `required`(唯一異動)|
| `contracts/openapi/analytics.yaml` | `FeedbackItem.reason` 加上 `enum: [INCORRECT, INCOMPLETE, OFF_TOPIC, OTHER]`(唯一異動,`type: string` 保留)|
| `contracts/openapi/__checks__/generated/generation.d.ts` | 重新產生 |
| `contracts/openapi/__checks__/generated/analytics.d.ts` | 重新產生 |
| `packages/api-client/src/generated/analytics.d.ts` | 重新產生(`generation.yaml` 不在 `@ai-km/api-client` 的 `SPEC_NAMES` 內,故該套件的 `src/generated/` 本story不產生 `generation.d.ts`——見下方「api-client 範圍說明」)|
| `contracts/openapi/__checks__/generation-compat.ts` | AC4:`modelAssignable` 改雙向、更新過時註解 |
| `contracts/openapi/__checks__/analytics-compat.ts` | **超出原始允許清單的必要延伸**——見下方專節說明與證明 |

## Anti-hallucination Guard 執行結果

- `FeedbackReason`(`services/conversation/src/repository/messages.repository.ts:21`):
  `export type FeedbackReason = "INCORRECT" | "INCOMPLETE" | "OFF_TOPIC" | "OTHER";`
  —— 與 spec.md 記載的四個值逐字相符,**無差異**。
- `GenerateResult.model`(`services/model-gateway/src/generation/provider.ts:41-45`):
  ```ts
  export interface GenerateResult {
    readonly answer: string;
    readonly citations: readonly Citation[];
    readonly model: string;
  }
  ```
  `model` 確實是必填(`string`,無 `?`)——spec 的假設成立,未觸發「停止並回報」的
  anti-hallucination 條款。

## 兩份 yaml 的 `git diff`(僅含兩個欄位,逐字)

```diff
diff --git a/contracts/openapi/analytics.yaml b/contracts/openapi/analytics.yaml
index b823141..5987bca 100644
--- a/contracts/openapi/analytics.yaml
+++ b/contracts/openapi/analytics.yaml
@@ -415,6 +415,7 @@ components:
           $ref: "#/components/schemas/FeedbackVerdict"
         reason:
           type: string
+          enum: [INCORRECT, INCOMPLETE, OFF_TOPIC, OTHER]
         comment:
           type: string
         citationFeedback:
diff --git a/contracts/openapi/generation.yaml b/contracts/openapi/generation.yaml
index ca5dc8d..d32fa51 100644
--- a/contracts/openapi/generation.yaml
+++ b/contracts/openapi/generation.yaml
@@ -125,7 +125,7 @@ components:
 
     GenerationResponse:
       type: object
-      required: [answer, citations]
+      required: [answer, citations, model]
       additionalProperties: false
       properties:
         answer:
```

AC-D1（僅限這兩個欄位）與 AC-D2（不新增/刪除任何 schema、path、operation、
securityScheme）皆滿足——上面是兩個檔案完整的 diff,沒有被截斷。

## 重新產生後的 `git diff --stat`

```
 contracts/openapi/__checks__/generated/analytics.d.ts  | 7 ++++++-
 contracts/openapi/__checks__/generated/generation.d.ts | 2 +-
 packages/api-client/src/generated/analytics.d.ts       | 3 ++-
```

`generation.d.ts` 的實際異動:
```diff
-            model?: string;
+            model: string;
```

`__checks__/generated/analytics.d.ts` 的實際異動(`packages/api-client` 版本除了
不含下面第二段的 `Error.code` 描述外完全相同):
```diff
-            reason?: string;
+            /** @enum {string} */
+            reason?: "INCORRECT" | "INCOMPLETE" | "OFF_TOPIC" | "OTHER";
```
`__checks__/generated/analytics.d.ts` 額外多出的 3 行(`Error.code` 的
`@description`)**與本 story 無關**:`core.yaml` 早先已經有這段説明文字,但
`__checks__/generated/analytics.d.ts` 當時沒有跟著重新產生過而落後(`packages/
api-client` 那份已經是最新的,兩者原本就不同步)。這次重新產生是誠實地把它一併
補齊,不是本 story 動了 `core.yaml`(`git diff` 對 `core.yaml` 為零)。

`generation.yaml` 未列入 `@ai-km/api-client/scripts/generate.mjs` 的
`SPEC_NAMES`(`["core", "auth", "conversations", "transcriptions", "analytics"]`)
——這是既有事實,不是本 story 造成或本 story 該修的範圍:`packages/api-client/
src/generated/` 本來就沒有 `generation.d.ts`,本 story 也沒有新增一個。

## AC4:`generation-compat.ts` 更新

`modelAssignable` 從單向 `AssignableTo` 改為雙向 `Exact`;整段解釋「契約
optional / 實作 required」的舊狀態註解全部改寫為過去式並說明已由本 story
關閉該落差;檔案裡不再有任何描述舊狀態的現在式敘述(已逐行檢查確認)。完整
diff 見下方。

```diff
--- a/contracts/openapi/__checks__/generation-compat.ts
+++ b/contracts/openapi/__checks__/generation-compat.ts
@@ -10,8 +10,10 @@
  * `services/model-gateway` (`generation.yaml` §paths./generate), and its
  * types are `services/model-gateway/src/generation/provider.ts`'s
  * `Citation`/`GenerateResult`. `Citation` is structurally identical (both
- * checked below); `GenerateResult` is not — see `responseAssignable`'s own
- * comment for the one real divergence this repoint surfaced.
+ * checked below); `GenerateResult` is not, in a way this repoint surfaced —
+ * see `modelAssignable`'s own comment. E04-S071 closed that gap by
+ * tightening `generation.yaml` to match what the implementation always
+ * provides; `modelAssignable` is bidirectional as of that story.
  *
  * Evidence layer: **policy L0** (static — typecheck only). This file is never
  * executed and never bundled; it proves shape compatibility and nothing else.
@@ -59,25 +61,24 @@ export const citationExact: Exact<Schemas["Citation"], Citation> = true;
  * thing this seam actually needs proven without tripping over that
  * technicality.
  *
- * WHY FIELD BY FIELD (E04-S064 repoint finding #2, a REAL divergence): the
- * pre-repoint version checked `AssignableTo<Schemas["GenerationResponse"],
+ * WHY FIELD BY FIELD (E04-S064 repoint finding #2, RESOLVED by E04-S071):
+ * the pre-repoint version checked `AssignableTo<Schemas["GenerationResponse"],
  * GenerationResult>` against `@ai-km/rag-skeleton`'s `GenerationResult`,
  * which had no `model` field at all, so "a contract response is usable
- * everywhere a pipeline result is" held trivially in that direction. Model
- * Gateway's own `GenerateResult` requires `model: string`, while
- * `generation.yaml`'s `GenerationResponse.model` is OPTIONAL — a
- * schema-conformant body is PERMITTED to omit `model`, which would violate
- * `GenerateResult`'s required field. That is a genuine, pre-existing
- * looseness in the contract relative to what this seam's implementation
- * always provides (`model-gateway-routes.ts` forwards `gateway.generate()`'s
- * result verbatim, which always sets `model`) — not a bug introduced by this
- * repoint. Not loosening the contract and not loosening `GenerateResult`
- * here (both are outside this story's scope — 鐵律 #1, and `GenerateResult`
- * is Team B's type); `modelAssignable` below asserts the direction that IS
- * true and meaningful: whatever `GenerateResult` always provides for `model`
- * is a valid value for the contract's optional field. A domain-owner call on
- * tightening `generation.yaml`'s `model` to required is a follow-up, not
- * this file's job.
+ * everywhere a pipeline result is" held trivially in that direction. The
+ * E04-S064 repoint then surfaced a real divergence: Model Gateway's
+ * `GenerateResult` requires `model: string`, while `generation.yaml`'s
+ * `GenerationResponse.model` was OPTIONAL — a schema-conformant body was
+ * PERMITTED to omit `model`, which would have violated `GenerateResult`'s
+ * required field. E04-S071 closed that gap by tightening the CONTRACT
+ * (`generation.yaml`'s `GenerationResponse.model` is now `required`), not by
+ * loosening the implementation — `model-gateway-routes.ts` always forwards
+ * `gateway.generate()`'s result verbatim, which always sets `model`, so the
+ * contract now states a guarantee the implementation already upheld
+ * unconditionally. `modelAssignable` below therefore asserts BOTH
+ * directions: whatever `GenerateResult` always provides for `model` is a
+ * valid value for the contract's (now required) field, AND the contract no
+ * longer permits a value `GenerateResult` could not have produced.
  */
 export const answerAssignable: AssignableTo<
   GenerateResult["answer"],
@@ -87,10 +88,7 @@ export const citationsElementAssignable: AssignableTo<
   GenerateResult["citations"][number],
   Schemas["GenerationResponse"]["citations"][number]
 > = true;
-export const modelAssignable: AssignableTo<
-  GenerateResult["model"],
-  Schemas["GenerationResponse"]["model"]
-> = true;
+export const modelAssignable: Exact<GenerateResult["model"], Schemas["GenerationResponse"]["model"]> = true;
```

## `analytics-compat.ts` 的延伸——超出原始允許清單,附機械證明與理由

**本 story 交付說明只把 `generation-compat.ts` 列入允許修改清單,沒有列
`analytics-compat.ts`。實測後發現這是不夠的**,理由如下,附機械證明:

`analytics-compat.ts` 原本的 `reasonAssignable` 是**單向**
`AssignableTo<AdminFeedbackItem["reason"], Schemas["FeedbackItem"]["reason"]>`。
這個方向在 `Schemas["FeedbackItem"]["reason"]` 是 `string`(收緊前)或收緊後的
四值 enum 時**都成立**(`FeedbackReason` 的四個字面值本來就是 `string` 的子集,
收緊後又剛好完全相同)——也就是說,**這個既有斷言不管契約收不收緊都不會變色**,
不能拿來當 AC2 反向驗證要求的守門。

**機械證明**(把 `contracts/openapi/__checks__/generated/analytics.d.ts` 的
`reason` 手動改回 `reason?: string;`,不動 `analytics-compat.ts` 半個字):

```
$ ./node_modules/.bin/tsc -p contracts/openapi/__checks__/tsconfig.json --noEmit
(no output — 0 errors)
```

`analytics-compat.ts` 保持全綠——完全沒有任何斷言會因為 `reason` 被鬆綁回
`string` 而變紅。這代表 AC2 明文要求的反向驗證(「把 `analytics.yaml` 的
`reason` 改回 `type: string` → `analytics-compat.ts` 必須紅」)在原始允許清單
下**機械上不可能通過**。

**處理方式**:比照 AC4 對 `generation-compat.ts` **同一類**的既有作法——把
`reasonAssignable`(單向 `AssignableTo`)換成 `reasonExact`(雙向 `Exact`),
並改寫其上方那段「CONTRACT LOOSE / IMPLEMENTATION STRICT」註解為過去式,連帶
修正兩處引用 `reasonAssignable`「divergence」字樣的下游註解(`feedbackItemSample`
與 `implFeedbackItemSample` 附近)。**這不是新邏輯,是把 AC4 已經授權的同一種
「單向 AssignableTo → 雙向 Exact + 改寫過時註解」處理方式套用到孿生檔案的孿生
欄位**,而不是引入新的檢查種類或新的行為。

修改後重跑機械證明,確認守門真的接住了:

```
$ ./node_modules/.bin/tsc -p contracts/openapi/__checks__/tsconfig.json --noEmit --pretty
contracts/openapi/__checks__/analytics-compat.ts:96:14 - error TS2322: Type 'true' is not assignable to type 'never'.

96 export const reasonExact: Exact<Schemas["FeedbackItem"]["reason"], AdminFeedbackItem["reason"]> = true;
                ~~~~~~~~~~~

Found 1 error in contracts/openapi/__checks__/analytics-compat.ts:96
```

**請獨立審核者特別確認這一項決定**:這是本 story 執行中唯一一處，判斷後超出
交付說明字面允許清單的異動，己誠實在此標示，並非隱瞞後補。

完整 diff:
```diff
--- a/contracts/openapi/__checks__/analytics-compat.ts
+++ b/contracts/openapi/__checks__/analytics-compat.ts
@@ -46,10 +46,9 @@
 /*
  * ── FeedbackItem, field by field ─────────────────────────────────────────
  *
- * WHY FIELD BY FIELD, NOT ONE WHOLE-OBJECT CHECK (repoint finding, same
- * shape as `generation-compat.ts`'s own comment on this): a single
- * `AssignableTo<Schemas["FeedbackItem"], AdminFeedbackItem>` does not
- * compile, for a reason that is a genuine, pre-existing divergence, not a
- * bug introduced by this repoint — see `reasonAssignable` below.
+ * shape as `generation-compat.ts`'s own comment on this): a single
+ * `AssignableTo<Schemas["FeedbackItem"], AdminFeedbackItem>` does not
+ * compile, for a reason that was a genuine divergence at the time this file
+ * was repointed (`reason`'s contract/implementation mismatch — see
+ * `reasonExact` below), since resolved by E04-S071. Kept field by field
+ * regardless: `citationFeedback`'s readonly-array technicality
+ * (`citationVerdictAssignable` below) still holds independently.
  */
@@ -71,25 +70,20 @@ export const commentExact: Exact<Schemas["FeedbackItem"]["comment"], AdminFeed
 /**
- * CONTRACT LOOSE / IMPLEMENTATION STRICT (repoint finding — informational,
- * a domain-owner call, NOT an escalation): `analytics.yaml`'s
- * `FeedbackItem.reason` is an unconstrained `type: string` — apps/admin's
- * old hand-written mirror also typed it as plain `string`, which is why the
- * pre-repoint check here compiled without ever noticing this. The REAL
- * implementation (`AdminFeedbackItem.reason`, `services/conversation/src/
- * repository/admin-read.repository.ts`) always supplies one of exactly the
- * four `FeedbackReason` literals (`INCORRECT`/`INCOMPLETE`/`OFF_TOPIC`/
- * `OTHER`) — the SAME enum `conversations.yaml` already declares and
- * `conversations-compat.ts` checks. The implementation never violates the
- * contract (every `FeedbackReason` value is a valid `string`), so the
- * assignment below asserts the direction that is actually true. Whether
- * `analytics.yaml`'s `FeedbackItem.reason` should be tightened to
- * `$ref: "./conversations.yaml#/components/schemas/FeedbackReason"` is a
- * follow-up for the domain owner, not this file's job (鐵律 #1 — contracts
- * are frozen, this story does not edit them).
+ * CONTRACT LOOSE / IMPLEMENTATION STRICT — RESOLVED by E04-S071: ... (見上方全文)
  */
-export const reasonAssignable: AssignableTo<AdminFeedbackItem["reason"], Schemas["FeedbackItem"]["reason"]> =
-  true;
+export const reasonExact: Exact<Schemas["FeedbackItem"]["reason"], AdminFeedbackItem["reason"]> = true;
@@ (下游兩處引用 reasonAssignable 的過時註解，改為說明已解決 — 見完整檔案)
```
(完整逐字 diff 已在本檔上方「重新產生後」章節之外另附於 commit body;此處為
節省篇幅摘要主要區塊，實際落地內容以 `git show` 為準。)

## 反向驗證(兩段,獨立,`tools/mutate.mjs`)

**工具適用性說明**:`generation-compat.ts` / `analytics-compat.ts` 是
「typecheck-only,never executed」的檔案(見該目錄 README),沒有可以直接
`vitest run` 的宿主套件,`tools/mutate.mjs` 設計上要求 `--expect-fail` 指向一個
會被 vitest 執行、且有真實通過測試的檔案。為了讓 mutate.mjs 可以套用在這兩個
typecheck-only 的守門上，替每一個各建立一個**暫時性**的 vitest 包裝測試
(`tools/__verify_e04_s071_generation.test.ts` / `..._analytics.test.ts`),
內容是呼叫 `contract-gate` 實際使用的同一條 `tsc -p
contracts/openapi/__checks__/tsconfig.json --noEmit --pretty` 指令,把對應
`-compat.ts` 檔案裡的錯誤原文拋出。**驗證完成後這兩個檔案已刪除**,不是本
story 的交付物(`git status` 已核對 `tools/` 內零殘留)。

### 反向驗證 #1 — `generation.yaml` 的 `model`

```
node tools/mutate.mjs \
  --file contracts/openapi/__checks__/generated/generation.d.ts \
  --replace '        GenerationResponse: {
            answer: string;
            citations: components["schemas"]["Citation"][];
            model: string;
        };' \
  --with '        GenerationResponse: {
            answer: string;
            citations: components["schemas"]["Citation"][];
            model?: string;
        };' \
  --expect-fail tools/__verify_e04_s071_generation.test.ts \
  --expect-message "model"
```

結果:
```
outcome: exit 0 — 守門正確觸發——突變前綠、突變後真的因斷言失敗變紅
green test count (baseline): 1
sha256 before: 4f609d2f62e958370a899997875eb79ec45b8e555cd91dd9bdf707e46cece939
sha256 after restore: 4f609d2f62e958370a899997875eb79ec45b8e555cd91dd9bdf707e46cece939
```

**失敗訊息原文(逐字)**:
```
Error: generation-compat.ts failed the contract gate:
contracts/openapi/__checks__/generation-compat.ts:91:14 - error TS2322: Type 'true' is not assignable to type 'never'.

91 export const modelAssignable: Exact<GenerateResult["model"], Schemas["GenerationResponse"]["model"]> = true;
Found 1 error in contracts/openapi/__checks__/generation-compat.ts:91
```
明確指名 `model`(`modelAssignable`、`GenerateResult["model"]`、
`GenerationResponse["model"]`)。sha256 突變前後一致,還原確認過。

### 反向驗證 #2 — `analytics.yaml` 的 `reason`

```
node tools/mutate.mjs \
  --file contracts/openapi/__checks__/generated/analytics.d.ts \
  --replace '            /** @enum {string} */
            reason?: "INCORRECT" | "INCOMPLETE" | "OFF_TOPIC" | "OTHER";' \
  --with '            reason?: string;' \
  --expect-fail tools/__verify_e04_s071_analytics.test.ts \
  --expect-message "reason"
```

結果:
```
outcome: exit 0 — 守門正確觸發——突變前綠、突變後真的因斷言失敗變紅
green test count (baseline): 1
sha256 before: bdf8ce8eb53e8bf19612b7cc0d74a1d44bd6e00ba8500fc375ce32bc2323b37e
sha256 after restore: bdf8ce8eb53e8bf19612b7cc0d74a1d44bd6e00ba8500fc375ce32bc2323b37e
```

**失敗訊息原文(逐字)**:
```
Error: analytics-compat.ts failed the contract gate:
contracts/openapi/__checks__/analytics-compat.ts:96:14 - error TS2322: Type 'true' is not assignable to type 'never'.

96 export const reasonExact: Exact<Schemas["FeedbackItem"]["reason"], AdminFeedbackItem["reason"]> = true;
Found 1 error in contracts/openapi/__checks__/analytics-compat.ts:96
```
明確指名 `reason`(`reasonExact`、`Schemas["FeedbackItem"]["reason"]`、
`AdminFeedbackItem["reason"]`)。sha256 突變前後一致,還原確認過。

## Gate 紀錄

| Gate | 指令 | Exit code | 備註 |
|---|---|---|---|
| L0 typecheck | `pnpm typecheck` | **0** | 38/38 successful |
| L0 typecheck(強制無快取) | `pnpm turbo run typecheck --force --concurrency=1` | **0** | 38/38 successful,1m48s |
| L0 lint | `pnpm lint` | **0** | 36/36 successful |
| L1 unit(有快取,依交付說明字面指令) | `pnpm turbo run test --filter='!@ai-km/e2e'` | **1** | 見下方「turbo 快取失準」與「BLOCKED 原因」 |
| L1 unit(強制無快取,`--concurrency=1 --continue`,真實結果) | `pnpm turbo run test --filter='!@ai-km/e2e' --force --concurrency=1 --continue` | **1** | 37/39 successful；`@ai-km/admin#test`、`@ai-km/service-model-gateway#test` 紅,詳見下方 |
| L2 contract | `pnpm contract-gate` | **0** | 202 檔閉包、0 error、覆蓋率見下 |
| Demo | `pnpm demo:w1-00` | **0** | PASSED,與本 story 無關(embedding/ingestion 管線),未受影響 |

### turbo 快取失準(誠實記錄的額外發現,非本 story 造成也非本 story 該修)

`pnpm turbo run test --filter='!@ai-km/e2e'`(不加 `--force`)第一次執行時,
`@ai-km/service-model-gateway#test` 顯示 `cache hit, replaying logs`,重播的是
**另一個 worktree**(`/data/python/AI_KM-worktrees/E04-S064`)先前的綠色結果,
而非針對本 story 已修改過的 `contracts/openapi/__checks__/generated/
generation.d.ts` 重新執行。`turbo.json` 沒有設定 `globalDependencies`,
`contracts/` 目錄不在任何 package 自己的檔案樹內,turbo 的 hash 機制因此**看不到**
`contracts/` 的變動對這個測試任務的影響——這是 monorepo 快取設定的既有缺口,
不是本 story 引入,也不在本 story 允許修改的檔案清單內(`turbo.json` 未列入),
**不予修改**,僅誠實記錄以避免對契約類 story 的 `pnpm turbo run test` 結果過度
信任。本 EVIDENCE 的 L1 判定改以強制無快取(`--force --concurrency=1
--continue`)的重跑結果為準(見上表)。

## BLOCKED 原因:兩個既有測試因本次(正確的)收緊而變紅,皆落在本 story 允許
修改清單之外

兩者皆**機械確認過根因**,不是臆測:

### 1. `apps/admin/src/lib/feedback.test.ts:32`(Team A,但屬本 story 邊界
   明文禁止的 `apps/**`)

```
FAIL  src/lib/feedback.test.ts > listFeedback (E11-S016, E13-S021 real API) > AC1: returns the real feedback queue from the server
AssertionError: expected false to be true
```

根因(用與 `apps/admin/src/test/fake-api.ts` 完全相同的 ajv 設定手動重現,
逐字):`data/reason must be equal to one of the allowed values`。
`apps/admin/src/test/fake-api.ts` 是一個「contract-validated fake」,用 ajv
在回應送出前對照 `analytics.yaml` 的 schema 驗證; `feedback.test.ts:32` 的
fixture 用了 `reason: "答案不正確"`(中文自由文字,舊契約下合法),收緊後被
正確攔下。**這正是 fake 該做的事,只是暴露了一個用了非典型值的舊 fixture。**

修法是把該行 `reason: "答案不正確"` 換成四個典型值之一(例如
`"INCORRECT"`),純資料修正,零行為變動。但 `apps/admin` 屬本 story 明文
「Forbidden: apps/**」,未經授權未修改。

### 2. `services/model-gateway/src/testing/contract-check.test.ts:70-86`
   (Team B,`services/**`,CLAUDE.md 鐵律 #6「一律不動」,本 story 授權清單
   未涵蓋)

```
FAIL src/testing/contract-check.test.ts > expectResponseMatchesContract — generation.yaml (relocated rag-skeleton AC-C6) > AC-C6 (PF0) schema 驗證抓不到捏造的引用
AssertionError: expected [Function] to not throw an error but 'Error: 回應不符合契約 POST /generate → 200:\…' was thrown
+ Received:
"Error: 回應不符合契約 POST /generate → 200:
  - (root) must have required property 'model'"
```

根因:該測試的 `fabricatedBody` fixture(用來證明「schema 驗證抓不到捏造的
引用,這是 `assertCitationsGrounded` 的工作」)寫於 `model` 選填時代,漏了
`model` 欄位。收緊後這個**與測試本身想驗證的事無關**的欄位遺漏讓測試整條紅,
掩蓋了它原本要證明的論點。修法是在 `fabricatedBody` 補上 `model: "..."`,
同樣是純資料修正、零邏輯變動。但 `services/model-gateway` 屬
`services/**`,CLAUDE.md 鐵律 #6 明文「一律不動」,本 story 授權清單未列
E04-S071,未經授權未修改。

### 為什麼不是「順手修了就好」

兩處都是一行、機械、零風險的 fixture 修正,但都落在寫死的邊界之外
(`apps/**`、`services/**`),而邊界的存在正是為了不讓「反正很小」變成擴權
的理由。已完整診斷、記錄根因與修法,交由使用者決定下列其中一種解除方式:
(a) 授權本 story 或指定的後續 story，各補一行修正兩個檔案的 fixture 值；
(b) 兩者分別轉為 Team A/Team B 各自的獨立小 story 跟進。**在此之前,本 story
就其自身授權範圍內的交付物(兩份 yaml + 2 個 generated 目錄 + 2 個 compat
檔)是完整、正確、且全部自身 gate 綠燈的**;紅的是下游、範圍外的既有測試。

## 覆蓋率(binding coverage)前後比較

| | Before | After |
|---|---|---|
| 型別閉包檔案數 | 202 | 202 |
| BOUND-L0 | 18 | 18 |
| BOUND-L2 | 3 | 3 |
| TRANSCRIBED | 6 | 6 |
| BOUND-VIA-PARENT | 1 | 1 |
| UNBOUND | 52 | 52 |

**AC5 滿足**:收緊後的 `enum` 是內嵌在 `FeedbackItem.reason` 屬性上(比照
`FeedbackVerdict` 以外、本檔案其餘 `enum` 的既有寫法),**沒有**產生新的具名
schema,因此覆蓋率數字前後完全一致,沒有新增 UNBOUND 項目,也不需要更動
`unbound-schema-allowlist.mjs`。

## AC 對照

| AC | 說明 | 驗證 |
|---|---|---|
| AC1 | `GenerationResponse.model` 加入 `required` | yaml diff;`generation-compat.ts` 的 `modelAssignable`(`Exact`)綠;反向驗證 #1 |
| AC2 | `FeedbackItem.reason` 為 enum,且與實作 `FeedbackReason` 逐字相同 | Anti-hallucination Guard 一節的原始碼比對;`analytics-compat.ts` 的 `reasonExact`(`Exact`)綠;反向驗證 #2 |
| AC3 | 兩處 generated 型別重新產生後,全 repo `pnpm typecheck` 綠 | 上方 Gate 紀錄(38/38,含強制無快取版本)|
| AC4 | `modelAssignable` 雙向,無舊狀態註解殘留 | `generation-compat.ts` diff(逐行檢查過,無殘留現在式舊狀態敘述)|
| AC5 | `pnpm contract-gate` exit 0,無新增 UNBOUND | Gate 紀錄 + 覆蓋率表格 |
| AC-D1 | 兩份 yaml 僅改兩個欄位 | 上方逐字 diff |
| AC-D2 | 不新增/刪除 schema/path/operation/securityScheme | 同上 diff;覆蓋率數字不變佐證 |

## SELF-REVIEW checklist 結果

- [x] 每條 AC 都有對應實作 + 對應測試？—— 見上方「AC 對照」。
- [x] 有無 scope 外變更？—— `git diff --name-only HEAD` 只列 7 個檔案：
  2 個 yaml、2 個 generated、`generation-compat.ts`、`analytics-compat.ts`
  (超出字面清單,已在專節誠實揭露並附機械證明必要性)。**沒有**碰
  `apps/**`、`services/**`、其他 yaml、`AI_KM_BMAD_High_Granularity/`、
  `CLAUDE.md`、`README.md`、`.claude/rules/`。
- [x] 有無發明 contract 未定義的 endpoint/欄位/權限？—— 沒有；兩個欄位改動
  皆為收緊既有欄位,值取自實作原始碼而非本檔猜測。
- [ ] N/A — 本 story 無 Security AC。
- [ ] N/A — 本 story 無 UX AC(純契約/型別層)。
- [x] 有無留下 debug 殘留？—— 暫時性 `tools/__verify_e04_s071_*.test.ts` 已
  刪除,`git status --short` 核對過 `tools/` 目錄零殘留;無註解掉的程式碼、
  無無 ticket 的 TODO。
- [x] 是否有任何「mock 通過就當作整合完成」的宣稱？—— 沒有;本 story 全程
  用真實 `tsc` 編譯結果與真實原始碼比對,無 mock。

**連續兩輪都過不了同一項**:不適用(每項都在第一輪確認)。

## 測試修正記錄(Phase 4 第 5 點窄例外)

None——本 story 沒有修改任何既有測試的斷言或內容;新增的
`reasonExact`/`modelAssignable` 是**新斷言**,不是修改既有測試去配合實作;
兩個因收緊而變紅的既有測試(`apps/admin`、`services/model-gateway`)**未修改**
(見「BLOCKED 原因」)。

## Assumptions / 計畫外事項

1. **`analytics-compat.ts` 的延伸**(見專節)——超出原始允許清單,判斷理由與
   機械證明已完整記錄,請獨立審核者特別覆核這一項決定是否同意。
2. **兩個下游測試變紅**(見「BLOCKED 原因」)——已診斷根因、寫出修法,但未
   越界修正,交由使用者裁示。
3. **turbo 快取對 `contracts/` 變動不敏感**——誠實記錄的既有缺口,未修
   `turbo.json`(不在允許清單內)。

## Rollback

`git revert` 本 branch 的 commit(s),或直接捨棄整個 `story/
E04-S071-contract-tightening` 分支(未 merge 進 main)。regenerated 檔案已
committed,revert 會一併還原。
