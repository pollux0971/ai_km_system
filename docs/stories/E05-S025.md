# E05-S025 — Archive document action

- 狀態:DONE
- Branch:story/E05-S025-archive-document-action
- 日期:2026-08-15

## Scope 說明

E05 的第二十五個 story。與 S001-S023 相同情況:SOURCE_BASELINE.md 對
E05 只有一份扁平標題清單,本 story 在 epic 檔中只有標題,無進一步
內文(line 5052-5261)。已讀過 epic 檔本 story 自己的完整章節,並用
`diff` 直接逐字比對本 story 章節與下一節「Delete document
confirmation」(S026,line 5262-5471)的內文(排除 H1 標題與「User /
System Value」段落唯一的story 名稱代換),確認兩者逐字相同,是通用
MVP 樣板文字,無本 story 專屬內容。

**前一個 story(S024 "Document version history")的狀態**:已標記
`blocked-team-b`(需要 E06-S030,未解除)。本 story 與 S024 是各自
獨立、AC 互不相依的能力(封存/取消封存 vs. 版本歷史),epic 檔中兩者
順序相鄰純屬編號巧合,S024 卡住不影響本 story 動工。

**設計上直接鏡射既有先例**:`archiveConversation`/
`unarchiveConversation` + `ArchiveConversation` 元件 +
`ConversationList` 的 view-switch(皆為 E03-S026,已核准)是本
codebase 已經建立的「封存/取消封存 + 雙視圖切換」完整既有設計,形狀與
本 story 的需求幾乎完全相同。本 story 沒有從零設計一個新模式,而是
逐項比對後直接複用同一套已驗證過的互動邏輯,並在唯一結構性不同之處
(見設計決策 #2)明確記錄偏離理由。

## 設計決策

1. **`listKnowledgeBaseDocuments` 新增第二參數 `archived = false`,
   語意與 `listConversations`(E03-S026)的 `archived` 參數完全相同
   ——是兩個互斥視圖之間的「切換」,不是「連同封存項目一起顯示」的
   include-toggle**。`(document.archived ?? false) === archived` 的
   過濾邏輯直接鏡射 `listConversations` 自己的既有寫法。`archived`
   本身為選填欄位(`archived?: boolean`),沿用 `ConversationSummary.
   archived` 已建立的「缺席即為 `false`」語意——每一筆 S010-S023
   既有種子資料與既有測試 fixture 都不帶這個欄位,不需要任何機械式
   回填。
2. **`KnowledgeDocumentArchiveToggle` 刻意不像 `ArchiveConversation`
   一樣保留自己的本地 `archived` state**——`ArchiveConversation` 活在
   對話自己的詳情頁上,無論封存與否該頁都繼續掛載,因此需要自行原地
   翻轉標籤;本元件活在
   `knowledge-document-list.tsx` 的作用中/已封存視圖切換裡,一旦
   toggle 成功,父層 `refetchDocuments()` 重新抓「目前這個視圖」,
   剛剛被切換的文件不再符合該視圖的過濾條件——這個元件自己的 `<li>`
   會整個從清單消失,沒有東西需要原地保留翻轉後的狀態。`onToggled`
   直接複用這個頁面上每個會改變清單的元件(上傳/URL匯入/文字新增/
   重試)早已共用的同一顆「清單變了」callback。這是本 story 與既有
   先例唯一的結構性差異,原因是所處的頁面結構不同,非隨意偏離。
3. **`viewingArchived` 刻意不放進 `knowledge-document-list.tsx` 主
   `useEffect` 的依賴陣列**——與 `ConversationList` 把 `viewingArchived`
   直接放進抓取 effect 的依賴陣列(切視圖時整頁回到 loading)不同,
   本頁面的主 effect 同時擁有 `knowledgeBase` 本身與兩段式循序 fetch
   (先確認 KB 存在,才抓文件清單)、not-found/error 分流,若跟著切
   視圖重新整個重跑,會連 KB 標題、上傳/URL匯入/文字新增三個既有
   元件都一起閃回 loading,只為了換文件清單這一小塊。改為
   `handleViewChange` 直接呼叫 `refetchDocuments(archived)`——與
   這個頁面自 S011 起「upload/URL匯入/文字新增/重試」每個 mutation
   成功後都用的「原地靜默重新整理」完全同一套模式,只是這次多帶一個
   明確的 view 參數而非用預設值。`refetchDocuments` 的預設參數
   `= viewingArchived` 正是每個既有 mutation 的 `onXxx` callback能
   正確運作的原因——封存/取消封存後重新抓「目前視圖」,正是讓剛被
   切換的項目從畫面消失的機制本身。
4. **上傳/URL匯入/文字新增三個既有元件在檢視已封存文件時整個隱藏
   (而非停用)**——在已封存視圖下新增文件,新文件必然是非封存
   (預設值),要嘛必須偷偷把視圖切回作用中(使用者沒有要求的畫面
   跳動),要嘛新項目會憑空出現在使用者當下看的畫面裡卻不符合
   已封存過濾條件而立刻消失,兩者皆是令人困惑的行為;直接隱藏三個
   新增入口迴避了這個問題,同時也是「已封存視圖是唯讀瀏覽,新增動作
   只屬於作用中視圖」這個語意上更清楚的呈現。
5. **第三種空清單訊息「尚無已封存的文件。」**,與
   `ConversationList` 既有的第三種訊息(「尚無已封存的對話。」)同一
   理由——沿用既有「這個知識庫尚無文件。」在已封存視圖顯示會是
   誤導性的假話(可能作用中文件一堆,只是封存區剛好是空的)。
6. **視圖切換用 `role="group"` + `aria-pressed` 按鈕組,標籤「作用中
   文件」/「已封存文件」**——逐字鏡射 `ConversationList` 既有的
   「作用中對話」/「已封存對話」按鈕組結構(只把「對話」換成
   「文件」),包含 `aria-label="文件檢視"`(對應既有的
   `aria-label="對話檢視"`)。

## 變更檔案

- `apps/web/src/lib/knowledge-documents.ts`——`KnowledgeBaseDocument`
  新增選填欄位 `archived?: boolean`(見設計決策 #1);
  `listKnowledgeBaseDocuments` 新增第二參數 `archived = false`;新增
  `archiveKnowledgeBaseDocument`/`unarchiveKnowledgeBaseDocument`
  兩個獨立函式(NOT_FOUND 含跨知識庫防呆,與
  `retryDocumentProcessing`/`renameKnowledgeBaseDocument` 既有檢查
  同一套邏輯)。
- `apps/web/src/lib/knowledge-documents.test.ts`——
  `listKnowledgeBaseDocuments` 既有 describe block 內新增 1 個測試
  (作用中/已封存視圖過濾);新增獨立 describe block
  「archiveKnowledgeBaseDocument / unarchiveKnowledgeBaseDocument
  (E05-S025)」共 6 個測試:封存設為 true、取消封存設為 false、兩者
  對不存在文件皆 NOT_FOUND、兩者跨知識庫皆 NOT_FOUND、不影響文件
  其餘欄位、不影響同一知識庫的其他文件。
- `apps/web/src/app/(app)/knowledge/[id]/documents/_components/knowledge-document-archive-toggle.tsx`
  (新增)——單一按鈕,標籤依 `archived` prop 顯示「封存文件」/
  「取消封存」,無確認步驟(可逆操作,同
  `RenameConversation`/`KnowledgeDocumentNameEditor` 已建立的「低
  風險可逆操作不需 confirm dialog」理由),non-optimistic,見設計
  決策 #2 說明其刻意不保留本地 state 的理由。
- `apps/web/src/app/(app)/knowledge/[id]/documents/_components/knowledge-document-archive-toggle.test.tsx`
  (新增)——11 個測試:依 `archived` prop 顯示正確標籤;點擊時呼叫
  正確方向的函式(封存/取消封存互不誤呼叫)且成功後呼叫
  `onToggled`;兩個方向各自的失敗顯示各自獨立訊息且不呼叫
  `onToggled`;進行中鎖定按鈕防止雙擊;attempt/success telemetry
  共用 correlationId 且明確排除文件名稱本身;取消封存 telemetry
  使用 unarchive 事件名稱而非 archive;failure telemetry 包含錯誤
  代碼。
- `apps/web/src/app/(app)/knowledge/[id]/documents/_components/knowledge-document-list.tsx`
  ——新增 `viewingArchived` state 與 `handleViewChange`;
  `refetchDocuments` 新增 `archived = viewingArchived` 參數;render
  新增 `role="group"` 視圖切換按鈕組;上傳/URL匯入/文字新增三個
  既有元件包進 `!viewingArchived` 條件;空清單訊息新增已封存分支;
  每個文件項目新增 `<KnowledgeDocumentArchiveToggle>`(見設計決策
  #3、#4、#5、#6)。
- `apps/web/src/app/(app)/knowledge/[id]/documents/_components/knowledge-document-list.test.tsx`
  ——`@/lib/knowledge-documents` 既有 mock 新增
  `archiveKnowledgeBaseDocument`/`unarchiveKnowledgeBaseDocument`;
  既有「calls listKnowledgeBaseDocuments with the given id」測試
  更新為斷言同時帶入 `false`(既有呼叫簽名因新增參數而改變,純機械
  性更新,未放寬斷言本身要驗證的行為);新增獨立 describe block
  「archive document action (E05-S025)」共 8 個測試:視圖切換按鈕
  預設 `aria-pressed` 狀態;掛載與切視圖時皆帶正確的 `archived`
  參數;切到已封存視圖時隱藏三個新增元件;已封存視圖空清單顯示
  專屬訊息;作用中視圖每個文件皆有「封存文件」按鈕;封存後從作用中
  視圖消失且不影響其他文件;已封存視圖顯示「取消封存」(非「封存
  文件」)按鈕;取消封存後從已封存視圖消失。
- `tests/e2e/specs/knowledge-documents.spec.ts`——新增 1 個關鍵流程
  測試(E05-S025):封存一筆種子文件,確認從作用中視圖消失、其他
  文件不受影響、且知識庫詳情頁的「N 份文件」headline 數字同步減少
  (獨立確認這個 ripple 效果真的發生,而非只在文件清單頁面內部驗證
  ——`knowledge-detail.tsx` 自己的
  `listKnowledgeBaseDocuments(id)` 呼叫本身零程式碼變更,純粹透過
  新的預設參數受益);切到「已封存文件」視圖確認該文件出現、其他
  文件不出現;取消封存後確認從已封存視圖消失、回到作用中視圖後
  重新出現、知識庫詳情頁的份數也回到原本的 3。
- `docs/stories/PROGRESS.md`——E05-S025 todo→in-progress→done;
  in-progress 轉換時同批次更新總覽表計數,並用逐列手動計數比對
  複核。

未修改 `apps/web/src/lib/knowledge-documents.ts` 的
`addKnowledgeBaseDocument`/`addKnowledgeBaseDocumentFromUrl`/
`addKnowledgeBaseDocumentFromText`/`retryDocumentProcessing`/
`renameKnowledgeBaseDocument`(既有函式簽名與內文皆未變更)、
`upload-progress.ts`/`parse-progress.ts`/`index-progress.ts`、
`knowledge-document-upload.tsx`/`knowledge-document-url-import.tsx`/
`knowledge-document-text-input.tsx`/`knowledge-document-retry-button.tsx`/
`knowledge-document-preview.tsx`/`knowledge-document-name-editor.tsx`
(既有元件本體)、`knowledge-detail.tsx`(見上方,零程式碼變更、純
受益於新預設參數)、`archive-conversation.tsx`/`conversation-list.tsx`
(E03 domain,只作為設計參考,完全未觸碰)、`packages/permissions`、
`packages/auth-client`。

## Contract / Migration 差異

None——純前端 mock。`contracts/openapi/core.yaml` 未變更。
`archiveKnowledgeBaseDocument`/`unarchiveKnowledgeBaseDocument` 為
全新 export;`KnowledgeBaseDocument.archived` 為新增選填欄位,
backward-compatible(既有 fixture/呼叫端不需任何改動即可繼續運作)。

## Gate 紀錄

| Gate | 指令 | Exit code | 關鍵測試 |
|---|---|---|---|
| typecheck | `pnpm typecheck`(自 repo root) | 0 | 20/20 tasks,乾淨(兩次執行皆通過,第二次為強化 E2E 斷言後複驗) |
| lint | `pnpm lint`(自 repo root) | 0 | 20/20 tasks,乾淨(兩次執行皆通過) |
| unit | `pnpm test`(自 repo root,turbo 全 workspace) | 0 | `@ai-km/web`:825/825(800+25 新增:knowledge-documents 7+archive-toggle 11+document list 8,一次通過) |
| E2E | 同上(`pnpm test` 觸發 `@ai-km/e2e:test`) | 0 | 148/148(147+新增 1 個 E05-S025 測試,一次通過,含既有 S010/S023 測試零回歸) |
| build | `pnpm build`(自 repo root) | 0 | 12/12 tasks(`/knowledge/[id]/documents` 反映新增的視圖切換與封存元件) |

**FIX 循環:0 次**(全部 gate 第一輪即通過)。SELF-REVIEW 階段主動
把 E2E 測試從「只在文件清單頁內部驗證封存/取消封存」強化為「額外
往返知識庫詳情頁,直接證明 `archived` 這個新的第二參數真的讓
`knowledge-detail.tsx` 既有的份數 headline 同步變化」——這不是修
一個紅燈,而是在寫 EVIDENCE 前自我檢視時,發現原始測試只能證明
「往返視圖不留下淨變化」(一個完全沒接上 `archived` 參數的實作
一樣會通過原始版本),強化後重跑 typecheck + 完整 `pnpm test` 皆
再次一次全綠。

L2 contract test:不適用。L3 integration:不適用(純前端 mock)。
L4 security-negative:與 S011-S023 相同分析——本 story 未新增任何
新的受保護資源類別,既有 route-level guard 已涵蓋
`/knowledge/[id]/documents`。**本 story 特有的驗證**:跨知識庫防呆
測試(封存/取消封存各自的「returns NOT_FOUND ... belongs to a
different knowledge base」)直接證明無法跨庫封存/取消封存其他 KB 的
文件。未修改 `role-guard.tsx`/`nav-items.ts`/`packages/permissions`/
`packages/auth-client`。

## AC 對照

| AC | 實作位置 | 測試 |
|---|---|---|
| Functional 1(合法輸入→預期成功結果) | 封存/取消封存皆成功更新 `archived` 並呼叫 `onToggled`,清單即時反映 | `knowledge-document-archive-toggle.test.tsx`「clicking 封存文件/取消封存 calls .../onToggled on success」;E2E |
| Functional 2(缺少必要輸入→fail-closed) | 不適用——本能力是無輸入欄位的單一按鈕 toggle,無「缺少輸入」情境,同 `ArchiveConversation`(E03-S026)既有先例 | — |
| Functional 3(無權限使用者不得讀取受保護內容) | 不適用——與 S011-S023 相同分析;**額外驗證**:無法跨知識庫封存/取消封存其他 KB 的文件 | `knowledge-documents.test.ts`「returns NOT_FOUND when the document exists but belongs to a different knowledge base」(archive/unarchive 各一) |
| Functional 4(dependency 失敗→可分類錯誤) | 失敗顯示各自方向獨立的錯誤訊息,不誤標為成功 | 「shows a distinct error message...and does not call onToggled when archiving/unarchiving fails」 |
| Functional 5(重複請求/重試不得造成未定義重複 side effect) | 進行中鎖定按鈕,直接驗證雙擊防護 | 「disables the button while the toggle is in flight, preventing a double click」 |
| Functional 6(correlation id / structured telemetry) | `knowledge_base_document_archive_*`/`knowledge_base_document_unarchive_*` 皆帶 correlationId,兩個方向使用各自獨立的事件名稱 | 「emits attempt and success telemetry sharing the same correlation id...」;「emits unarchive telemetry (not archive) when toggling from archived」;「emits failure telemetry with the error code...」 |
| Functional 7(敏感操作 audit event) | 不適用(與 S011-S023 同理)——封存是可逆的視圖過濾變更,非存取控制變更 | — |
| Functional 8(MVP 可簡化但功能本身不可缺席) | 完整可用的封存/取消封存能力:雙向切換、真實(mock)持久化、清單與詳情頁份數皆即時反映、跨頁面導覽後仍持久化 | 全部新測試;E2E |
| Security(Authorization/Deny-Wins/stale cache) | 不適用——與 S011-S023 相同分析 | 同上 |
| Data/Contract(向後相容、UTC 時間) | `archived?: boolean` 為新增選填欄位,`archiveKnowledgeBaseDocument`/`unarchiveKnowledgeBaseDocument` 為全新 export,不改變任何既有型別或函式簽名 | typecheck 全綠;既有 800 個測試零修改(除 1 處機械性參數簽名更新)仍全數通過 |
| UX(loading/empty/success/validation/error 狀態皆有明確呈現;keyboard 可操作;不得只靠顏色) | 進行中/成功/失敗三種結果皆有明確處理;第三種「尚無已封存的文件」空清單訊息;按鈕標籤文字本身變化(非顏色)傳達狀態;原生 `<button>` 天生可鍵盤操作 | `knowledge-document-archive-toggle.test.tsx`/`knowledge-document-list.test.tsx` 全部測試;E2E |

## SELF-REVIEW checklist 結果

- ✅ 每條 AC 都有對應實作 + 對應測試(見上表;不適用項目皆有明確
  理由)。
- ✅ 無 scope 外變更——`git status --short` 確認變更檔案全部落在
  `apps/web/src/lib`、`apps/web/src/app/(app)/knowledge`、
  `tests/e2e/specs`(Team A 範圍)內;`git diff -- apps/web/src/lib/knowledge-documents.ts`
  逐行核對確認 `addKnowledgeBaseDocument`/
  `addKnowledgeBaseDocumentFromUrl`/`addKnowledgeBaseDocumentFromText`/
  `retryDocumentProcessing`/`renameKnowledgeBaseDocument` 完全未
  修改;`knowledge-detail.tsx` 零程式碼變更(用 grep 逐一核對其
  `listKnowledgeBaseDocuments(id)` 呼叫點,確認純粹透過新預設參數
  受益,見設計決策說明);`archive-conversation.tsx`/
  `conversation-list.tsx`(E03 domain)完全未觸碰,只作為設計參考;
  `pnpm test` 全量 825 unit + 148 E2E 皆綠已驗證。
- ✅ 無發明 contract 未定義的 endpoint/欄位/權限——
  `archiveKnowledgeBaseDocument`/`unarchiveKnowledgeBaseDocument` 是
  全新 Team A mock export,`archived` 是全新選填欄位,
  `contracts/openapi/core.yaml` 未變更。
- ✅ 未偷跑其他 story 的範圍——不做刪除(E05-S026 "Delete document
  confirmation" 是下一個 story,刪除是不可逆操作,與本 story 的
  可逆封存語意上完全不同);不做版本歷史(E05-S024,已
  blocked-team-b)。
- ✅ Security AC:延續 S011-S023 已完整記錄的分析,額外驗證跨知識庫
  防呆(封存/取消封存各自的 NOT_FOUND)。
- ✅ UX AC:三種送出結果、第三種空清單訊息皆有明確處理;按鈕標籤
  文字變化而非顏色傳達封存狀態。
- ✅ 無 debug 殘留——對本次變更的所有新增/修改檔案逐一搜尋
  `console.`/`TODO`/`FIXME`/`debugger`,0 筆命中
  (`git diff -- apps/web tests/e2e | grep -n "console\.\|TODO\|FIXME\|XXX\|debugger"`
  exit code 1,無輸出)。
- ✅ 無「mock 通過就當作整合完成」的宣稱——本 EVIDENCE 誠實記錄
  SELF-REVIEW 階段主動強化 E2E 測試的過程(見上方 Gate 紀錄),
  並清楚說明強化前的版本存在的邏輯漏洞(無法真正證明 ripple 效果),
  不誇大也不隱瞞。

## Assumptions / 計畫外事項

1. **S024(blocked-team-b)與本 story 無相依關係,可直接動工**——
   兩者是各自獨立的 AC,epic 編號相鄰純屬巧合。
2. **`KnowledgeDocumentArchiveToggle` 刻意不保留本地 `archived`
   state,與 `ArchiveConversation` 唯一的結構性差異**——見設計決策
   #2,原因是所處頁面結構(清單 item vs. 詳情頁)不同,非隨意偏離。
3. **`viewingArchived` 刻意不進主 `useEffect` 依賴陣列**——見設計
   決策 #3,避免切視圖時整頁(含 KB 標題與三個新增元件)不必要地
   閃回 loading。
4. **上傳/URL匯入/文字新增三個既有元件在已封存視圖下整個隱藏**——
   見設計決策 #4,避免新增文件與已封存過濾條件之間的視覺矛盾。
5. **SELF-REVIEW 階段強化 E2E 測試,加入知識庫詳情頁份數的往返
   驗證**——見上方 Gate 紀錄,不是修復紅燈,是自我檢視時發現原始
   測試邏輯上不夠嚴謹而主動加強。

## Rollback

Revert 本 story 的 merge commit 即可。變更集包含純新增檔案
(`knowledge-document-archive-toggle.tsx`/`.test.tsx`)、以及對既有
檔案的追加式修改(`knowledge-documents.ts` 新增一個選填欄位與兩個
函式;`listKnowledgeBaseDocuments` 新增的第二參數帶預設值,既有
零參數呼叫端行為不變;`knowledge-document-list.tsx` 新增 state 與
render 區塊)——revert 不影響任何既有 export 的簽名相容性,
`knowledge-detail.tsx`/`addKnowledgeBaseDocument` 等既有函式完全未
被觸碰,revert 不會對既有功能造成任何影響。
