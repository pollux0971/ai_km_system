# E06-S043 — 同一 documentId 以不同 scopeKey 重新匯入會靜默改寫可見性

- 狀態:DONE
- Branch:`story/E06-S043-reingest-scope-guard`
- 日期:2026-09-02

## 變更檔案

- `services/retrieval/src/vector/store.ts` — 新增 `DocumentScopeConflictError`、
  `DOCUMENT_SCOPE_CONFLICT_MESSAGE`、共用的 `groupRecordsByDocumentId()` /
  `checkDocumentScopeConsistency()`;`createInMemoryVectorStore().upsert()`
  改為兩階段(全部驗證 → 才寫入),同 scope 重匯改為刪除該 document 不在新集合
  中的舊 chunk 再寫入新的(原子替換)。
- `services/retrieval/src/vector/sqlite-vec.store.ts` — 新增 `deleteMeta`、
  `existingScopeKeysForDoc`、`existingChunkIdsForDoc` 三個 prepared statement;
  `upsert()` 同樣改為兩階段,Phase 2(刪除多餘 chunk + 寫入新 chunk)包在真正
  的 `BEGIN`/`COMMIT`/`ROLLBACK` 交易中。
- `services/retrieval/src/index.ts` — 匯出 `DocumentScopeConflictError`、
  `DOCUMENT_SCOPE_CONFLICT_MESSAGE`。
- `services/retrieval/src/vector/store.test.ts`(新檔)— in-memory store 的
  reingest scope guard 單元測試,9 條。
- `services/retrieval/tests/sqlite-vec-store.integration.test.ts` — 新增
  `describe("sqlite-vec store — E06-S043 …")`,5 條測試(含直接下 SQL 查
  `_vec`/`_meta` 表)。
- `services/ingestion/src/pipeline.test.ts` — 新增一條透過真實 `ingest()`
  入口的端到端測試,證明 guard 從 store 層一路傳到重匯入口。

## Contract / Migration 差異

None。`_meta`/`_vec` 表結構(欄位、型別)未變更——只是新增了一個對既有
`chunk_id` 欄位的 `DELETE` prepared statement,不是 schema 變更。
`contracts/openapi/**` 未觸碰,`pnpm contract-gate` 綠燈確認。

## Gate 紀錄

| Gate | 指令 | Exit code | 關鍵測試 |
|---|---|---|---|
| install | `pnpm install` | 0 | — |
| typecheck (ingestion) | `pnpm --filter @ai-km/service-ingestion typecheck` | 0 | — |
| lint (ingestion) | `pnpm --filter @ai-km/service-ingestion lint` | 0 | — |
| test (ingestion) | `pnpm --filter @ai-km/service-ingestion test` | 0 | 35/35(舊 34 + 新 1),見下方 AC 對照 |
| typecheck (retrieval) | `pnpm --filter @ai-km/service-retrieval typecheck` | 0 | — |
| lint (retrieval) | `pnpm --filter @ai-km/service-retrieval lint` | 0 | — |
| test (retrieval) | `pnpm --filter @ai-km/service-retrieval test` | 0 | 41/41(舊 27 + 新 14),見下方 AC 對照 |
| turbo typecheck | `pnpm turbo run typecheck` | 0 | 39/39 tasks successful |
| turbo test | `pnpm turbo run test --filter='!@ai-km/e2e'` | 0 | 40/40 tasks successful(全 monorepo,含 rag-skeleton/design-tokens/api-client 等既有套件) |
| contract gate | `pnpm contract-gate` | 0 | `contract gate: 293 files in the type closure, 0 error(s) total` / `PASS: no error in any *-compat.ts.` |
| demo | `pnpm demo:w1-00` | 0 | `W1-00 DEMO: PASSED`(含 scope-filter 檢查步驟,未受本 story 影響) |

未跑 Playwright E2E(依指示禁止);未 register 任一 plugin 進 `apps/api`
(`grep -rn "ingestionPlugin\|retrievalPlugin" apps/api/` 仍是 0 命中,曝險維持零)。

## 測試修正記錄(若有)

None——本 story 全部是新增測試,未修改任一既有測試的斷言或邏輯。既有
27(retrieval)+ 34(ingestion)條測試全部原封不動且全綠。

## 反向驗證(spec 規定,不可省略)

### 反向驗證 #1 — 把「不同 scope 重匯即拒絕」改成永遠放行

做法:暫時讓 `checkDocumentScopeConsistency()` 一進函式就 `return`(略過所有
檢查)。

**紅(`pnpm --filter @ai-km/service-retrieval test`)**:

```
× createInMemoryVectorStore — E06-S043 re-ingest scope guard > AC1 ★ ...
× createInMemoryVectorStore — E06-S043 re-ingest scope guard > AC2 承上 ...
× createInMemoryVectorStore — E06-S043 re-ingest scope guard > AC6 ...
× createInMemoryVectorStore — E06-S043 re-ingest scope guard > 同一次 upsert 呼叫本身對同一 documentId 帶兩個不同 scopeKey ...
× sqlite-vec store — E06-S043 re-ingest scope guard (PF2) > AC1+AC2 ★ ...
× sqlite-vec store — E06-S043 re-ingest scope guard (PF2) > AC6 ...
× sqlite-vec store — E06-S043 re-ingest scope guard (PF2) > AC5 ★ ...
Test Files  2 failed | 3 passed (5)
     Tests  7 failed | 34 passed (41)
```

`pnpm --filter @ai-km/service-ingestion test` 同步確認:

```
× IngestionService.ingest (E06-S042) > E06-S043 AC1+AC2 ★ 透過真實 ingest() 入口重匯 ...
Test Files  1 failed | 4 passed (5)
     Tests  1 failed | 34 passed (35)
```

AC1、AC2 皆變紅——且 AC1 是靠「finance 重匯前後查詢結果逐筆相同」的斷言變紅,
不是靠「有沒有拋錯」變紅(拋錯的斷言在這個探針下本來就會失敗,但重點是這條
規則本身:斷言的是可見性有沒有變,不是有沒有錯誤)。

**還原後(綠)**:兩個套件都回到 41/41、35/35(見上方 Gate 紀錄)。

### 反向驗證 #2 — 把原子替換改回「只覆寫、不刪除多餘 chunk」

做法:暫時讓兩個 store 的 Phase 2 只跑「寫入新 chunk」那段迴圈,拿掉「刪除
不在新集合中的舊 chunk」那段。

**紅**:

```
× createInMemoryVectorStore — E06-S043 re-ingest scope guard > AC3 ★ 相同 scope 重匯較短版本(chunk 數變少)→ 舊版多出來的 chunk 不存在於庫中
× sqlite-vec store — E06-S043 re-ingest scope guard (PF2) > AC3 ★ 相同 scope 重匯較短版本 → 直接查 _vec/_meta 證明舊的多餘 chunk 真的被刪除,不是只是查不到
Test Files  2 failed | 3 passed (5)
     Tests  2 failed | 39 passed (41)
```

只有 AC3(兩個 store 各一條)變紅——包含 AC4 的兩條原子性測試都維持綠燈,
證明這個探針精準只打中「刪除多餘 chunk」這個機制,沒有連帶弄壞別的斷言。

**還原後(綠)**:41/41(見上方 Gate 紀錄)。

## AC 對照

| AC | 實作位置 | 測試 |
|---|---|---|
| AC1(不同 scope 重匯拒絕,finance 逐筆不變) | `store.ts` `checkDocumentScopeConsistency` + `upsert` Phase 1;`sqlite-vec.store.ts` 同名邏輯 | `store.test.ts::AC1`;`sqlite-vec-store.integration.test.ts::AC1+AC2`、`::AC5`;`pipeline.test.ts::E06-S043 AC1+AC2`(真實 ingest() 入口) |
| AC2(maintenance 看不到任何東西,無部分寫入) | 同上(Phase 1 全驗證完才進 Phase 2) | `store.test.ts::AC2`;`sqlite-vec-store.integration.test.ts::AC1+AC2`;`pipeline.test.ts::E06-S043 AC1+AC2` |
| AC3(相同 scope 重匯較短版本,舊多餘 chunk 消失) | `store.ts`/`sqlite-vec.store.ts` `upsert` Phase 2 的「刪除不在新集合中的舊 chunk」 | `store.test.ts::AC3`、`::AC3b`;`sqlite-vec-store.integration.test.ts::AC3`(含直接查 `_vec`/`_meta`) |
| AC4(替換過程失敗不得新舊混合) | in-memory:Phase 1/2 分離、全同步無 await 中斷點;sqlite-vec:`BEGIN`/`COMMIT`/`ROLLBACK` 真交易 | `store.test.ts::AC4`(驗證階段失敗);`sqlite-vec-store.integration.test.ts::AC4`(注入的 mid-transaction I/O 失敗,非驗證階段失敗——ROLLBACK 後直接查表確認新舊都沒有殘留) |
| AC5(兩個 store 行為一致) | 共用的 `groupRecordsByDocumentId`/`checkDocumentScopeConsistency`(邏輯共用),各自實作替換 | `sqlite-vec-store.integration.test.ts::AC5`(同一劇本跑兩個 store 比對);AC1–AC4 每條都在兩個 store 上各自跑一份對應測試 |
| AC6(錯誤說明是 scope 變更被拒、有穩定 code、不只說「匯入失敗」) | `DocumentScopeConflictError`(`code = "DOCUMENT_SCOPE_CONFLICT"`)+ `DOCUMENT_SCOPE_CONFLICT_MESSAGE` | `store.test.ts::AC6`;`sqlite-vec-store.integration.test.ts::AC6` |
| Security AC(fail closed、錯誤不洩漏目前 scopeKey、`assertNoScopeLeak`/`AC-V6` 逐字不變) | 訊息設計上不含實際 scopeKey 值;`authorization/scope.ts` 完全未修改(`git diff` 確認) | `store.test.ts::AC6`、`sqlite-vec-store.integration.test.ts::AC6` 斷言 message 不含 `dept:finance`/`dept:maintenance`;既有 `AC-V6`、`scope.test.ts` 8 條全數原樣通過 |

## SELF-REVIEW checklist 結果

- [x] 每條 AC 都有對應實作 + 對應測試——見上方 AC 對照表,逐條列出。
- [x] 有無 scope 外變更?否。`git diff --name-only` 只命中
  `services/retrieval/**`、`services/ingestion/src/pipeline.test.ts`(測試檔,
  屬「重匯入口」的允許修改範圍),無 migration 檔變更、無 `services/generation`
  或 `services/model-gateway`、無 `apps/api`。
- [x] 有無發明 contract 未定義的 endpoint/欄位/權限?否。`DocumentScopeConflictError`
  是 in-process 拋出的 TypeScript 例外,不是新 HTTP endpoint 或 contract 欄位;
  `pnpm contract-gate` 綠燈確認 `*-compat.ts` 零錯誤。未新增任何資料庫欄位。
- [x] Security AC:未授權路徑真的被測過且被拒?錯誤格式符合 contract?
  是——`AC1`/`AC2`/`AC6` 系列測試涵蓋兩個 store;`assertNoScopeLeak`、
  partition-key pre-filter(`AC-V6`)完全未修改且全數通過(這不是走 HTTP
  contract 的錯誤,是 in-process 例外,無 HTTP 錯誤格式適用)。
- [x] UX AC:error/loading/empty/permission-denied 狀態都有處理?本 story
  的層級是 store/ingestion-service(無 UI),UX AC 不適用;`IngestionScopeError`
  等既有錯誤路徑未受影響(35/35 ingestion 測試全綠)。
- [x] 有無留下 debug 殘留(console.log、註解掉的程式、TODO 無 ticket)?否。
  兩次反向驗證的暫時性探針(`REVERSE-VERIFICATION PROBE` 註解)皆已還原,
  `grep -n "REVERSE-VERIFICATION" services/retrieval/src/vector/*.ts` 確認
  零命中(commit 前執行過)。
- [x] 是否有任何「mock 通過就當作整合完成」的宣稱?否。sqlite-vec 測試對
  真實 `better-sqlite3` + `sqlite-vec` extension 跑(非 mock),含直接 SQL
  查 `_vec`/`_meta` 表;唯一用到結構化 mock db 的地方(AC4 的
  mid-transaction 失敗測試)明確標註是「注入的 I/O 失敗」用於製造真交易失敗
  情境,不是拿 mock 通過來宣稱整合完成——同一測試最後仍直接對真實資料庫連線
  下 SQL 驗證 ROLLBACK 後的實際內容。

## Assumptions / 計畫外事項

1. **同一次 upsert 呼叫內部就帶兩個不同 scopeKey(同一 documentId)也視為衝突
   而拒絕**——spec 明確描述的情境是「先後兩次匯入用不同 scope」,沒有明講
   「同一批次內部自相矛盾」這種情況。判斷:這仍然是同一條 Deny-Wins 底線的
   延伸(不縮小拒絕範圍,只是把同一守門邏輯也套用到批次內部自相矛盾的輸入),
   不是在回答 Scope Out 裡「文件能否屬於多個 scope」那個問題——即使沒有這個
   額外檢查,現有的「與既有 chunk 比對」機制也會在下一次獨立呼叫時擋下真正的
   跨部門搬移;這個額外檢查只是讓「同一批次內部就已經自相矛盾」這種輸入更早、
   更明確地被拒絕,而不是被兩個 scopeKey 中較晚寫入的那個悄悄決定結果。有一條
   測試(`store.test.ts`「同一次 upsert 呼叫本身對同一 documentId 帶兩個不同
   scopeKey」)專門釘住這個決定,方便日後 review 時單獨評估是否要調整。
2. **sqlite-vec 的交易改用 `db.exec("BEGIN"/"COMMIT"/"ROLLBACK")` 而非
   better-sqlite3 原生的 `db.transaction()`**——因為 `SqliteDatabase` 是本檔案
   自訂的 structural type(讓沒裝 better-sqlite3 的環境也能 typecheck),幫它
   加一個 `transaction()` 方法會擴大這個介面的面積,而既有測試裡的
   `counting`/`faulty` db 替身都只實作 `prepare`/`exec`/`close` 三個方法。
   用 `exec()` 跑裸 SQL `BEGIN`/`COMMIT`/`ROLLBACK` 不需要擴大介面,且
   better-sqlite3、真實連線上的語意等價。
3. 未新增任何 migration——`_meta`/`_vec` 表結構本來就有 `chunk_id`/`document_id`
   欄位,足以支撐這次的查詢與刪除;沒有新增欄位。
4. **未回答 Scope Out 的兩個問題**(文件能否屬於多個 scope;`chunkId` 是否該帶
   scope 維度)——維持 spec 要求的「在使用者裁示之前一律拒絕」,未在本 story
   內做任何決定或鋪路性的 schema 變更。

## Rollback

`git revert` 本 story 在 `story/E06-S043-reingest-scope-guard` 上的 commit(s),
或直接捨棄該分支——目前尚未 merge 進 main,亦未 push。
