# E07-S003 — Serial-number input

- Branch: `story/E07-S003-serial-number-input`
- Date: 2026-08-15
- Status: DONE

## Scope 說明

E07 第三個 story,延續 S002 已建立的「`/maintenance/new` 是 S002-S005
共用的同一張表單,各自負責加一個欄位」設計(理由見 E07-S002.md 自己的
「設計決策」章節)。本 story 在同一張表單上新增「設備序號」欄位。

**`serialNumber` 為何是選填,而非必填**:S002 自己的 EVIDENCE 已經明確
記載「設備選擇→建立案例→出現在首頁,完整閉環,不是半成品」——如果
S003 把序號改成必填,會回頭破壞 S002 已經審核通過、已經合併的能力
(單靠設備就能建立有效案例)。因此採用與 `KnowledgeBaseSummary` 完全
相同的先例:`visibleToRoles`/`members`/`boundPrompt`/`boundModel`/
`folderSyncPath` 全部是選填欄位,S003(KB 那邊的「新增 KB 表單」)
建立的「只有名稱」的知識庫,在 S006-S016 陸續加完所有欄位後依然有效
——同一種「持續新增選填欄位到同一個實體上」的形狀。序號本身沒有做
格式驗證(只有 trim)——SOURCE_BASELINE 沒有任何地方定義序號格式,
發明一個會直接違反 Anti-hallucination Guard「不確定的規則不可假裝
存在」。

## 變更檔案

- 修改:`apps/web/src/lib/maintenance-cases.ts`(`MaintenanceCaseSummary`
  新增 `serialNumber?`,`createMaintenanceCase` 新增第二參數)
- 修改:`apps/web/src/lib/maintenance-cases.test.ts`(新增
  `serialNumber` 測試區塊)
- 修改:`apps/web/src/app/(app)/maintenance/new/page.tsx`(新增序號
  文字輸入欄位,選填,telemetry 刻意不含序號本身)
- 修改:`apps/web/src/app/(app)/maintenance/new/page.test.tsx`(修正
  既有「submits the selected equipmentId」測試的呼叫參數斷言、新增
  序號欄位測試區塊)
- 修改:`apps/web/src/app/(app)/maintenance/_components/
  maintenance-case-list.tsx`(有序號才顯示,鏡射
  knowledge-document-list.tsx 對 sizeBytes 的既定作法)
- 修改:`apps/web/src/app/(app)/maintenance/_components/
  maintenance-case-list.test.tsx`(新增序號顯示/不顯示測試)
- 修改:`tests/e2e/specs/maintenance-home.spec.ts`(既有建立案例流程
  測試補上序號欄位;新增「只填設備、不填序號依然可以建立案例」測試)

## Contract / Migration 差異

None——純前端 mock 欄位新增,不涉及任何跨組 contract。

## Gate 紀錄

第一輪即全綠,沒有 FIX 循環:
```
$ pnpm typecheck                        exit 0
$ pnpm lint                             exit 0
$ pnpm --filter @ai-km/web test         928 passed(含新增 7 個測試)
$ pnpm --filter @ai-km/e2e test          158 passed (2.2m)
$ pnpm build                            12/12 successful
```

## AC 對照

**Functional**:
1. 合法輸入 → 預期成功結果:單元測試「stores a trimmed serial number
   when one is given」+ E2E 測試(選設備+填序號→建立→首頁顯示
   「序號:SN-2026-0042」)。
2. 缺少必要輸入 → fail-closed:序號本身選填,無「缺少」情境;唯一
   必要輸入(設備)仍由 S002 已建立的驗證把關,單元測試「stays
   valid...when serialNumber is omitted, empty, or whitespace-only」
   確認省略序號不影響案例建立成功;E2E 新增測試明確驗證「只填設備、
   不填序號」依然完整可用,且清單上不會出現空的「序號:」行。
3. 無權限使用者 → N/A,同 S001/S002——沿用既有 RoleGuard。
4. Dependency timeout/unavailable → 沿用 S002 已有的
   SERVICE_UNAVAILABLE mock 情境測試,序號欄位不影響這條路徑。
5. 重複請求 → 沿用 S002 已有的 pending-guard 測試,序號欄位是額外
   state,不影響既有防護機制。
6. Correlation id/telemetry → 沿用 S002 已有機制;新增測試明確驗證
   序號本身(自由文字)不會出現在任何 telemetry payload 中。
7. Audit event → N/A,同 S002。
8. MVP 可簡化但能力不可缺席 → 序號輸入是真實可用的欄位,有輸入、
   有持久化、有條件式顯示,不是半成品。

**Security**:N/A,理由同 S001/S002。

**Data/Contract**:None。

**UX**:loading/error/success 狀態沿用 S002 已驗證的機制,序號欄位
本身不引入新狀態;validation N/A(選填欄位無格式要求);permission
N/A。

## SELF-REVIEW

- [x] 每條 AC 都有對應實作+測試(見上方 AC 對照)
- [x] 無 scope 外變更——`git status --porcelain` 確認只修改了 7 個
      S001/S002 已建立的既有檔案,沒有新增任何檔案,皆在 Team A
      允許清單內
- [x] 無發明 contract 未定義的 endpoint/欄位/權限——零 contract
      變更;序號格式刻意不驗證(SOURCE_BASELINE 未定義任何格式)
- [x] Security AC 檢查——N/A,理由已附
- [x] UX AC 檢查——沿用 S002 已驗證機制,新欄位本身無新增狀態需要
      額外處理
- [x] 無 debug 殘留——已用 grep 確認所有修改檔案內無
      console.*/TODO/FIXME/debugger
- [x] 無「mock 通過就當作整合完成」的宣稱——同 S001/S002,誠實記載
      純前端 mock
- [x]「是否重複已知的 race condition 類別」複查——`NewMaintenanceCase
      Page` 新增的 `serialNumber` state 與既有 `equipmentId` 同一個
      元件、同一個表單、同一次送出,無跨元件/跨視圖的非同步協調
      需求;`MaintenanceCaseList` 的條件式顯示是純渲染邏輯,無新增
      非同步或 mutation 面;未發現新的 race 類別

## Assumptions / 計畫外變更 / 未解疑問

- 沒有計畫外的原始碼變更——本 story 完全依照 S002 已建立的「共用表單
  加欄位」設計進行,沒有觸碰任何 S002 範圍以外的檔案。

## Rollback

Revert 這個 branch 對應的 commits(全部是對 S001/S002 既有檔案的修改,
revert 後回到 S002 剛完成時的狀態,無新增檔案需要額外處理)。

## 獨立審核結果

以「沒有參與實作的獨立審查者」身分重新核對,不信任 EVIDENCE 的文字
敘述,逐項親自複驗:

1. **Diff 範圍**:`git diff main...HEAD --name-status` 共 9 個檔案,
   全部是修改 S001/S002 已建立的既有檔案(無新增原始碼檔案),皆在
   Team A 允許清單內。
2. **造假跡象掃描**:grep 全部修改檔案的
   `.skip`/`.only`/`passWithNoTests`/`force-exit`/`xdescribe`/`xit(`
   與 `console.*`/`TODO`/`FIXME`/`debugger`,均為 0 命中。
3. **對抗性驗證核心主張**(本 story 沒有具體的 bug fix 可還原重現,
   審核者改為直接驗證 EVIDENCE 最關鍵的宣稱——「序號選填,不會回頭
   破壞 S002 的僅設備流程」是否真的被測試網住,而非只是文件上的
   宣稱):在 `createMaintenanceCase` 內暫時注入「序號為空即拒絕」的
   假 bug(模擬 EVIDENCE 聲稱被避免的那個確切錯誤),重跑
   `apps/web` 的 maintenance 相關單元測試。**3 個測試立即失敗**——
   不只是 S003 自己新增的測試,連 **S002 自己原本就有**的
   「a newly created case appears in listMaintenanceCases()」測試
   也失敗,證實這個屬性從 S002 就已經被測試保護,S003 沒有削弱它,
   而是提供了第二層保護。還原注入的假 bug,確認
   `git diff HEAD -- apps/web/src/lib/maintenance-cases.ts | wc -l`
   = 0。
4. **Gate 全部強制單獨重跑**:`typecheck --force` 20/20、
   `lint --force` 20/20、`build --force` 12/12,皆單獨執行,皆過。
   審核者自己重新觸發的全量測試:`pnpm --filter @ai-km/web test`
   928 passed,`pnpm --filter @ai-km/e2e test` 158 passed。
5. **PROGRESS.md 逐列手動計數複核**:
   `grep -E "^\| E[0-9]+-S[0-9]+ \|"` 全檔案計數為
   85 approved + 1 done + 1 blocked-team-b + 88 todo = 175,與總覽表
   `175 | 85 | 1 | 0 | 1 | 88` 完全一致;E07 章節單獨計數為
   2 approved + 1 done + 22 todo = 25,與 E07 列
   `25 | 2 | 1 | 0 | 0 | 22` 完全一致。
6. **telemetry 不含序號本身的宣稱複查**:直接讀取
   `maintenance/new/page.tsx` 的 `handleSubmit`,確認
   `trackEvent(...)` 呼叫的 `properties` 只有 `equipmentId`,從未把
   `serialNumber` 傳入任何 telemetry 呼叫;對應單元測試也確實用
   `JSON.stringify` 掃過全部 telemetry 呼叫的 properties 找序號字串,
   不是空殼斷言。

**結論:0 個 BLOCKER、0 個 MAJOR、0 個 MINOR。APPROVE。**

本 story 是這個 session 至今唯一一個第一輪 gate 就全綠、零 FIX 循環
的 E07 story——審核者複查後認為這與其他 story 的品質落差無關,單純
是這個 story 的範圍(在既有、已被充分驗證的表單上加一個結構相同的
選填欄位)本身風險就低。
