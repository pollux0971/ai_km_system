# E09-S002 — Natural-language query composer

- Branch: `story/E09-S002-nl-query-composer`
- Date: 2026-08-16
- Status: DONE

## Scope 說明

E09 的第二個 story,對應 E09-S001「ERP assistant home」自己的 doc
comment 明確標記為刻意延後的能力——「開始新的 ERP 查詢」入口連結與其
目標路由。與 E07-S002「Equipment selector」對 E07-S001「Maintenance
home」的關係完全對應。

## 設計決策

**單一自由文字問題,而非多欄位表單**:E07-S002 到 E07-S005 花了 4 個
story 逐步為 `/maintenance/new` 這「同一個表單」增加欄位(設備/序號/
錯誤代碼/問題描述)。E09 的 story 序列不是這個形狀——S003「Query
scenario selector」、S004「Clarification UI」、S005「Query confirmation
UI」、S006「Query loading state」都是「送出問題之後」的獨立畫面/狀態,
不是輸入表單本身的欄位。因此本 story 的 composer 刻意只有一個欄位:
自由文字問題,送出後就建立查詢紀錄並導向該查詢自己的頁面
(`/erp/[id]`),把「送出之後的畫面」完全留給 S003 以後的 story。

**`/erp/[id]` 本 story 就建立(而非等待專屬的「shell」 story)**:E09
的 24 個 story 標題中沒有一個是「Query session shell」這種獨立 story
(對照 E07-S006「Diagnostic session shell」是與 E07-S002 分開的
story)。若 S002 只做 composer、不順便建立 `/erp/[id]` 的最小可渲染
內容,送出後會無處可去。因此 S002 自己建立 `/erp/[id]` 的最小 shell
(顯示問題文字+時間戳記+返回連結),鏡射 `case-detail.tsx`(E07-S021)
的 loading/error/not-found/loaded 狀態機形狀,但內容故意最小化——S003
以後的 story 才會在這個頁面加上 scenario/clarification/confirmation/
loading/結果等真正內容。

**`ErpQuerySummary` 型別直接擴充,不建立平行型別**:比照
`MaintenanceCaseSummary` 從 S001 到 S005 逐步長出 `equipmentId`/
`serialNumber`/`errorCode` 欄位、而非另建一個「MaintenanceCase」型別的
既定作法。本 story 沒有為 `ErpQuerySummary` 新增欄位(`id`/
`questionText`/`createdAt` 對建立/查詢單一查詢已經足夠),只新增
`getErpQuery`(單筆查詢,鏡射 `getMaintenanceCase`)與
`submitErpQuery`(建立,鏡射 `createMaintenanceCase` 的 fail-closed
驗證與 trim 慣例)兩個函式,以及本檔案第一次呼叫的 `writeStore`。

**`ErpQueryList`(S001)刻意不 retrofit 連結**:即使 `/erp/[id]` 現在
存在,`ErpQueryList` 的既有、已核准測試明確斷言「zero links」——比照
`CaseDetail`(E07-S021)自己的 doc comment 對 `MaintenanceCaseList` 立下
的相同自我約束(該測試不屬於「新增互動步驟、斷言不變」的窄例外,因為
把項目變成連結會直接翻轉那個斷言的真假值)。本 story 完全沒有觸碰
`erp-query-list.tsx`/`erp-query-list.test.tsx`(`git diff` 確認零異動)。

**`route-guards.spec.ts` 既有測試不受影響**:該檔案自己在 E09-S001 之後
已經沒有任何「page-less restricted route」的候選目標,本 story 未觸碰
該檔案,其僅存的 unknown-route 測試與本 story 無關聯,獨立驗證通過。

## 變更檔案

```
apps/web/src/lib/erp-queries.ts                                        (edit — + writeStore/getErpQuery/submitErpQuery)
apps/web/src/lib/erp-queries.test.ts                                   (edit — + getErpQuery/submitErpQuery 測試,S001 區塊零異動)
apps/web/src/app/(app)/erp/new/page.tsx                                (new)
apps/web/src/app/(app)/erp/new/page.test.tsx                           (new)
apps/web/src/app/(app)/erp/[id]/page.tsx                               (new)
apps/web/src/app/(app)/erp/[id]/_components/erp-query-detail.tsx       (new)
apps/web/src/app/(app)/erp/[id]/_components/erp-query-detail.test.tsx  (new)
apps/web/src/app/(app)/erp/page.tsx                                    (edit — + 「開始新的 ERP 查詢」入口連結)
tests/e2e/specs/erp-home.spec.ts                                       (edit — S001 測試 + 連結存在斷言,S002 新測試)
docs/stories/PROGRESS.md                                               (edit — todo → in-progress → done)
```

`git diff main --stat -- contracts/ AI_KM_BMAD_High_Granularity/ apps/api apps/worker-* services/ db/`
→ 空。`git diff main --stat -- apps/web/src/app/(app)/erp/_components/erp-query-list.tsx apps/web/src/app/(app)/erp/_components/erp-query-list.test.tsx`
→ 空(確認未 retrofit)。

## Contract / Migration 差異

None。純前端 mock,無 contract、無 migration,與 E09-S001 相同的
E10-未存在前提。

## Gate 紀錄

```
pnpm typecheck (@ai-km/web + deps)   → 10/10 通過
pnpm lint (@ai-km/web + deps)        → 10/10 通過
pnpm build (@ai-km/web)              → 9/9 通過(/erp、/erp/[id]、/erp/new 三個路由皆出現在 manifest)
pnpm test unit (@ai-km/web)          → 93 檔案 / 1206 tests 全過(較 S001 的 91/1188 增加 2 檔案 18 個測試:
                                        erp-queries.test.ts +4、page.test.tsx(NewErpQueryPage)9、
                                        erp-query-detail.test.tsx 5)
pnpm test E2E (@ai-km/e2e)           → 203/203 全過(較 S001 的 202 增加本 story 新增的 1 個測試;
                                        erp-home.spec.ts 兩個測試單獨重跑也先確認過)
```

L2/L3/L4:與 S001 相同判定——無 contract、無跨服務整合、無新增授權邏輯
(沿用既有 RoleGuard,見 E09-S001.md 的完整推理,本 story 未修改任何
授權相關程式碼)。

## SELF-REVIEW

**對抗性突變複驗**(鎖定本 story 真正新增的業務邏輯:`submitErpQuery`
的空白驗證):`git add -A` 全部 staged 後,把 `if (!trimmed)` 突變成
`if (false)`(讓驗證永遠不觸發),單獨執行 `erp-queries.test.ts`——精準
命中預期的唯一失敗:「fails with VALIDATION_ERROR for an empty or
whitespace-only question...」(1 failed, 6 passed)。`git checkout --
apps/web/src/lib/erp-queries.ts` 還原(從 repo root 執行),`git diff
--stat`/`git status --short` 確認乾淨,重跑確認 7/7 恢復全過。

## AC 對照

| AC | 對應實作 | 對應測試 |
|---|---|---|
| 1. 合法輸入 → 預期成功結果 | `submitErpQuery` 建立+回傳;`NewErpQueryPage` 送出+導向;`ErpQueryDetail` 顯示 | `erp-queries.test.ts`「creates a query with the trimmed...」;`page.test.tsx`「submits the typed question...」;`erp-home.spec.ts` S002 測試 |
| 2. 缺少必要輸入 → fail-closed validation error | `submitErpQuery` 對空白/純空格問題回傳 `VALIDATION_ERROR`,無 side effect | `erp-queries.test.ts`「fails with VALIDATION_ERROR...」;`page.test.tsx` 送出按鈕停用測試(client 端 defense-in-depth) |
| 3. 無權限使用者 | 既有 `RoleGuard`(未修改)| 見 E09-S001.md |
| 4. dependency error → 可分類錯誤 | `NewErpQueryPage`/`ErpQueryDetail` 的 error 分支 | `page.test.tsx`「shows a distinct error alert...」;`erp-query-detail.test.tsx`「shows a distinct error state...」 |
| 5. 重複請求不得造成未定義 side effect | 送出按鈕在 pending 期間停用,防止 double-submit | `page.test.tsx`「disables the submit button while the request is pending...」 |
| 6. correlation id / telemetry | `logger`/`trackEvent` 皆帶 `correlationId`,attempt/success 共用同一個 id | `page.test.tsx`「emits attempt and success telemetry sharing the same correlation id...」 |
| 7. 敏感操作需 audit event | 不適用——內容建立動作,非存取控制變更,同 `createMaintenanceCase` 自己的既定判斷 | 記入 assumptions |
| 8. MVP 簡化 | 符合(單一文字欄位,無複雜互動) | — |

Data/Contract AC:UTC storage——`createdAt` 為 `new Date().toISOString()`,
同 `createMaintenanceCase` 既有慣例。UX AC:loading/empty(N/A,建立
動作無 empty 狀態)/success/error/permission-denied 皆有處理;
Telemetry 刻意排除 `questionText` 本身(自由文字使用者內容,同
`problemDescription` 既有克制)。

## SELF-REVIEW checklist

- [x] 每條 AC 都有對應實作 + 對應測試(不適用的已記錄理由)
- [x] 無 scope 外變更(`git diff main --stat` 10 個檔案,皆在允許清單內;
      禁止清單資料夾零命中)
- [x] 無發明 contract 未定義的 endpoint/欄位/權限
- [x] Security AC:未修改任何授權邏輯,沿用既有機制
- [x] UX AC:error/loading/permission-denied 狀態都有處理
- [x] 無 debug 殘留(重新讀取全部新檔案確認無 console.log、無註解掉的
      程式碼、無無 ticket 的 TODO)
- [x] 無「mock 通過就當作整合完成」的宣稱

## Assumptions / 計畫外變更 / 未解疑問

- Functional AC 7(audit event)判定不適用——同 `createMaintenanceCase`
  自己的既定判斷(內容建立,非存取控制變更)。
- `/erp/[id]` 目前只是最小 shell(問題文字+時間戳記+返回連結),S003
  「Query scenario selector」起才會加上真正的查詢流程內容——這是本
  story 刻意留白,不是遺漏。
- `ErpQueryList`(S001)刻意不 retrofit 連結,理由與 `CaseDetail`
  (E07-S021)對 `MaintenanceCaseList` 的既有先例完全相同,已在設計決策
  段落記錄。

## Rollback

`git revert` 本 story 合併後的 merge commit(`--no-ff`)。無 migration、
無 contract 變更,回滾無副作用。
