# E09-S008 — Result table

- Branch: `story/E09-S008-result-table`
- Date: 2026-08-16
- Status: DONE

## Scope 說明

E09 的第八個 story,在「文字摘要」(S007)之後,顯示查詢結果的表格化
資料——「顯示結果」子流程(S007-S011:文字摘要/表格/分頁/KPI 卡片/
圖表)的第二個,只負責一次性顯示整張表格,不做分頁(S009 自己的範圍)。

## 設計決策

**純粹疊加,不動 S006/S007 任何既有內容**:與 S007 完全相同的理由——
「已執行完成」是真實、會持續存在的靜止畫面,S008 只是在 S007 的摘要文字
「旁邊多加一張表格」,不移除、不取代任何既有內容。S006/S007 的每一個
既有測試(含 E2E)都完全不需要異動——`git diff main --stat` 顯示 5 個
檔案共 190 行新增、**零刪除**(diffstat 本身即可證實,無需額外 grep)。

**表格內容綁定「情境」,不綁定「個別查詢」**:同 `erp-results.ts` 的
既有理由——這個 mock 系統沒有真實變動的資料,同一個情境永遠得到同樣的
表格是誠實的(不假裝有即時資料)。`getErpResultTable` 是純函式,以
`scenarioId` 為 key 查表,不持久化到 `ErpQuerySummary` 本身。

**列數與 `erp-results.ts` 自己的文字摘要對齊,不互相矛盾**:例如
`revenue-by-branch` 的摘要說「3 個分公司」,表格就真的是 3 列(台北/
台中/高雄);`low-stock-items` 摘要沒有明確列數宣稱,表格用 5 列品項。
這是誠實 mock 資料的基本要求——兩個獨立檔案的數字不能互相打架。

**獨立檔案,不加到 `erp-results.ts` 本身**:「文字摘要」(S007 的責任)
與「表格資料」(本 story 的責任)是不同關注點,只是剛好共用同一個
scenarioId key——同一種「每個 story 在自己的檔案裡管自己的事」紀律,
`erp-results.ts` 相對 `erp-scenarios.ts` 已經是這樣。

**Fallback 保證表格結構永遠合法**:未知 scenarioId 時回傳
`{ columns: ["查無資料"], rows: [] }` 而非拋錯或回傳零欄位——零欄位的
表格會渲染出空的 `<thead><tr></tr></thead>`,是結構性壞掉的表格,不是
「查無資料」的誠實呈現。這個保證由 SELF-REVIEW 的對抗性突變直接驗證
(見下)。

**AC7(audit)判定不適用**:同 S007 的理由——顯示一個已經執行過、已經
被 audit 過的結果,不是新的敏感操作。

## 變更檔案

```
apps/web/src/lib/erp-result-tables.ts                                  (new)
apps/web/src/lib/erp-result-tables.test.ts                             (new)
apps/web/src/app/(app)/erp/[id]/_components/erp-query-detail.tsx       (edit — 疊加表格,S006/S007 既有內容零異動)
apps/web/src/app/(app)/erp/[id]/_components/erp-query-detail.test.tsx  (edit — 純新增 2 個測試,零刪除零修改)
tests/e2e/specs/erp-home.spec.ts                                       (edit — 純新增斷言)
docs/stories/PROGRESS.md                                               (edit — in-progress → done)
```

`git diff main --stat -- contracts/ AI_KM_BMAD_High_Granularity/ apps/api apps/worker-* services/ db/`
→ 空。`git diff main --stat -- apps/web tests/e2e` → `5 files changed,
190 insertions(+)`,diffstat 本身零刪除,確認純疊加。

## Contract / Migration 差異

None。

## Gate 紀錄

```
pnpm typecheck (workspace)   → 20/20 通過
pnpm lint (workspace)        → 20/20 通過
pnpm build (workspace)       → 12/12 通過
pnpm test unit (@ai-km/web)  → 97 檔案(+1:erp-result-tables.test.ts)/ 1252 tests 全過(較 S007 的
                                96/1246 增加 6 個測試:erp-result-tables.test.ts 4、
                                erp-query-detail.test.tsx +2,既有 1246 個測試零異動)
pnpm test E2E (@ai-km/e2e)   → 203/203 全過(本story只新增斷言,總數不變,系統負載中等 7.90 一次
                                乾淨通過)
```

L2/L3/L4:無 contract、無跨服務整合、無新增授權邏輯。

## SELF-REVIEW

**對抗性突變複驗**(鎖定本 story 真正的邏輯:`getErpResultTable` 對
未知 scenarioId 的 fallback 保證——表格結構必須永遠合法,不能是零欄位的
壞表格):`git add -A` 全部 staged 後,把 `?? { columns: ["查無資料"],
rows: [] }` 突變成 `?? { columns: [], rows: [] }`,單獨執行
`erp-result-tables.test.ts`——精準命中預期的唯一失敗:「returns an
empty-rows table (not a crash) for an unrecognized scenario id」
(`expected 0 to be greater than 0`,1 failed, 3 passed)。
`git checkout -- apps/web/src/lib/erp-result-tables.ts` 還原(從 repo
root 執行),`git status --short` 確認乾淨(僅 5 個原本 staged 的檔案,
無額外異動),重跑確認 4/4 恢復全過,並重新執行完整 gate 序列
(typecheck/lint/build/unit/E2E)全部維持綠燈。

## AC 對照

| AC | 對應實作 | 對應測試 |
|---|---|---|
| 1. 合法輸入 → 預期成功結果 | 執行完成後顯示情境專屬表格(欄位+列) | `erp-result-tables.test.ts`;`erp-query-detail.test.tsx`「shows the scenario's own result table...」;`erp-home.spec.ts` 新增斷言 |
| 2-7 | 不適用——純呈現層,無新輸入、無新 side effect、無新 telemetry 動作、無新敏感操作 | 記入 assumptions |
| 8. MVP 簡化 | 符合(固定表格查表,非真正資料查詢/分頁——分頁是 S009 自己的範圍) | — |

UX AC:表格只在真正執行完成後顯示(`erp-query-detail.test.tsx`
「does not show any result table before execution completes」);未知
scenarioId 不會渲染出結構壞掉的表格(fallback 保證,見對抗性突變)。

## SELF-REVIEW checklist

- [x] 每條 AC 都有對應實作 + 對應測試(不適用的已記錄理由)
- [x] 無 scope 外變更(`git diff main --stat` 5 個檔案,皆在允許清單內;
      禁止清單資料夾零命中)
- [x] 無發明 contract 未定義的 endpoint/欄位/權限
- [x] Security AC:未修改任何授權邏輯
- [x] UX AC:表格只在正確時機顯示,且 fallback 保證結構永遠合法
- [x] 無 debug 殘留(`grep` 對整個 diff 的新增行搜尋
      skip/only/passWithNoTests/`|| true`/TODO/console.\* 全部零命中)
- [x] 無「mock 通過就當作整合完成」的宣稱
- [x] **確認純疊加**:`git diff main --stat` 對 5 個變更檔案回傳
      「190 insertions(+)」、零刪除,確認本 story 沒有刪除或修改任何
      既有測試斷言

## Assumptions / 計畫外變更 / 未解疑問

- Functional AC 2/3(除既有 RoleGuard)/4/5/6/7 判定不適用——本 story
  是純呈現層,沒有新增任何使用者輸入、非同步 mutate、或敏感操作,與
  S007 判定理由相同。
- 表格列數刻意與 `erp-results.ts` 既有文字摘要的數字宣稱對齊(例如
  revenue-by-branch 的「3 個分公司」對應 3 列),避免兩個 mock 資料來源
  互相矛盾;這是實作時的細節決策,不影響 AC 判定,故記錄於此而非另開
  advisor 流程。

## Rollback

`git revert` 本 story 合併後的 merge commit(`--no-ff`)。無 migration、
無 contract 變更,回滾無副作用。
