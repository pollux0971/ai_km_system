# E09-S011 — Chart renderer

- Branch: `story/E09-S011-chart-renderer`
- Date: 2026-08-16
- Status: DONE

## Scope 說明

E09 的第十一個 story,在「KPI 卡片」(S010)之後,加一個長條圖呈現查詢
結果——「顯示結果」子流程(S007-S011)的最後一個。至此摘要文字/表格
/分頁/KPI 卡片/圖表五種呈現形式全部到齊,S012 起進入不同的子流程
(applied-filter/data-freshness/source-system 等中繼資訊呈現)。

## 設計決策

**沒有既有圖表函式庫,不新增依賴**:實作前搜尋確認
`packages/ui`、`apps/web/package.json`、`pnpm-lock.yaml` 全部沒有任何
圖表函式庫(recharts/chart.js/victory/visx/nivo/apexcharts/d3 等零
命中)。這個 epic 至今沒有任何 story 新增過 npm 依賴,新增一個圖表庫
是比「加一個檔案」重得多、更難回退的決定(套件、bundle size、往後的
維護面)。AC 8 明確允許「MVP 可以簡化視覺」,因此選擇手刻一個
零依賴、純 CSS 寬度比例的水平長條圖,而不是引入新依賴。

**長條資料直接來自表格本身,不是另一份手寫資料**:同一種
`erp-result-kpis.ts` 已經建立的「衍生而非手寫」紀律(直接回應
E09-S008 獨立審核抓到的跨檔案數字漂移教訓)。每一根長條的 label 是
該列第一格(`row[0]`)、旁邊顯示的文字是該列第二格(`row[1]`)原文
——兩者都是表格已經核准、已經顯示過的既有內容,不是重新計算或猜測
出來的新數字。

**用第二欄位驅動長條寬度,失敗則整體退回等寬**:4 個情境的表格結構
不同,不是每個情境的第二欄位都是數字(`purchase-order-status` 的
第二欄是「供應商」,是人名/公司名文字)。與其為每個情境各自寫一套
「這個情境該抓哪一欄」的特判邏輯(容易出錯、難以維護、也是另一種
沒有真相來源的猜測),選擇統一規則:嘗試解析每一列的第二格
(`parseNumericCell`,處理 `NT$`/千分位逗號/`%`/`+` 前綴這些既有
mock 資料實際出現過的格式);只要表格裡「任何一列」解析失敗,整張
圖表就退回等寬長條(仍然把每一列都畫出來,只是不假裝有量化比例)
——寧可誠實地退化成「條列」也不要顯示錯誤或誤導的比例。

**AC 2-7 判定不適用**:與 S007-S010 判定理由相同——圖表是純衍生
呈現層,沒有新的使用者輸入、非同步 dependency、資料存取、或敏感操作。

## 變更檔案

```
apps/web/src/lib/erp-result-charts.ts                                  (new)
apps/web/src/lib/erp-result-charts.test.ts                             (new — 9 tests)
apps/web/src/app/(app)/erp/[id]/_components/erp-query-detail.tsx       (edit — 疊加圖表於 KPI 卡片與表格之間,
                                                                          S006-S010 既有內容零異動)
apps/web/src/app/(app)/erp/[id]/_components/erp-query-detail.test.tsx  (edit — 純新增 2 個測試 + import within,零刪除零修改)
tests/e2e/specs/erp-home.spec.ts                                       (edit — 純新增斷言)
docs/stories/PROGRESS.md                                                (edit — in-progress → done)
```

`git diff main --stat -- contracts/ AI_KM_BMAD_High_Granularity/ apps/api apps/worker-* services/ db/`
→ 空。`git diff main --cached --stat -- apps/web tests/e2e` →
`5 files changed, 208 insertions(+), 1 deletion(-)`——唯一的刪除行是
測試檔案 import 陳述式從 `waitFor` 擴充成 `waitFor, within`(git 把
「修改一行」表示為刪除舊行+新增新行),不是任何測試斷言被移除;純
疊加,不需要窄化任何既有測試(圖表的 `role="group"` 容器與 KPI 卡片
的既有測試互不干擾)。

## Contract / Migration 差異

None。

## Gate 紀錄

```
pnpm typecheck (workspace)   → 20/20 通過
pnpm lint (workspace)        → 20/20 通過
pnpm build (workspace)       → 12/12 通過
pnpm test unit (@ai-km/web)  → 100 檔案(+1:erp-result-charts.test.ts)/ 1285 tests 全過
                                (較 S010 的 99/1274 增加 11 個測試:charts 檔案 9、
                                erp-query-detail.test.tsx +2 新增)
pnpm test E2E (@ai-km/e2e)   → 完整套件跑出 2 個失敗(knowledge-ui-e2e.spec.ts E05-S031、
                                maintenance-e2e.spec.ts E07-S025),但兩者都是本 session 已多次記錄
                                過的既有 flaky 測試,與本 story 的 erp-home.spec.ts 零關聯;
                                跑出時系統負載達 21.95(異常高),E07-S025 的失敗訊息是
                                page.goto() 導覽逾時,典型資源競爭症狀;獨立隔離重跑
                                erp-home.spec.ts(僅本 story 觸及的檔案,--workers=1)
                                2/2 乾淨通過,確認本 story 自己的變更完全無關;不視為
                                本story的 FIX 循環(無需修正本story自己的任何程式碼)
```

L2/L3/L4:無 contract、無跨服務整合、無新增授權邏輯。

## SELF-REVIEW

**對抗性突變複驗**(鎖定本 story 真正的邏輯:`getErpResultChart` 的
比例正規化——最大值那一列必須是 100%,其餘依比例縮放):`git add -A`
全部 staged 後,把 `Math.max(...(parsedValues as number[]))` 突變成
`Math.min(...(parsedValues as number[]))`(拿最小值當分母而非最大值)
,單獨執行 `erp-result-charts.test.ts`——精準命中預期的唯一失敗:
「computes a proportional width when every row's second cell is
numeric, with the largest value at 100%」(`expected
152.94117647058823 to be 100`,1 failed, 8 passed),確認比例正規化
邏輯真的有被測試覆蓋。`git checkout --
apps/web/src/lib/erp-result-charts.ts` 還原(從 repo root 執行),
`git status --short`/`git diff --stat` 確認乾淨,重跑確認 9/9 恢復
全過。

## AC 對照

| AC | 對應實作 | 對應測試 |
|---|---|---|
| 1. 合法輸入 → 預期成功結果 | 執行完成後顯示情境專屬長條圖(每列一根長條,標籤+細節文字) | `erp-result-charts.test.ts`;`erp-query-detail.test.tsx`「ErpQueryDetail chart (E09-S011)」2 個測試;`erp-home.spec.ts` 新增斷言 |
| 2-7 | 不適用——純呈現層的衍生視覺化,無新輸入驗證情境、無新非同步 dependency、無新資料存取、無新敏感操作 | 記入 assumptions |
| 8. MVP 簡化 | 符合(零依賴 CSS 長條圖取代真正圖表庫;無法解析為數字時誠實退回等寬,不假裝比例) | — |

UX AC:圖表只在執行完成後顯示(`erp-query-detail.test.tsx`
「does not show any chart before execution completes」)。

## SELF-REVIEW checklist

- [x] 每條 AC 都有對應實作 + 對應測試(不適用的已記錄理由)
- [x] 無 scope 外變更(`git diff main --cached --stat` 5 個檔案,皆在
      允許清單內;禁止清單資料夾零命中;未新增任何 npm 依賴)
- [x] 無發明 contract 未定義的 endpoint/欄位/權限
- [x] Security AC:未修改任何授權邏輯;圖表只呈現已授權的已載入表格
      資料
- [x] UX AC:圖表只在正確時機顯示;無法量化時誠實退回等寬而非誤導
      比例
- [x] 無 debug 殘留(`grep` 對整個 diff 的新增行搜尋
      skip/only/passWithNoTests/`|| true`/TODO/console.\* 全部零命中)
- [x] 無「mock 通過就當作整合完成」的宣稱;E2E 的 2 個既有 flaky 測試
      已獨立隔離重跑確認與本 story 無關,誠實記錄而非略過不提
- [x] **確認純疊加**:`git diff main --cached --stat` 對 5 個變更檔案
      回傳「208 insertions(+), 1 deletion(-)」,唯一刪除行是 import
      陳述式擴充,確認本 story 沒有刪除或修改任何既有測試斷言

## Assumptions / 計畫外變更 / 未解疑問

- Functional AC 2/3(除既有 RoleGuard)/4/5/6/7 判定不適用——本 story
  是純呈現層的衍生視覺化,沒有新增任何使用者輸入驗證情境、非同步
  dependency、或敏感操作,與 S007-S010 判定理由相同。
- 圖表刻意用「每列第二欄位」統一規則,而非為每個情境各自指定「該用
  哪一欄」——後者需要人工判斷、容易與表格實際欄位脫鉤(重演
  E09-S008 的教訓),前者雖然對 `purchase-order-status` 這個情境
  退化成等寬長條(因為第二欄「供應商」不是數字),但這是誠實的
  簡化而非缺陷:寧可對某些情境顯示較樸素的等寬長條,也不要為了
  「每個情境都要有漂亮的比例圖」而引入特判邏輯或猜測性數字。
- E2E 完整套件跑出的 2 個既有 flaky 測試(E05-S031/E07-S025)已於
  「Gate 紀錄」誠實記錄,並透過獨立隔離重跑 erp-home.spec.ts 確認
  與本 story 無關,系統負載於跑出當下達到異常值(21.95)。

## Rollback

`git revert` 本 story 合併後的 merge commit(`--no-ff`)。無 migration、
無 contract 變更,回滾無副作用。
