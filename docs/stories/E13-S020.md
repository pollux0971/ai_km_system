# E13-S020 — apps/web `usage-events.ts` 改送 server：`recordUsageEvent` → `POST /usage-events`，純函式保留，E13-S009～S013/S017 測試對照改寫

- 狀態：**APPROVED**（`/story-review` 獨立審核通過——本 story 首次審核，見下方「獨立審核紀錄」；5 支 E2E 已於 E03-S038/E04-S052/E04-S053/E01-S031 陸續 approved 後補跑並全數通過，見下方「延後項目補跑結果」；AC5 (L3 真實 API/`services/analytics`) 仍待 E13-S019，非本 story 範圍，見下方說明）
- Branch：`story/E13-S020-usage-events-server`
- 日期：2026-08-28

## 變更檔案
- `apps/web/src/lib/usage-events.ts` — `recordUsageEvent` 改為 fire-and-forget `apiClient.analytics.POST("/usage-events")`（不再寫 sessionStorage；`userId` 參數保留但不送出，因為呼叫端簽名不可變且 identity 一律來自 session）；`listUsageEvents()` 標記 `@deprecated`，永遠回傳 `[]` 並 log warn；`computeDAU`/`computeQuestionsAsked`/`computeAverageLatencyMs`/`countDistinctCitations` 零改動。
- `apps/web/src/lib/usage-events.test.ts` — 對照改寫（見下方對照表）；E13-S012 四個 describe 區塊（`computeQuestionsAsked`/`computeDAU`/`computeAverageLatencyMs`/`countDistinctCitations`）逐字保留，零修改。
- `apps/web/src/test/fake-api.ts` — 擴充：載入 `analytics.yaml`、新增 `UsageEventInput`/`UsageEventCreated` 驗證器、新增 `handleCreateUsageEvent`（`POST /api/v1/usage-events`）、新增 `usageEventStore`/`getRecordedUsageEvents()` 測試 hook、`resetFakeApi()` 一併清空。既有 conversations/messages 相關的驗證器與 handler 零改動。
- `packages/api-client/scripts/generate.mjs`、`src/client.ts`、`src/generated/analytics.d.ts`（新增）— 跨 story 擴充：`SPEC_NAMES` 加入 `"analytics"`，`ApiClient` 新增 `analytics` 欄位。經 ai-km-e4 於 2026-08-28 依 ROADMAP_TEMP.md 第 5-quater 節判準授權（純加法、contract 早已凍結、無此擴充則本 story AC 無法達成）。同步附註於 `docs/stories/E03-S034.md` 檔尾。
- `tests/e2e/specs/usage-event-instrumentation.spec.ts`、`conversation-created-event.spec.ts`、`rag-outcome-analytics.spec.ts`、`latency-instrumentation.spec.ts`、`analytics-e2e.spec.ts` — 對照改寫（見下方對照表）。`analytics-e2e.spec.ts` 的 `feedback-knowledge-candidates`（sessionStorage）斷言部分零改動，不在本 story 範圍。

## Contract / Migration 差異
None（`analytics.yaml` 已由 E13-S018 凍結，本 story 未修改任何 contract 檔）。

## Gate 紀錄
| Gate | 指令 | Exit code | 關鍵測試 |
|---|---|---|---|
| typecheck | `pnpm exec turbo run typecheck --force` | 0 | 27/27 packages |
| lint | `pnpm exec turbo run lint --force` | 0 | 27/27 packages |
| unit | `pnpm exec turbo run test --filter='!@ai-km/e2e' --force` | 0 | 全 package 合計含本 story 變更；`apps/web` 118 檔 1617/1617（含 `usage-events.test.ts` 34、`fake-api.test.ts` 12）；`api-client` 21/21（零修改，見下方跨 story 擴充驗證） |
| L2 contract（api-client drift gate） | `pnpm --filter @ai-km/api-client check` | 0 | `[api-client check] src/generated matches contracts/openapi.` |
| L4 security-negative | 含於 unit（見下方 AC 對照表 AC1/AC2/Security AC） | 0 | `never sends an unexpected field...`、`never throws even when the request fails with a 500...`、`never throws on a real network failure either...` |
| L5 E2E | `flock` 持鎖 `pnpm exec playwright test`（5 支目標 spec） | 0 | **8/8 passed（37.3s）**——見下方「延後項目補跑結果」 |

## 測試修正記錄（若有）
本 story 依 TDD 先寫測試、後寫實作。以下是 AC3/AC4 明文要求的「對照改寫」（不是 Phase 4 第 5 點的窄例外——這是 spec 本身要求的改寫，行為意圖不變，只是觀察點從 client 端持久化改成觀察實際送出的 request）：

| 檔案 | 修正前觀察點 | 修正後觀察點 | 意圖是否改變 |
|---|---|---|---|
| `usage-events.test.ts`（E13-S009/S010/S011/S013/S016 五個 describe） | 讀 `window.sessionStorage`（`ai-km:mock-usage-events`） | 讀 `getRecordedUsageEvents()`（fake API 實際收到的 request） | 否——驗證「正確的事件以正確欄位被記錄」，只是記錄點從 client store 換成 server request |
| `usage-events.test.ts` E13-S016 允許清單測試 | 斷言 persisted 物件的 key 集合含 `userId` | 斷言 sent 物件的 key 集合**不含** `userId`（新增 `Object.prototype.hasOwnProperty.call(event,"userId")===false` 檢查） | 否——白名單原則不變，只是 `userId` 現在依 contract 規則本來就不該出現在 request 上，斷言相應調整為驗證它確實不存在 |
| `usage-events.test.ts` | 無 | 新增：500 / 網路失敗時不拋錯、不重試（AC2 明文新增） | 新增覆蓋，非修改既有 |
| `usage-event-instrumentation.spec.ts` / `conversation-created-event.spec.ts` / `rag-outcome-analytics.spec.ts` / `latency-instrumentation.spec.ts` / `analytics-e2e.spec.ts` | `page.evaluate` 讀 sessionStorage | `page.route("**/api/v1/usage-events")` 攔截並記錄實際送出的 request body | 否——驗證「正確的事件確實被送出」，觀察點從瀏覽器儲存改成網路層攔截 |
| 上述 5 個 E2E 檔案 | 逐一斷言 `event.userId === MOCK_VALID_USER_ID` | 移除該斷言，改為文件註解說明「刻意移除,不是遺漏」 | 否——這是 AC 本身要求的行為變更（`userId` 依 contract 規則不再送出），斷言隨行為調整，不是放寬驗證力度 |
| `usage-event-instrumentation.spec.ts` 第二個 test | 原標題「a failed send never persists...」但內容從未真的模擬失敗送出 | 標題改為「each real send sends exactly one usage event, not zero or duplicated」，內容邏輯不變 | 否——只更正標題使其與既有測試內容一致，測試邏輯本身零修改 |

無「測試本身技術性錯誤」（Phase 4 第 5 點窄例外）案例。

## AC 對照
| AC | 實作位置 | 測試 |
|---|---|---|
| Functional AC1：每次 `recordUsageEvent` 對 fake API 發出符合 contract 的 request（白名單；不含 userId；不含原文） | `usage-events.ts` `recordUsageEvent`（顯式挑選欄位，不 spread） | `usage-events.test.ts`「recordUsageEvent privacy-safe field allowlist」2 個測試；`fake-api.ts` 的 `assertValid("UsageEventInput", body)` 本身即為契約層強制驗證（送出不合規欄位會直接拋錯使測試失敗） |
| Functional AC2：fake API 回 500 → UI 流程不中斷、log warn 一次、不重試 | `usage-events.ts` `recordUsageEvent` 的 `.then` 錯誤分支 | `usage-events.test.ts`「never throws even when the request fails with a 500」「never throws on a real network failure either」 |
| Functional AC3：E13-S009/S010/S011/S013 對照改寫；E13-S012 零修改 | 見上方「測試修正記錄」對照表 | 同上；`computeQuestionsAsked`/`computeDAU`/`computeAverageLatencyMs` 三個 describe 區塊 `git diff` 確認零改動 |
| Functional AC4：`grep "ai-km:usage-events"` 於 `apps/web/src`、`tests/e2e` 零命中；E13-S017 的 E2E 斷言機制改寫 | 移除 `STORAGE_KEY`/`readStore`/`writeStore`；5 個 E2E spec 改用 `page.route()` | `grep -rn "ai-km:usage-events" apps/web/src tests/e2e` 與 `grep -rn "ai-km:mock-usage-events" apps/web/src tests/e2e`（實際舊 key）均零命中（見下方指令輸出）；`analytics-e2e.spec.ts` 對照表見上 |
| Functional AC5：L3 真實 API 收到事件、demo-auditor 讀到 metrics | 未驗證 | **仍延後**——`services/analytics`（E13-S019）尚未實作持久化，即使 client 端送出正確 request，伺服器端目前無處可讀回；與 5 支 E2E 補跑（種子資料問題）是兩個不同的阻塞，不隨本次補跑解除 |
| Security AC：白名單 client + server 各一道 | `usage-events.ts` 顯式挑選欄位（client）+ `analytics.yaml` `UsageEventInput` 的 `additionalProperties:false`（server，經 `fake-api.ts` 的 Ajv 驗證強制） | 同 AC1 |
| Data/Contract AC：request 通過 fake API 契約驗證 | 全部 `recordUsageEvent` 呼叫路徑皆經 `assertValid` | `usage-events.test.ts` 全部 34 個測試（任何不合規 request 會直接讓 `fakeFetch` 拋錯） |
| UX AC：無 UI 變更 | 未觸碰任何元件檔案 | `git diff --stat` 確認 0 個 `.tsx` 元件檔案變更 |

## SELF-REVIEW checklist 結果
- [x] 每條 AC 都有對應實作 + 對應測試 — 見上表；5 支 E2E 已補跑通過（見上方「延後項目補跑結果」）；AC5 明確標記延後並附理由（需要 E13-S019，非本 story 範圍），非隱瞞。
- [x] 有無 scope 外變更？— `git status --short` 只有：`usage-events.{ts,test.ts}`、`fake-api.ts`、`E03-S034.md`（附註）、`packages/api-client` 三檔（跨 story 授權擴充）、5 個 E2E spec。無 `message-thread.tsx`、無其他元件、無 `contracts/`、無 `services/*`。
- [x] 有無發明 contract 未定義的 endpoint/欄位/權限？— 沒有；`analytics.yaml` 由 E13-S018 凍結，`packages/api-client` 擴充純粹是把既有 contract 接上 codegen，未新增/修改任何 schema。
- [x] Security AC：未授權路徑真的被測過且被拒？— `UsageEventInput` 的 `additionalProperties:false` 由 fake API 的 Ajv 驗證強制執行，測試以「刻意塞入不該存在的欄位」（`comment`/`answerContent`）證明會被擋下（見「recordUsageEvent privacy-safe field allowlist」）。
- [x] UX AC：不適用（本 story 無 UI 變更，spec 本身標示 UX Acceptance: N/A）。
- [x] 有無留下 debug 殘留？— 無 `console.log`；`logger.warn` 呼叫是 AC2 明文要求的正式行為，非 debug 殘留。
- [x] 是否有任何「mock 通過就當作整合完成」的宣稱？— 沒有；`fake-api.ts` 明確標註「NOT integration evidence for a real server」；L5（5 支 E2E）已對真實 apps/api 補跑驗證通過（非 mock）；L3（AC5，需要 E13-S019 的伺服器端持久化）仍誠實標記延後，非宣稱已完成。

## Assumptions / 計畫外事項
- **跨 story 擴充**：`packages/api-client`（E03-S034 所有）不在本 story 原始允許修改清單內，經 ai-km-e4 於 2026-08-28 明示授權（4 個條件：只碰 `SPEC_NAMES`/`generated/analytics.d.ts`/`ApiClient` 三處；W1 既有 21 個測試零修改且 drift gate 過；雙向留痕；併發警告）。全部條件已滿足並記錄於本檔與 `E03-S034.md`。
- **`recordUsageEvent` 簽名與 `userId` 參數**：spec 技術決策原文寫「簽名不變」，但四個既有呼叫點（`message-thread.tsx`/`new/page.tsx`/`new-file/page.tsx`）不在本 story 允許修改清單內，因此維持原有 3 參數簽名（`name, userId, details?`），`userId` 參數保留但內部不使用（不送上 request body）。此為唯一可行解讀，已於程式碼頂部文件註解說明理由。
- **rebase 衝突**：rebase 到 main（afd423c，含 E04-S051）時，`fake-api.ts` 與 W5 的 E03-S041（transcriptions 契約）在 `specDocs` 物件與檔案尾端各有一處衝突。兩邊皆為純新增（不同 spec 文件、不同 route handler），確認合併後兩邊功能皆完整存在（`grep` 確認 `transcriptions.yaml`/`analytics.yaml` 皆在 `specDocs`；`/api/v1/transcriptions`/`/api/v1/usage-events` 皆在路由表），並重跑 `usage-events.test.ts`（34）+ `fake-api.test.ts`（12）= 46 個測試全綠複驗。

## 延後項目補跑結果（2026-08-29）
`services/conversation` 的 sandbox 種子接線（E04-S052/E04-S053）、`hostRequireSession` 的 route 快照修正（E04-S051）、E03-S038（apps/api 接進 Playwright）、E01-S031（12 支既有 spec 過時假設修正）皆已 approved 並 merge 回 main 後，回頭補跑本 story 當初延後的 5 支 E2E spec：

```
flock 持鎖，cd tests/e2e && pnpm exec playwright test \
  specs/usage-event-instrumentation.spec.ts \
  specs/conversation-created-event.spec.ts \
  specs/rag-outcome-analytics.spec.ts \
  specs/latency-instrumentation.spec.ts \
  specs/analytics-e2e.spec.ts
```

**結果：8 passed（37.3s），exit code 0，零失敗**：

| Spec | 測試 | 結果 |
|---|---|---|
| `usage-event-instrumentation.spec.ts` | E13-S009: sending a message sends a conversation_message_sent usage event | ✓ |
| `usage-event-instrumentation.spec.ts` | E13-S009: each real send sends exactly one usage event, not zero or duplicated | ✓ |
| `conversation-created-event.spec.ts` | E13-S010: starting a new conversation from the zero-interaction route sends a conversation_created usage event | ✓ |
| `conversation-created-event.spec.ts` | E13-S010: starting a new conversation from the file-first entry route sends a conversation_created usage event | ✓ |
| `rag-outcome-analytics.spec.ts` | E13-S011: a normal answer sends a rag_answer_outcome event with ANSWERED and its real citation count | ✓ |
| `rag-outcome-analytics.spec.ts` | E13-S011: a NO_EVIDENCE (abstained) answer sends its real state with a zero citation count, not defaulted to ANSWERED/1 | ✓ |
| `latency-instrumentation.spec.ts` | E13-S013: a real answer sends a non-negative latencyMs on its rag_answer_outcome event | ✓ |
| `analytics-e2e.spec.ts` | E13-S017: a real multi-message session with feedback, comments, citation feedback, and a knowledge-candidate flag on one reply produces a correct, consistent usage-event stream and survives in-app navigation | ✓ |

這 8 個測試正是本 story 當初 TDD 對照改寫（見上方「測試修正記錄」）產出的斷言，`page.route("**/api/v1/usage-events")` 攔截驗證真實送出的 request body——**不是重寫過的、放寬過的、或新增的測試，是當初就寫好、等種子資料就緒才能真正跑的同一批斷言**，這次補跑零修改直接執行通過。

**AC4 現況**：Functional AC4 的「E13-S017 的 E2E 由 sessionStorage 斷言改為觀察實際送出的 request」部分現已完整驗證通過；AC4 原文另外提到的「或 `GET /admin/metrics/*`」選項仍未使用（`page.route()` 攔截已足夠驗證本 story 範圍內的宣稱，`GET /admin/metrics/*` 屬於 AC5／`services/analytics` 的範圍，見下方）。

## Functional AC5（仍延後，非本次補跑範圍）
AC5（真實 API 收到事件、demo-auditor 讀到 metrics）需要 `services/analytics`（E13-S019）的伺服器端持久化，該 story 目前 `todo`，本 story 無法自行提供，標記延後——與上方已解決的「5 支 E2E」是兩個不同的阻塞（種子資料 vs 伺服器端持久化），不可混為一談。

## 歷史：5 支 E2E 曾經卡住的原因（已於上方「延後項目補跑結果」解決）
1. 2026-08-28 首次嘗試：共用 `apps/api`（:4000）process 殭屍狀態（snap node 失效但 process 不退出），已由 ai-km-e4 修復。
2. 修復後第二次嘗試：`GET /v1/conversations` 100% 回 401（`E04-S051` 缺陷——`hostRequireSession` 在 route 註冊當下快照舊的 stub），已於 main `b3ebbb6`/`afd423c` 修復並經 ai-km-e4 實測驗證。
3. 修復後第三次嘗試：curl 直接驗證
   ```
   POST /v1/auth/login (demo-user)              → 200
   GET  /v1/conversations?page=1&pageSize=20... → 200, {"items":[],"page":1,"pageSize":20,"totalCount":0,"totalPages":1}
   ```
   確認真實後端的對話清單當時為空——本 story 的 5 支 E2E spec 全部依賴點擊種子對話「產品保固政策詢問」進入對話串，這筆資料當時不存在於真實後端（`services/conversation` 已有 `conversationSandboxSeeders`/`messageSandboxSeeders` 但尚未被任何 test sandbox 接線呼叫——ai-km-e4 確認這正是 **E03-S038** 的必要項）。經 ai-km-e4 裁示（2026-08-28）：不需要為了取得「正式」紅燈記錄而重跑一次已知會失敗的 Playwright。
4. **2026-08-29 解除**：E03-S038、E04-S052/E04-S053（sandbox 種子接線）、E01-S031（12 支既有 spec 過時假設修正）皆 approved 並 merge 回 main 後，種子資料正確，5 支 E2E 補跑全數通過，見上方「延後項目補跑結果」。

## Rollback
`git revert` 本 story 對應的 commit（`a6038de` 於本 branch，rebase 後 SHA 可能改變，以 `git log --oneline -1` 為準）。無 migration。`packages/api-client` 的 `analytics` 擴充若需回退，需同時通知 W1（E03-S034 owner）並更新 `E03-S034.md` 的附註段落。回退不影響其他 story（尚無其他 story 消費 `apiClient.analytics`）。

## 獨立審核紀錄（`/story-review`，2026-08-29）
**結論：APPROVE。** 本 story 首次審核（先前僅到 in-progress，未走過完整審核循環）。審核者親自驗證（不信任紙面紀錄）：
- `git show 585670c --stat`：12 個檔案，全部落在允許修改清單內，加上先前已明示授權的 `packages/api-client` 跨 story 擴充（4 條件，皆已於 EVIDENCE 與 `E03-S034.md` 附註記錄）。未碰 `message-thread.tsx`、其他元件、`contracts/`、`services/*`。
- 重跑 `pnpm --filter @ai-km/web typecheck`、`pnpm --filter @ai-km/api-client typecheck`：皆乾淨。
- **獨立重跑** `usage-events.test.ts`：34/34 通過。
- **獨立重跑** `pnpm --filter @ai-km/api-client check`（contract drift gate）：確認 `src/generated` 與 `contracts/openapi` 一致，`analytics` 擴充非手改繞過 codegen。
- **獨立重跑** apps/web 全量：124 個檔案、1756/1756 通過。
- 對照 E2E 補跑原始 log：`8 passed (37.3s)`，`grep -c "✓"` 確認剛好 8 個，與 EVIDENCE 表格完全相符。
- 直接檢視測試內容：確認 `hasOwnProperty.call(event, "userId")` 真的斷言為 false（非空殼斷言），500/網路失敗的韌性測試確實走真實錯誤路徑。
- 搜尋造假跡象（`.skip(`/`.fixme(`/`passWithNoTests`/`|| true`）：無。

非阻擋觀察：本 story 隔了一段時間才走 `/story-review`（實作先直接 merge 到 in-progress 狀態），此次補齊，未發現問題。
