/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/auth/login": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Exchange credentials for a session cookie.
         * @description On success the server sets `ai_km_session` as an **HttpOnly** cookie with `SameSite=Lax` and `Path=/`, adding `Secure` whenever the request arrived over HTTPS. The response body carries the session's 描述 fields only — it never carries the token itself, so no client script can read, log or forward it.
         *
         *     `SameSite=Lax` is the deliberate choice over `Strict`: `Strict` would drop the cookie on a top-level navigation into the app from an internal wiki or email link, logging the user out for no security gain that matters here. Cross-site *writes* are additionally defended by the custom-header requirement in E04-S048.
         *
         *     Failures are distinguished by machine-readable code, never by message: `INVALID_CREDENTIALS` (401) is returned for both an unknown username and a wrong password, so the endpoint cannot be used to enumerate accounts, while `ACCOUNT_DISABLED` (403) is a deliberate exception — a user whose account was turned off needs to know to contact an administrator rather than keep retrying a password that is correct.
         */
        post: operations["login"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/auth/logout": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * End the current session.
         * @description Deletes the session server-side and clears the cookie.
         *
         *     **Idempotent**: logging out with no session, or with an expired or already-deleted one, is still 204. Signing out is the user asking to end up signed out; reporting an error for "you were already signed out" would give a client something to handle that has no useful handling, and would leak whether a token was still valid.
         */
        post: operations["logout"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/auth/session": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * The caller's current session.
         * @description Used on app load and after a reload to decide whether the user is signed in. Returns 401 `UNAUTHENTICATED` when there is no valid session; `@ai-km/auth-client` maps that to `ok: null` rather than an error, because "not signed in" is an expected state, not a failure.
         *
         *     A valid call also refreshes the session's idle window (ADR 0005 §2: 12-hour sliding `last_seen_at` within an absolute 7-day `expires_at`).
         */
        get: operations["getSession"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        /**
         * @description Credentials only. There is deliberately no field naming a user id, role or owner: identity is what the server DERIVES from these credentials, never what the caller asserts.
         * @example {
         *       "username": "demo-user",
         *       "password": "demo-pass-123"
         *     }
         */
        LoginRequest: {
            username: string;
            /**
             * Format: password
             * @description Never logged, never echoed, never included in any error body or audit payload.
             */
            password: string;
        };
        /**
         * @description Field-for-field the `AuthSession` interface in `packages/auth-client/src/index.ts`. Carries no token — see the top-level description.
         * @example {
         *       "userId": "demo-user",
         *       "roles": [
         *         "general_user"
         *       ],
         *       "expiresAt": "2026-09-04T05:12:00.000Z",
         *       "name": "示範使用者",
         *       "email": "demo-user@example.com",
         *       "department": "工程部",
         *       "group": "平台組"
         *     }
         */
        AuthSession: {
            /** @description Stable identity. Derived from the session, never from the request body. */
            userId: string;
            /** @description Role names. Informational for the UI only — the client must never derive a permission the server has not already enforced. An empty array is valid and means "no roles", not "all roles". */
            roles: string[];
            /**
             * Format: date-time
             * @description UTC ISO-8601 absolute expiry (7 days, ADR 0005 §2). The idle window is enforced server-side and is deliberately not exposed — a client must not try to predict its own logout.
             */
            expiresAt: string;
            /** @description Profile display fields (E01-S010) are optional: a real E02-backed session may not populate them, and every session-shaped fixture written before they existed stays valid. */
            name?: string;
            email?: string;
            department?: string;
            group?: string;
        };
        /**
         * @description Exactly the `AuthErrorCode` union in `@ai-km/auth-client`. Consumers branch on these; they must never branch on `message`.
         * @example INVALID_CREDENTIALS
         * @enum {string}
         */
        AuthErrorCode: "VALIDATION_ERROR" | "INVALID_CREDENTIALS" | "ACCOUNT_DISABLED" | "SERVICE_UNAVAILABLE";
        /**
         * @example {
         *       "code": "VALIDATION_ERROR",
         *       "message": "請輸入帳號與密碼。"
         *     }
         */
        ValidationErrorBody: components["schemas"]["Error"] & {
            /** @enum {string} */
            code: "VALIDATION_ERROR";
        };
        /**
         * @example {
         *       "code": "INVALID_CREDENTIALS",
         *       "message": "帳號或密碼不正確。"
         *     }
         */
        InvalidCredentialsBody: components["schemas"]["Error"] & {
            /** @enum {string} */
            code: "INVALID_CREDENTIALS";
        };
        /**
         * @example {
         *       "code": "ACCOUNT_DISABLED",
         *       "message": "此帳號已停用,請聯絡系統管理員。"
         *     }
         */
        AccountDisabledBody: components["schemas"]["Error"] & {
            /** @enum {string} */
            code: "ACCOUNT_DISABLED";
        };
        /**
         * @example {
         *       "code": "UNAUTHENTICATED",
         *       "message": "請先登入。"
         *     }
         */
        UnauthenticatedBody: components["schemas"]["Error"] & {
            /** @enum {string} */
            code: "UNAUTHENTICATED";
        };
        /**
         * @example {
         *       "code": "UNSUPPORTED_MEDIA_TYPE",
         *       "message": "僅接受 application/json。"
         *     }
         */
        UnsupportedMediaTypeBody: components["schemas"]["Error"] & {
            /** @enum {string} */
            code: "UNSUPPORTED_MEDIA_TYPE";
        };
        /**
         * @example {
         *       "code": "SERVICE_UNAVAILABLE",
         *       "message": "認證服務暫時無法使用,請稍後再試。"
         *     }
         */
        ServiceUnavailableBody: components["schemas"]["Error"] & {
            /** @enum {string} */
            code: "SERVICE_UNAVAILABLE";
        };
        /**
         * @example {
         *       "code": "INTERNAL_ERROR",
         *       "message": "系統發生未預期的錯誤。"
         *     }
         */
        InternalErrorBody: components["schemas"]["Error"] & {
            /** @enum {string} */
            code: "INTERNAL_ERROR";
        };
        Error: {
            /**
             * @description Stable, machine-readable failure code. Deliberately free-form (not an enum) — each domain spec defines and documents its own codes; turning this into a shared enum would make every new domain code a breaking change here. Platform-wide codes used across every domain (not specific to one spec's own endpoints) are documented here for discoverability:
             *     `CSRF_HEADER_MISSING` (403) — E04-S048, ADR 0005 addendum. A state-changing (POST/PUT/PATCH/DELETE) request was missing the `x-requested-with` header, or — for `multipart/form-data` requests specifically — had no `Origin`/`Referer` matching the configured allowlist. Returned instead of a domain-specific code so the check can sit in front of every domain uniformly. Never distinguishable from a session-related 401/403 by response shape alone; only the `code` differs.
             */
            code: string;
            message: string;
            details?: {
                [key: string]: unknown;
            };
        };
    };
    responses: {
        /** @description Missing or malformed credentials. */
        ValidationError: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ValidationErrorBody"];
            };
        };
        /** @description Unknown username OR wrong password — deliberately indistinguishable, so this endpoint cannot enumerate accounts. */
        InvalidCredentials: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["InvalidCredentialsBody"];
            };
        };
        /** @description The credentials were correct but the account is disabled. Distinct from 401 on purpose: the user must know to contact an administrator instead of retrying a password that is already right. */
        AccountDisabled: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["AccountDisabledBody"];
            };
        };
        /** @description No valid session. Expected on first load — not an error condition for the client. */
        Unauthenticated: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["UnauthenticatedBody"];
            };
        };
        /** @description Request body was not `application/json`. */
        UnsupportedMediaType: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["UnsupportedMediaTypeBody"];
            };
        };
        /** @description The identity backend could not be reached. */
        ServiceUnavailable: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ServiceUnavailableBody"];
            };
        };
        /** @description Unexpected server error. Never contains a stack trace. */
        InternalError: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["InternalErrorBody"];
            };
        };
    };
    parameters: never;
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    login: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["LoginRequest"];
            };
        };
        responses: {
            /** @description Signed in. The session cookie is set on this response. */
            200: {
                headers: {
                    /** @description `ai_km_session=<opaque>; HttpOnly; SameSite=Lax; Path=/` plus `; Secure` over HTTPS. The value is a 256-bit random token; only its SHA-256 is stored server-side. */
                    "Set-Cookie"?: string;
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthSession"];
                };
            };
            400: components["responses"]["ValidationError"];
            401: components["responses"]["InvalidCredentials"];
            403: components["responses"]["AccountDisabled"];
            415: components["responses"]["UnsupportedMediaType"];
            500: components["responses"]["InternalError"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    logout: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Signed out. No body. The response clears `ai_km_session`. */
            204: {
                headers: {
                    /** @description `ai_km_session=; Max-Age=0; HttpOnly; SameSite=Lax; Path=/` (plus `; Secure` over HTTPS). */
                    "Set-Cookie"?: string;
                    [name: string]: unknown;
                };
                content?: never;
            };
            500: components["responses"]["InternalError"];
        };
    };
    getSession: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The active session. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthSession"];
                };
            };
            401: components["responses"]["Unauthenticated"];
            500: components["responses"]["InternalError"];
        };
    };
}
