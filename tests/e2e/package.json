{
  "name": "@ai-km/e2e",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "echo \"no build step\"",
    "lint": "echo \"no lint step\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "playwright test",
    "test:unit": "vitest run"
  },
  "dependencies": {
    "@ai-km/auth-client": "workspace:*"
  },
  "devDependencies": {
    "@ai-km/admin": "workspace:*",
    "@ai-km/web": "workspace:*",
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
