{
  "name": "@ai-km/tool-asr-readiness",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check-asr": "tsx src/check-asr.ts",
    "verify-asr": "tsx src/verify-asr.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "opencc-js": "^1.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
